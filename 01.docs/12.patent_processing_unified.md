# 특허 처리 통합 설계서 (Agent · Prior Art · Collection→MyKnowledge · Upload Pipeline · Quality/KPI) — 단일 정본

**작성일:** 2025-12-28  
**정책:** 본 문서는 특허 도메인 관련 “단일 정본(canonical)” 문서이며, 분산되어 있던 문서들을 본문으로 완전 통합한다.

포함 범위:
- 선행기술조사 에이전트 설계(입력: 특허 PDF → Prep → 반복 검색 → 조사보고서 템플릿 자동 생성)
- 특허 수집 → MyKnowledge 표출/검색/임베딩 플로우
- 특허 업로드(전문 기반) 파이프라인(PatentPipeline) 및 섹션 감지 품질 지표(KPI) + 실측

## 1. 개요
본 문서는 기존의 단일 도구(`PatentSearchTool`) 기반 특허 분석 시스템을 **Layered Tool Architecture**로 재설계하여, 에이전트가 사용자의 의도(검색, 상세 분석, 권리 분석 등)에 맞춰 최적의 도구를 선택하고 정밀한 데이터를 수집할 수 있도록 하는 것을 목적으로 한다.

> 참고: 지식등록에서 PDF를 업로드하는 “특허 업로드(전문 기반)” 경로는 `PatentPipeline`이 담당하며, 섹션 감지/청구항 파싱 품질(KPI)은 본 문서의 “특허 섹션 감지 품질 지표” 섹션에서 관리한다.

## 2. 아키텍처 설계 (Layered Architecture)

시스템은 다음 3계층으로 구성된다.

### Layer 1: Provider Adapter (저수준 API 클라이언트)
각 데이터 제공자(KIPRIS, SerpAPI 등)의 API 명세와 1:1로 매핑되는 저수준 클라이언트 모듈.

*   **KIPRIS Client (`backend/app/clients/kipris.py`)**
    *   **KIPRIS PLUS API 레퍼런스(정리 문서)**: `01.docs/15.kipris_plus_api_reference.md`
    *   `search_patents`: 특허/실용신안 검색 (Advanced Search)
    *   `get_bibliography`: 서지 정보 조회 (요약, 대표도면 등)
    *   `get_full_text`: 전문(Full Text) 조회 (청구항, 상세설명)
    *   `get_legal_status`: 행정/법적 상태 조회 (등록, 거절, 소멸, 심사이력)
    *   `get_images`: 도면 이미지 조회
*   **Google Patents Client (`backend/app/clients/google_patents.py`)**
    *   `search`: 글로벌 특허 검색
    *   `get_details`: 상세 정보 및 인용 관계

### Layer 2: Functional Tools (기능별 도구)
에이전트가 사용하는 실제 도구(Tool). 사용자의 "의도" 단위로 기능을 추상화한다.

1.  **PatentDiscoveryTool (특허 탐색 도구)**
    *   **목적**: 광범위한 탐색, 리스트 확보.
    *   **기능**: 키워드/출원인/IPC 기반 검색. 가벼운 서지 정보(번호, 제목, 출원인, 날짜, 상태)만 반환.
    *   **Use Case**: "삼성전자의 AI 특허 찾아줘", "최근 3년 반도체 특허 리스트 뽑아줘"

2.  **PatentDetailTool (특허 상세 분석 도구)**
    *   **목적**: 특정 특허의 기술적 내용 심층 분석.
    *   **기능**: 특허 번호를 입력받아 **청구항(Claims)**, **상세설명**, **도면** 등 무거운 데이터를 로딩.
    *   **Use Case**: "이 특허의 핵심 기술이 뭐야?", "청구항 1항 분석해줘"

3.  **PatentLegalTool (권리/행정 분석 도구)**
    *   **목적**: 특허의 법적 유효성 및 권리 상태 확인.
    *   **기능**: 현재 권리 상태(등록/포기/소멸), 존속 기간, 심사 진행 이력, 등록료 납부 현황 확인.
    *   **Use Case**: "이 특허 아직 유효해?", "등록된 특허야 거절된 특허야?"

4.  **PatentCitationTool (인용 분석 도구)** (Optional/Future)
    *   **목적**: 기술의 영향력 및 원천 기술 추적.
    *   **기능**: 인용(Forward)/피인용(Backward) 관계 분석.

### Layer 3: Agent Strategy (에이전트 운용 전략)
에이전트는 사용자의 요청을 분석하여 위 도구들을 순차적 또는 병렬적으로 호출한다.

*   **Scenario A: 기술 동향 조사**
    *   User: "현대차의 수소전지 최근 동향 알려줘"
    *   Agent: `PatentDiscoveryTool` 호출 → 리스트 확보 → 연도별/IPC별 통계 분석 → 요약 보고.
*   **Scenario B: 특정 기술 심층 분석**
    *   User: "공개번호 10-2024-0000000 특허 분석해줘"
    *   Agent: `PatentDetailTool` 호출 → 청구항/요약 확보 → LLM 기술 분석 수행.
*   **Scenario C: FTO(자유실시) 기초 조사**
    *   User: "이 기술 우리가 써도 되는지 확인해줘"
    *   Agent: `PatentDiscoveryTool`로 유사 특허 검색 → `PatentLegalTool`로 유효성(소멸 여부) 확인.

## 3. 데이터 모델 설계

### 3.1. PatentBasicInfo (Discovery 결과)
*   `patent_number`: 출원/등록 번호
*   `title`: 발명의 명칭
*   `applicant`: 출원인
*   `application_date`: 출원일
*   `status`: 요약 상태 (등록/공개/거절 등)
*   `ipc_codes`: 대표 IPC

### 3.2. PatentFullText (Detail 결과)
*   `claims`: 청구항 리스트
*   `description`: 상세 설명
*   `abstract`: 초록
*   `inventors`: 발명자 전체
*   `drawings`: 도면 URL 리스트

### 3.3. PatentLegalStatus (Legal 결과)
*   `current_status`: 현재 법적 상태 (등록, 포기, 소멸, 거절)
*   `registration_date`: 등록일
*   `expiration_date`: 예상 만료일
*   `history`: 심사 이력 (출원서 제출 -> 의견제출통지 -> 등록결정 등)

## 4. 구현 로드맵

1.  **Phase 1: Client Refactoring**
    *   `backend/app/clients/kipris.py` 작성
    *   KIPRIS API Key 관리 및 세션 처리 최적화
    *   각 API 엔드포인트별 메서드 구현

2.  **Phase 2: Tool Implementation**
    *   `PatentDiscoveryTool`, `PatentDetailTool`, `PatentLegalTool` 구현
    *   LangChain BaseTool 상속 및 스키마 정의

3.  **Phase 3: Agent Integration**
    *   `PatentAnalysisAgent`가 새로운 툴들을 사용하도록 로직 변경
    *   LLM 프롬프트 최적화 (툴 선택 가이드)

4.  **Phase 4: Testing & Verification**
    *   단위 테스트 및 시나리오별 통합 테스트

## 5. 구현 결과 (Implementation Results - 2025.11.28)

설계에 따른 구현이 완료되었으며, 주요 변경 사항과 결과는 다음과 같다.

### 5.1. Layer 1: KIPRIS Client 구현 (`backend/app/clients/kipris.py`)
*   **기능 구현**:
    *   `search_patents`: `getAdvancedSearch` API를 사용하여 검색 기능 구현. `ipc_all` 파싱 및 `customer_no` 처리 로직 추가.
    *   `search_applicant_code`: 출원인 명칭으로 특허고객번호를 조회하여 검색 정확도를 높이는 로직 구현.
    *   `get_biblio_detail`: `getBiblioDetailInfo` API 연동 (청구항, 상세 정보 조회용).
*   **개선 사항 (2025.11.28 업데이트)**:
    *   **URL 인코딩 수정**: `aiohttp`와 `urllib.parse.quote`의 이중 인코딩 문제 해결. 검색 정확도 대폭 향상.
    *   **Total Count 파싱**: XML 응답에서 `<totalCount>` 필드를 추출하여 전체 검색 결과 수 제공 기능 추가.
    *   **반환 타입 변경**: `(List[PatentData], int)` 튜플을 반환하여 데이터와 메타데이터(총 개수)를 분리 전달.
    *   XML 파싱 에러 핸들링 강화.
    *   API 키 누락 시 안전한 폴백 처리.
    *   서비스명 오타(`patUtiModInfoSearchSevice`) 수정 및 적용.

### 5.2. Layer 2: Functional Tools 구현 (`backend/app/tools/retrieval/patent_functional_tools.py`)
*   **PatentDiscoveryTool**:
    *   상태: **완료 (Verified)**
    *   테스트 결과: "인공지능 이미지 생성" 쿼리로 8건의 유효한 특허 데이터 확보 확인. "삼성전자" 검색 시 30만 건 이상의 Total Count 정확히 반환 확인.
    *   **반환 포맷 변경**: 기존 `List[Dict]`에서 `Dict{'patents': List, 'total_count': int}` 구조로 변경하여 통계 정보 전달 능력 강화.
    *   특징: 검색 속도가 빠르며, 시각화에 필요한 핵심 메타데이터(출원인, 날짜, IPC 등)를 효율적으로 제공.
*   **PatentDetailTool**:
    *   상태: **구현 완료 (Refinement Needed)**
    *   구조는 완성되었으나, KIPRIS의 상세 정보 API 엔드포인트 특성상 일부 데이터(청구항 등)가 비어있는 경우가 있어 추가 튜닝 필요.
*   **PatentLegalTool**:
    *   상태: **구현 완료 (Refinement Needed)**
    *   기본 골격 구현 완료. 행정 정보 API 연동 최적화 필요.

### 5.3. Layer 3: Agent Integration (`backend/app/agents/patent/patent_analysis_agent.py`)
*   **하이브리드 검색 로직 적용**:
    *   **KR (한국)**: 새로 구현된 `PatentDiscoveryTool`을 사용하여 KIPRIS 직접 연동. 데이터 정합성 및 최신성 확보.
    *   **Global (미국/유럽 등)**: 기존 `PatentSearchTool` (SerpAPI)을 사용하여 글로벌 커버리지 유지.
*   **데이터 매핑 및 통계 처리**:
    *   `_map_discovery_to_patent_data` 메서드를 통해 Layer 2의 결과를 기존 에이전트 파이프라인과 호환되도록 변환.
    *   **Total Count 통합**: `PatentDiscoveryTool`이 반환하는 `total_count`를 에이전트 최종 결과(`total_patents`)에 반영하여, 실제 검색된(retrieved) 개수보다 더 정확한 전체 모집단 규모를 사용자에게 제공.
*   **검증**:
    *   통합 테스트 스크립트(`test_patent_tools.py`)를 통해 모듈 동작 확인 완료.
    *   "삼성전자" 키워드 테스트를 통해 검색 결과 및 총 건수(30만+ 건) 정상 출력 확인.

### 5.4. 특허 수집 관리 시스템 고도화 (2025-12-24)

#### 배경 및 문제점
기존 특허 수집 시스템은 Celery 비동기 작업으로 KIPRIS 검색 및 데이터 저장을 수행했으나, 사용자 피드백에서 다음과 같은 UX 문제가 발견되었다:

1. **상태 모호성**: 수집 완료 시 "completed" 상태만 표시되어, 0건 수집이 실패인지 검색 조건 문제인지 구분 불가
2. **결과 추적 부재**: 각 설정의 마지막 실행 이력을 확인할 방법 없음
3. **피드백 부족**: 진행 중/성공/실패 상태 구분이 시각적으로 명확하지 않음

#### 개선 구현 사항

**1) 명확한 4단계 상태 관리**

`TbPatentCollectionTasks.status` 필드 활용:
- `pending`: Celery 작업 대기 중
- `running`: KIPRIS API 호출 및 DB 저장 진행 중
- `completed`: 정상 완료 (0건 포함)
- `failed`: 예외 발생으로 인한 실패

**2) 상태별 사용자 메시지 자동 생성**

프론트엔드 `pollTask()` 함수가 백엔드 상태를 기반으로 적절한 메시지 생성:

```typescript
interface TaskStatus {
  status: 'pending' | 'running' | 'completed' | 'failed';
  message?: string;  // 자동 생성된 사용자 메시지
  completedAt?: string;  // 완료 시각
  collected: number;
  errors: number;
}

// 메시지 생성 로직
if (status === 'completed') {
  if (collected === 0) {
    message = '⚠️ 검색 조건에 맞는 특허가 없습니다. 검색 조건을 조정해보세요.';
  } else {
    message = `✅ 수집 완료: ${collected}건 성공${errors > 0 ? `, ${errors}건 실패` : ''}`;
  }
}
```

**3) 마지막 수집 결과 영속화**

스키마 확장 (`TbPatentCollectionSettings`):
```python
last_collection_date = Column(DateTime(timezone=True), nullable=True)
last_collection_result = Column(JSONB, nullable=True)
# 형식: {"collected": 25, "errors": 2}
```

작업 완료 시 자동 업데이트 (`PatentCollectionService.update_task_progress()`):
```python
if status == "completed":
    await self.session.execute(
        update(TbPatentCollectionSettings)
        .where(TbPatentCollectionSettings.setting_id == task.setting_id)
        .values(
            last_collection_date=datetime.utcnow(),
            last_collection_result={"collected": collected_count, "errors": error_count}
        )
    )
```

**4) UI/UX 시각화 개선**

- **색상 구분**: 상태별 텍스트 색상 자동 적용
  - 🔵 파란색 (`text-blue-700`): 실행 중
  - 🟡 노란색 (`text-yellow-700`): 0건 수집 (주의)
  - 🟢 녹색 (`text-green-700`): 성공 (건수 > 0)
  - 🔴 빨간색 (`text-red-700`): 실패

- **조건부 렌더링**:
  - 진행 중일 때만 프로그레스 바 표시
  - 완료 시 결과 요약 박스 5초간 표시 후 자동 제거
  - 비활성 상태일 때 마지막 수집 정보 카드 표시

**5) 구현 파일 목록**

| 계층 | 파일 | 변경 내용 |
|------|------|----------|
| Model | `backend/app/models/patent/collection_models.py` | `last_collection_result` JSONB 컬럼 추가 |
| Service | `backend/app/services/patent/collection_service.py` | `update_task_progress()`에서 completed 시 설정 업데이트 |
| API | `backend/app/api/v1/patent_collection.py` | `PatentCollectionSettingResponse`에 필드 추가 |
| Migration | `backend/alembic/versions/20251224_add_last_collection_result.py` | 스키마 변경 마이그레이션 |
| Frontend | `frontend/src/pages/user/PatentCollectionSettings.tsx` | `pollTask()` 로직 개선, UI 컴포넌트 리팩토링 |

**6) 사용자 시나리오**

*시나리오 A: 정상 수집*
1. 사용자가 "수집 시작" 클릭
2. "🚀 수집 작업을 시작합니다..." (pending)
3. "🔄 수집 중... (15/100)" + 프로그레스 바 (running)
4. "✅ 수집 완료: 25건 성공" (completed, 녹색)
5. 5초 후 자동 제거 → "마지막 수집: 2025-12-24 10:30 / 25건 수집"

*시나리오 B: 0건 수집 (검색 조건 문제)*
1. 동일 흐름
2. "⚠️ 검색 조건에 맞는 특허가 없습니다. 검색 조건을 조정해보세요." (completed, 노란색)
3. 사용자는 실패가 아니라 조건 재검토가 필요함을 명확히 인지

*시나리오 C: 작업 실패*
1. KIPRIS API 오류 또는 DB 저장 예외 발생
2. "❌ 수집 작업이 실패했습니다" (failed, 빨간색)
3. 백엔드 로그에 상세 오류 기록

#### 성과 및 효과

- **사용자 혼란 해소**: 0건 수집과 실패를 명확히 구분
- **운영 효율성**: 각 설정의 실행 이력으로 활용도 파악 가능
- **자가 진단 가능**: 0건 시 자동 안내 메시지로 사용자 스스로 조건 조정
- **UI 일관성**: 상태별 색상 및 아이콘으로 시각적 가독성 향상

## 6. 향후 고도화 계획 (Future Improvements)

### 6.1. 데이터 깊이 확보 (Deep Dive)
*   **전문(Full Text) 확보**: 현재 서지 정보 위주의 데이터를 넘어, `PatentDetailTool`이 청구항 전문과 상세 설명을 완벽하게 가져오도록 KIPRIS의 `getFullTextInfo` 등 추가 API 발굴 및 연동.
*   **법적 상태 상세화**: 단순 등록/거절 상태를 넘어, 심사 진행 단계(의견제출통지, 보정서 제출 등)를 타임라인으로 시각화할 수 있도록 `PatentLegalTool` 고도화.

#### (추가 메모) 업로드 파이프라인 기반 전문 처리 품질(2025-12-28 실측)

서지 기반(KIPRIS) `PatentDetailTool` 고도화와 별개로, 지식등록에서 PDF를 업로드하는 경로는 `PatentPipeline`이 전문 추출 후 `PatentSectionDetector`로 섹션 감지를 수행한다.

- 테스트 문서: `backend/tests/1020240027504.pdf` (업로드 경로)
- 실측 요약(document_id=80): 섹션 커버리지 80%(8/10), 도면/청구항 중복 감지(drawings 5회, claims 3회), 청구항 개별 항 분리는 1항만 성공

의미:
- 조사보고서 자동 생성/권리범위 요약의 품질을 올리려면, (1) 섹션 중복 축소와 (2) 청구항 파싱 안정화가 우선순위가 된다.

### 6.2. 에이전트 지능화
*   **자동 심층 분석**: 검색 결과 중 사용자가 관심을 보일만한 핵심 특허(Top 3)에 대해서는 에이전트가 스스로 `PatentDetailTool`을 호출하여 요약 정보를 미리 생성하는 로직 추가.
*   **FTO(자유실시) 분석 자동화**: 청구항 비교 분석 기능을 에이전트 스킬로 내재화.

### 6.3. 시각화 고도화
*   **IPC 네트워크 맵**: `ipc_all` 데이터를 활용하여 기술 간의 융합 관계를 보여주는 네트워크 그래프 추가.
*   **출원인-기술 매트릭스**: 경쟁사들이 어떤 기술 분야에 집중하고 있는지 보여주는 히트맵 생성.

## 7. 선행기술조사 Agentic AI 설계 (Prior Art Search with ReAct)

본 섹션은 “특허 문서 → 서지/핵심요소 추출 → KIPRIS 입력값 자동 구성 → ReAct 반복 검색/정제 → 선행기술조사보고서 생성” 플로우를 제품 코드에 연결하기 위한 구체 설계이다.

### 7.1. 목표 / 비목표
*   **목표**:
    *   특허 명세서(또는 아이디어 문서) 1건을 입력으로 받아, KIPRIS(우선) 기반의 재현 가능한 검색 전략을 수립하고 반복 개선하여 선행기술 후보를 수집한다.
    *   후보 특허의 핵심 유사점/차이점 및 법적 상태를 요약하여 “선행기술조사보고서” 템플릿으로 출력한다.
    *   구조적으로는 Provider Adapter 패턴을 유지하여, 추후 USPTO/Google Patents 등으로 자연스럽게 확장한다.
*   **비목표(초기 버전)**:
    *   법률적 판단을 대체하는 FTO/무효가능성에 대한 확정 결론 제공.
    *   모든 국가/언어를 동일 품질로 커버(우선순위는 KR(KIPRIS) → Global 확장).

### 7.2. 입력/출력 정의
*   **입력(MVP 1차 타깃)**:
    *   **기존 특허 PDF 1건** (지식등록 업로드 또는 내부 문서로 이미 존재하는 PDF)
        *   한국(KIPO) 공개/등록 공보 형식을 우선 지원한다.
        *   연구노트/발명 설명 문서는 추후 동일한 스키마(아래 `TargetPatentSpec`/`InventionModel`)로 매핑하여 확장한다.
*   **입력(확장)**:
    *   URL 링크(예: KIPRIS 상세 페이지, 공개/등록 공보 URL)
    *   DOCX/Markdown(연구노트) — “특허 큰 틀” 기반 섹셔닝/구조화로 대응
*   **출력(MVP 목표)**:
    *   **선행기술조사보고서 템플릿 자동 생성**
        *   구조화 JSON(`PriorArtReport`) + Markdown/HTML 렌더
        *   (선택) PDF 렌더는 2차 단계(HTML→PDF 등)로 분리
    *   재현 가능한 검색 로그(검색식, 필터, 반복 단계별 의사결정)

#### 7.2.1. MVP 실행 계약(권장)

MVP는 “문서 입력 → 보고서 파일 1개 생성”까지 end-to-end가 보장되어야 한다.

- 입력(권장): `document_id` 또는 `file_id` (지식등록/문서관리에서 이미 생성된 엔트리)
- 출력(권장):
  - `report_id` (보고서 엔티티)
  - `report_json` (구조화)
  - `report_markdown` 또는 `report_html` (사용자 다운로드/열람)
  - `search_log` (재현성)

### 7.3. 전체 처리 흐름 (End-to-End)
0) **Prep(필수) — 입력 특허 PDF를 “검색 가능한 형태”로 변환**
    *   선행기술조사 에이전트의 실질적인 시작점은 검색이 아니라 **Prep 도구**다.
    *   목적: 특허 PDF → (a) 정규화 텍스트/섹션/청구항 → (b) 검색 시드(query seed) → (c) 반복 검색이 가능한 조사 단위(IR)
    *   구현 재사용(권장): 지식등록 업로드 경로의 `PatentPipeline` 산출물을 그대로 활용한다.
        *   `intermediate/multimodal/{document_id}/extraction_full_text.txt`
        *   `intermediate/multimodal/{document_id}/patent_sections.json`
        *   청크/임베딩(`derived/.../chunks/...`)은 “내부 검색(로컬 RAG)”에 유용하고,
          KIPRIS 검색을 위해서는 별도로 “검색 시드”를 뽑아야 한다.
    *   산출물(권장):
        *   `TargetPatentSpec` + `InventionModel` + `QuerySeedSet`

1) **문서 이해(Extraction / Understanding)**
    *   Prep 결과에서 아래 정보를 추출/정규화한다.
        *   서지: 발명의 명칭, 출원인/발명자, 출원/공개/등록 번호(가능 시), 출원일/공개일/우선권, 국가
        *   기술요약: 요약(abstract), 해결 과제, 핵심 구성요소, 주요 효과
        *   청구항: **독립항(1항) 우선** + 종속항은 요약 수준
        *   분류: IPC/CPC(가능 시), 키워드
    *   산출물: `TargetPatentSpec` + `InventionModel`

2) **KIPRIS 검색 입력값 자동 구성(Query Synthesis)**
    *   `TargetPatentSpec` → KIPRIS Advanced Search에 투입 가능한 “검색식 후보군”을 생성한다.
    *   KIPRIS PLUS API 파라미터/응답 구조는 별도 문서 `01.docs/15.kipris_plus_api_reference.md`를 기준으로 한다.
    *   생성 전략(예시):
        *   (A) 제목/요약 키워드 기반 OR 묶음 + 핵심 구성요소(필수어) AND
        *   (B) IPC/CPC 기반 필터 + 키워드 보강
        *   (C) 출원인/발명자 시드(과도한 편향 방지를 위해 낮은 가중치)
        *   (D) 청구항 독립항에서 추출한 기능/구성요소 기반 조합
    *   산출물: `SearchPlan` (query_variants, 필터, 가드레일)

3) **ReAct 반복 검색/정제(Agent Loop)**
    *   에이전트는 “현재 검색식으로 충분한 커버리지가 확보되었는가?”를 기준으로 반복 수행한다.
    *   각 반복 단계(Iteration)에서 수행 가능한 행동(Action) 예시:
        *   `PatentDiscoveryTool`로 검색 실행(필요 시 여러 query variant를 순차 실행)
        *   결과의 상위 N건을 샘플링해 요약/키워드/IPC를 관찰하고, 검색식의 누락/과잉을 진단
        *   검색식 수정(동의어 추가, 필수어/제외어 조정, IPC 확장/축소)
        *   중복 제거/클러스터링으로 후보군의 다양성 확보
    *   종료 조건(초기 권장):
        *   최대 반복 횟수/시간 제한
        *   유효 후보(Top-K)에서 신규 정보(새 IPC/새 키워드/새 패밀리)가 더 이상 증가하지 않을 때

4) **상세/법적 상태 보강(Enrichment)**
    *   최종 후보 Top-K에 대해:
        *   `PatentDetailTool`: 서지/요약/청구항/상세설명(가능 범위) 확보
        *   `PatentLegalTool`: 법적 상태/권리 존속/이력 요약
    *   산출물: `CandidatePatent[]` (원문/요약/법적상태 포함)

5) **유사도 및 근거 작성(Scoring & Evidence)**
    *   유사도는 단일 수치로 고정하지 않고, “근거 중심”으로 구성한다.
        *   텍스트 유사도(요약/청구항 기반)
        *   구성요소 매칭(핵심 구성요소가 후보에 존재하는지)
        *   분류/키워드 일치도(IPC/CPC)
    *   산출물: `Comparison[]` (유사점/차이점, 리스크 포인트, 근거 인용)

6) **보고서 템플릿 생성(Reporting) — MVP 필수**
    *   보고서 섹션(권장):
        *   조사 목적/범위
        *   입력 문서 요약(서지 + 핵심요소)
        *   검색 전략(반복 단계별 검색식/필터/의사결정 로그)
        *   검색 결과 요약(Top-K 후보 리스트)
        *   후보별 상세 비교(핵심 유사점/차이점/법적상태)
        *   결론(초기 관찰/추가 조사 권고; 법률 자문 아님 명시)

#### 7.3.1. Prep 도구(MVP) 상세 — PriorArtPrepTool / InventionModelBuilder

Prep은 “특허 PDF 입력”을 전제로 아래 3가지를 반드시 만든다.

1) **청구항 기반 핵심 구성요소 추출(필수)**
    - 독립항(1항)의 구성요소/한정요소를 최대한 안정적으로 추출
    - 현재 업로드 파이프라인에서 청구항 파싱이 불안정한 실측이 있으므로,
      MVP에서는 “1항 텍스트 자체 + 구성요소 후보 리스트”를 함께 저장해 손실을 방지한다.

2) **검색 시드(QuerySeedSet) 생성(필수)**
    - 키워드(국/영 혼합 가능), 동의어/확장어, 필수어/제외어
    - IPC 후보(가능하면), 날짜 범위(옵션)
    - 최소 3종의 query variant(broad / balanced / strict)

3) **보고서 입력값으로 쓰일 InventionModel 생성(필수)**
    - 보고서의 “대상 발명 요약”, “구성요소”, “차별점 후보”, “조사 범위”를 채울 수 있어야 한다.

권장 산출물 스토리지 키(예: S3):
- `intermediate/prior_art/{document_id}/target_spec.json`
- `intermediate/prior_art/{document_id}/invention_model.json`
- `intermediate/prior_art/{document_id}/query_seed_set.json`

#### 7.3.2. 반복 검색 루프의 최소 형태(권장)

MVP에서 처음부터 완전 자율 ReAct를 과도하게 만들 필요는 없다.

- Iteration 1: broad(키워드 OR 중심) + 기간/국가 기본 필터
- Iteration 2: balanced(IPC + 핵심 구성요소 AND) + 제외어 추가
- Iteration 3: strict(독립항 핵심구성요소 AND) + 결과 다양성 확보(클러스터링)

3회 루프의 산출물만으로도 “검색 로그 + 후보 리스트 + 비교표”를 보고서 템플릿에 넣을 수 있다.

### 7.4. 데이터 모델(초안)
*   `TargetPatentSpec`
    *   `title`, `abstract`, `claims_independent[]`, `applicants[]`, `inventors[]`, `ipc[]`, `cpc[]`, `country`, `dates{}`
    *   `core_components[]`, `core_effects[]`, `keywords[]`

*   `InventionModel` (Prep의 핵심 IR)
    *   `problem`, `solution`, `effects`
    *   `independent_claim_text` (원문 보존)
    *   `components[]` (구성요소 후보 + 동의어)
    *   `novelty_points[]` (차별점 후보)
    *   `exclude_terms[]` (불필요 후보 제거용)
    *   `ipc_candidates[]`

*   `QuerySeedSet`
    *   `keywords_primary[]`, `keywords_expanded[]`, `synonyms_map{}`
    *   `required_terms[]`, `excluded_terms[]`
    *   `query_variants[{name, query_string, filters}]`
*   `SearchPlan`
    *   `query_variants[]`, `filters{ipc/cpc/date/country}`, `stop_conditions{}`
    *   `guardrails{max_iterations,max_results_per_query,rate_limit}`
*   `SearchIteration`
    *   `query_used`, `result_stats{total_count,retrieved}`, `observations[]`, `refinement_decision`
*   `CandidatePatent`
    *   `publication_number`, `title`, `abstract`, `ipc[]`, `assignee`, `url`, `legal_status{}`
    *   `evidence{matched_components[],key_snippets[]}`
*   `PriorArtReport`
    *   `target: TargetPatentSpec`, `iterations[]`, `candidates[]`, `comparisons[]`, `recommendations[]`

#### 7.4.1. 보고서 템플릿(권장 출력 스키마)

보고서 템플릿은 최소한 아래를 포함해야 한다(= 자동 생성 가능).

- 표지/메타: 조사 ID, 생성일, 입력 문서(파일명/식별자), 조사 범위(국가/기간/데이터소스)
- 대상 발명 요약: 문제/해결/효과, 독립항 요약, 핵심 구성요소 리스트
- 검색 전략 로그: Iteration별 query/filter/의사결정/결과 수
- 결과 요약: Top-K 리스트(번호/제목/출원인/IPC/요약/법적상태)
- 후보별 비교: 유사점/차이점, 근거 스니펫(요약/청구항 일부), 리스크/추가조사 포인트
- 면책: 법률 자문 아님

---

### 7.7. MVP 구현 체크리스트(권장)

아래는 “특허 PDF 입력 → KIPRIS 반복 검색 → 조사보고서 템플릿 자동 생성”이 실제로 돌아가기 위한 최소 구현 단위이다.

1) Prep 서비스
    - `PriorArtPrepService`: `document_id`를 받아 (a) `PatentPipeline` 산출물 확인/생성 (b) `InventionModel` + `QuerySeedSet` 생성
2) 검색 실행 서비스
    - `PriorArtSearchService`: `QuerySeedSet`을 받아 KIPRIS 반복 검색(3회 루프) + 후보 정리
3) 근거/비교 생성
    - `PriorArtEvidenceService`: Top-K 후보에 대해 Detail/Legal 보강 + 근거 스니펫 생성
4) 보고서 렌더
    - `PriorArtReportRenderer`: `PriorArtReport` → Markdown/HTML
5) API/저장
    - API 1개로 end-to-end 실행(동기/비동기 선택)
    - 결과는 JSON + 렌더 결과를 파일/스토리지로 저장하고 다운로드 가능하게 제공

### 7.5. 기존 코드와의 연결 포인트(권장)
*   **ReAct 루프 구현 기반**: `backend/app/agents/patent/patent_analysis_agent_v2.py`
    *   기존 LangGraph ReAct 스타일을 유지하되, “선행기술조사”에 맞는 상태(`TargetPatentSpec`, `SearchPlan`, `SearchIteration`)를 추가하는 방향이 안전하다.
*   **도구 계층 재사용**: `backend/app/tools/retrieval/patent_functional_tools.py`
    *   Discovery/Detail/Legal 3종 도구를 “수집/보강” 단계에 그대로 사용한다.
*   **Provider Adapter 확장(USPTO)**:
    *   Layer 1에 `USPTOClient`(또는 `uspto.py`)를 추가하고, Discovery/Detail/Legal의 동일 인터페이스를 제공한다.
    *   Layer 2/3는 Provider가 바뀌어도 동일한 `CandidatePatent` 스키마를 사용하도록 매핑 계층을 둔다.

### 7.6. 운영/품질 가드레일
*   **재현성**: 보고서에 “검색식/필터/반복 로그”를 반드시 포함한다.
*   **비용/속도**: 반복 횟수, 상세 조회 Top-K, 레이트리밋을 기본 제한으로 둔다.
*   **신뢰성**: KIPRIS 장애/응답 스키마 변경 대비(에러 분류, 재시도, 부분 결과 반환).
*   **컴플라이언스**: 결과는 참고용이며 법률 자문이 아님을 템플릿에 명시한다.
---

## 8. 특허 정보 자동 수집 시스템 (Patent Auto-Collection System)

> **최초 구현**: 2025-12-22  
> **상태**: ✅ 프로덕션 배포 완료  
> **통합**: KIPRIS API + Celery + S3 + Vector Search

### 8.1. 시스템 개요

KIPRIS API를 통한 특허 자동 수집 시스템으로, 사용자가 UI에서 검색 조건을 설정하면 백그라운드에서 특허를 수집하고 벡터 검색 가능한 형태로 저장합니다.

#### 8.1.1. 주요 기능
- **자동 수집**: 키워드, IPC 코드, 출원인 기반 KIPRIS 검색
- **스케줄링**: 수동/일간/주간/월간 자동 수집
- **PDF 관리**: KIPRIS PDF 자동 다운로드 및 S3 저장
- **벡터 검색**: 임베딩 자동 생성으로 의미 기반 검색 지원
- **실시간 모니터링**: Celery 작업 진행률 실시간 확인

#### 8.1.2. 데이터 흐름
```
[Frontend] 수집 설정 생성
    ↓
[Backend API] 설정 저장 + Celery 작업 dispatch
    ↓
[Celery Worker] KIPRIS 검색
    ↓
[처리 파이프라인]
    ├─ PostgreSQL 저장 (TbPatentBibliographicInfo)
    ├─ PDF 다운로드 → S3 업로드
    ├─ 임베딩 생성 (Bedrock/Azure)
    └─ 벡터 인덱싱 (pgvector)
    ↓
[지식 컨테이너] 특허 문서 표시 + 검색 가능
```

### 8.2. UI 설계

#### 8.2.1. 수집 설정 화면 (CollectionSettingsPanel)

**위치**: `frontend/src/pages/user/my-knowledge/components/PatentCollectionSettings.tsx`

**주요 컴포넌트**:
```typescript
interface CollectionSettings {
  container_id: string;           // 저장할 지식 컨테이너
  search_config: {
    keywords: string[];           // 검색 키워드
    ipc_codes: string[];          // IPC 분류 코드
    applicants: string[];         // 출원인명
  };
  max_results: number;            // 최대 수집 건수
  schedule_type: 'manual' | 'daily' | 'weekly' | 'monthly';
  auto_download_pdf: boolean;     // PDF 자동 다운로드
  auto_generate_embeddings: boolean; // 임베딩 자동 생성
}
```

**UI 구성**:
1. **검색 조건 입력**
   - 키워드 입력 (다중 입력, 쉼표 구분)
   - IPC 코드 선택 (드롭다운 + 자동완성)
   - 출원인 입력 (다중 입력)

2. **수집 옵션**
   - 최대 수집 건수 (슬라이더: 10~1000)
   - PDF 다운로드 여부 (토글)
   - 임베딩 생성 여부 (토글)

3. **스케줄 설정**
   - 수집 주기 (라디오 버튼: 수동/일간/주간/월간)
   - 다음 실행 시각 표시 (자동 계산)

#### 8.2.2. 실시간 모니터링 (CollectionMonitorPanel)

**위치**: `frontend/src/pages/user/my-knowledge/components/PatentCollectionMonitor.tsx`

**모니터링 정보**:
```typescript
interface CollectionStatus {
  task_id: string;
  state: 'PENDING' | 'PROGRESS' | 'SUCCESS' | 'FAILURE';
  current: number;        // 현재 처리 건수
  total: number;          // 전체 건수
  message: string;        // 상태 메시지
  result?: {
    saved_count: number;  // 저장 성공
    skipped_count: number; // 중복 스킵
    failed_count: number;  // 실패
  };
}
```

**UI 요소**:
- **진행률 바**: 퍼센트 표시 + 현재/전체 건수
- **상태 메시지**: "KIPRIS 검색 중...", "10/50 특허 저장 중..."
- **실시간 로그**: WebSocket 또는 3초 폴링으로 업데이트
- **완료 통계**: 성공/스킵/실패 건수 요약

#### 8.2.3. 수집 이력 조회

**테이블 구성**:
| 수집 시각 | 검색 조건 | 결과 | 상태 | 액션 |
|----------|----------|------|------|------|
| 2025-12-22 14:30 | AI, G06N | 45건 | 완료 | 상세 |
| 2025-12-21 10:00 | 삼성전자 | 120건 | 완료 | 상세 |

**필터링**:
- 기간 선택 (최근 7일/30일/전체)
- 상태별 필터 (성공/실패/진행중)

### 8.3. 백엔드 아키텍처

#### 8.3.1. API 엔드포인트

**라우터**: `backend/app/api/v1/patent_collection.py`

```python
# 수집 설정 관리
POST   /api/v1/patent-collection/settings          # 설정 생성
GET    /api/v1/patent-collection/settings          # 설정 목록 조회
PUT    /api/v1/patent-collection/settings/{id}     # 설정 수정
DELETE /api/v1/patent-collection/settings/{id}     # 설정 삭제

# 수집 실행 및 모니터링
POST   /api/v1/patent-collection/start/{setting_id} # 수집 시작
GET    /api/v1/patent-collection/status/{task_id}   # 진행률 조회
POST   /api/v1/patent-collection/cancel/{task_id}   # 수집 취소

# 수집 이력
GET    /api/v1/patent-collection/history            # 이력 조회
GET    /api/v1/patent-collection/statistics         # 통계 조회
```

#### 8.3.2. Celery 작업 큐

**메인 작업**: `backend/app/tasks/patent_collection_tasks.py`

```python
@celery_app.task(bind=True, base=BaseTask)
def collect_patents_from_kipris(
    self,
    setting_id: int,
    user_emp_no: str
) -> dict:
    """
    KIPRIS API를 통한 특허 수집 메인 작업
    
    처리 단계:
    1. 수집 설정 로드
    2. KIPRIS 검색 (KIPRISClient.search_patents)
    3. 각 특허마다:
       - TbPatentBibliographicInfo 저장
       - TbFileBssInfo 생성
       - PDF 다운로드 (선택)
       - S3 업로드 (선택)
       - 임베딩 생성 (선택)
    4. 결과 반환 (성공/스킵/실패 건수)
    """
```

**진행률 업데이트**:
```python
# Celery 작업 내부에서 호출
self.update_state(
    state='PROGRESS',
    meta={
        'current': processed_count,
        'total': total_count,
        'message': f'{patent.title} 처리 중...'
    }
)
```

**에러 처리**:
- `autoretry_for`: KIPRIS API 일시 장애 시 자동 재시도
- `max_retries`: 3회
- `retry_backoff`: 지수 백오프 (2초 → 4초 → 8초)

#### 8.3.3. 데이터 모델

**특허 서지정보**: `backend/app/models/patent/tb_patent_bibliographic_info.py`

```python
class TbPatentBibliographicInfo(Base):
    __tablename__ = "tb_patent_bibliographic_info"
    
    patent_id = Column(BigInteger, primary_key=True)
    application_number = Column(String(50), unique=True, index=True)
    publication_number = Column(String(50), index=True)
    
    # 발명 정보
    title = Column(Text, nullable=False)
    abstract = Column(Text)
    
    # 출원인/발명자
    applicant_name = Column(String(500))
    inventor_names = Column(ARRAY(String))
    
    # 분류 정보
    ipc_codes = Column(ARRAY(String))
    
    # 날짜 정보
    application_date = Column(Date)
    publication_date = Column(Date)
    
    # 법적 상태
    legal_status = Column(String(50))
    jurisdiction = Column(String(10), default='KR')
    
    # 연결
    file_bss_info_sno = Column(BigInteger, ForeignKey("tb_file_bss_info.file_bss_info_sno"))
```

**수집 설정**: `backend/app/models/patent/tb_patent_collection_settings.py`

```python
class TbPatentCollectionSettings(Base):
    __tablename__ = "tb_patent_collection_settings"
    
    setting_id = Column(BigInteger, primary_key=True)
    user_emp_no = Column(String(20), nullable=False, index=True)
    container_id = Column(String(50), nullable=False)
    
    # 검색 조건 (JSON)
    search_config = Column(JSONB, nullable=False)
    # 예: {"keywords": ["AI"], "ipc_codes": ["G06N"], "applicants": ["삼성"]}
    
    # 수집 옵션
    max_results = Column(Integer, default=100)
    schedule_type = Column(String(20), default='manual')
    auto_download_pdf = Column(Boolean, default=False)
    auto_generate_embeddings = Column(Boolean, default=True)
    
    # 상태
    is_active = Column(Boolean, default=True)
    last_run_at = Column(DateTime(timezone=True))
    next_run_at = Column(DateTime(timezone=True))
```

**수집 로그**: `backend/app/models/patent/tb_patent_collection_logs.py`

```python
class TbPatentCollectionLog(Base):
    __tablename__ = "tb_patent_collection_logs"
    
    log_id = Column(BigInteger, primary_key=True)
    setting_id = Column(BigInteger, ForeignKey("tb_patent_collection_settings.setting_id"))
    task_id = Column(String(255), unique=True, index=True)
    
    # 결과
    status = Column(String(20))  # SUCCESS, FAILURE, CANCELLED
    saved_count = Column(Integer, default=0)
    skipped_count = Column(Integer, default=0)
    failed_count = Column(Integer, default=0)
    
    # 에러 정보
    error_message = Column(Text)
    
    # 시각
    started_at = Column(DateTime(timezone=True))
    completed_at = Column(DateTime(timezone=True))
```

### 8.4. 데이터 파이프라인

#### 8.4.1. KIPRIS → PostgreSQL

**서비스**: `backend/app/services/patent/collection_service.py`

```python
class PatentCollectionService:
    async def save_patent_to_database(
        self,
        patent_data: Dict[str, Any],
        container_id: str,
        user_emp_no: str,
        auto_generate_embeddings: bool = True
    ) -> Optional[int]:
        """
        KIPRIS 특허 데이터를 데이터베이스에 저장
        
        단계:
        1. 중복 체크 (application_number)
        2. TbPatentBibliographicInfo 생성
        3. TbFileBssInfo 생성 (document_type='patent')
        4. 임베딩 생성 (선택)
        
        Returns:
            file_bss_info_sno (문서 ID)
        """
```

**XML 파싱**: `backend/app/services/patent/kipris_client.py`

```python
class KIPRISClient:
    async def search_patents(
        self,
        keywords: List[str] = None,
        ipc_codes: List[str] = None,
        applicants: List[str] = None,
        start_date: str = None,
        end_date: str = None,
        max_results: int = 100
    ) -> List[Dict[str, Any]]:
        """
        KIPRIS AdvancedSearch API 호출
        
        응답 XML 파싱:
        - applicationNumber: 출원번호
        - inventionTitle: 발명명칭
        - abstract: 초록
        - applicantName: 출원인
        - inventorName: 발명자
        - ipcNumber: IPC 코드
        - applicationDate: 출원일
        - publicationNumber: 공개번호
        - publicationDate: 공개일
        """
```

#### 8.4.2. PDF → S3

**PDF 다운로드**: `backend/app/services/patent/kipris_client.py`

```python
async def download_patent_pdf(
    self,
    application_number: str,
    save_path: str
) -> bool:
    """
    KIPRIS PDF 다운로드 API
    
    엔드포인트: /downloadService/download
    파라미터:
    - applicationNumber: 출원번호
    - publicationNumber: 공개번호 (폴백)
    
    저장 경로: uploads/patents/{application_number}.pdf
    """
```

**S3 업로드**: `backend/app/services/patent/collection_service.py`

```python
async def download_and_upload_patent_pdf(
    self,
    patent_data: Dict[str, Any],
    file_bss_info_sno: int
) -> Optional[str]:
    """
    1. KIPRIS에서 PDF 다운로드
    2. S3 업로드 (documents/patents/{file_bss_info_sno}.pdf)
    3. 로컬 파일 삭제
    4. TbFileBssInfo.path 업데이트
    
    Returns:
        S3 키 (예: "documents/patents/12345.pdf")
    """
```

#### 8.4.3. 임베딩 생성

**벡터 생성**: `backend/app/services/patent/collection_service.py`

```python
async def _generate_patent_embeddings(
    self,
    patent_data: Dict[str, Any],
    file_bss_info_sno: int
) -> bool:
    """
    특허 텍스트 임베딩 생성
    
    처리 단계:
    1. 임베딩 텍스트 준비
       - 발명명칭 + 초록 + 청구항 (우선순위 순)
       - 최대 8000 토큰
    
    2. 임베딩 생성
       - Provider: Bedrock Titan v2 (1024d) 또는 Azure OpenAI (1536d)
       - 모델: settings.default_embedding_model
    
    3. DocChunkSession 생성
       - chunking_strategy: 'patent_auto'
    
    4. DocChunk 생성
       - content_text: 임베딩 텍스트
       - section_heading: 'patent_summary'
    
    5. DocEmbedding 저장
       - aws_vector_1024 또는 azure_vector_1536
       - provider, model_name, dimension 메타데이터
    
    6. TbDocumentSearchIndex 업데이트
       - Full-Text Search 인덱스 (Korean + English)
    """
```

**Provider 선택**:
```python
# .env 설정
DEFAULT_EMBEDDING_PROVIDER=aws  # 또는 azure

# Bedrock (기본)
- 모델: amazon.titan-embed-text-v2:0
- 차원: 1024
- 저장: DocEmbedding.aws_vector_1024

# Azure OpenAI (폴백)
- 모델: text-embedding-3-small
- 차원: 1536
- 저장: DocEmbedding.azure_vector_1536
```

### 8.5. 운영 및 모니터링

#### 8.5.1. 성능 최적화
- **배치 처리**: 100건씩 그룹화하여 처리
- **병렬 처리**: PDF 다운로드와 임베딩 생성 병렬 실행 (asyncio)
- **캐싱**: KIPRIS API 응답 캐시 (Redis, 1시간 TTL)

#### 8.5.2. 에러 처리
- **KIPRIS API 장애**: 자동 재시도 (3회, 지수 백오프)
- **중복 특허**: application_number 기준 스킵
- **PDF 다운로드 실패**: 로그만 남기고 계속 진행
- **임베딩 실패**: Bedrock → Azure 폴백

#### 8.5.3. 로깅
```python
logger.info(f"[PATENT-COLLECTION] 수집 시작: {search_config}")
logger.info(f"[PATENT-COLLECTION] KIPRIS 검색 완료: {len(patents)}건")
logger.info(f"[PATENT-COLLECTION] 저장 완료: {saved_count}건")
logger.warning(f"[PATENT-COLLECTION] 중복 스킵: {skipped_count}건")
logger.error(f"[PATENT-COLLECTION] 실패: {failed_count}건")
```

---

## 9. 특허 문서 구조화 처리 파이프라인 (Patent Document Structuring Pipeline)

> **최초 구현**: 2025-12-24  
> **상태**: ✅ 프로덕션 배포 완료  
> **목적**: 특허 문서의 정형화된 구조(청구항, 명세서, 도면)를 보존하여 검색 품질 향상

### 9.1. 개요

특허 문서는 특허청 표준 양식에 따라 **청구항, 기술분야, 발명의 배경, 상세한 설명** 등 11개 섹션으로 구조화되어 있습니다. 일반 문서 파이프라인(GeneralPipeline)은 이러한 구조를 무시하고 청킹하므로, 특허 특화 파이프라인(PatentPipeline)을 구현하여 섹션별 청킹 및 메타데이터 저장을 수행합니다.

#### 9.1.1. 문제점 (기존 GeneralPipeline)
- **섹션 혼재**: 청구항과 명세서가 하나의 청크에 혼합
- **청구항 손실**: 가장 중요한 법적 요소가 다른 내용과 구분 불가
- **검색 정확도 저하**: "청구항만 검색" 같은 섹션 기반 쿼리 불가
- **구조 정보 소실**: 특허의 계층적 구조가 평면화됨

#### 9.1.2. 해결 방안 (PatentPipeline)
- **섹션 자동 감지**: 정규식 패턴으로 11개 표준 섹션 인식
- **청구항 개별 파싱**: 독립항/종속항 자동 분리
- **섹션별 청킹**: 각 섹션을 독립 청크로 처리
- **메타데이터 저장**: DocChunk.section_heading에 섹션 타입 저장

### 9.2. PatentPipeline 아키텍처

#### 9.2.1. 파이프라인 구조

**파일**: `backend/app/services/document/pipelines/patent_pipeline.py`

```python
class PatentPipeline(GeneralPipeline):
    """
    특허 문서 처리 파이프라인
    
    GeneralPipeline 상속:
    - 기본 멀티모달 처리 (Upstage Document Parse)
    - 텍스트/이미지/표 추출
    - 청킹, 임베딩, 인덱싱
    
    특허 특화 추가:
    - 특허 섹션 감지 (PatentSectionDetector)
    - 청구항 개별 항 파싱
    - 섹션 정보 Blob Storage 저장
    """
    
    async def process(self) -> Dict[str, Any]:
        # 1. 기본 파이프라인 실행 (추출, 청킹, 임베딩, 인덱싱)
        result = await super().process()
        
        # 2. 특허 섹션 감지 (후처리)
        await self._detect_and_save_patent_sections()
        
        return result
```

**처리 흐름**:
```
PDF 업로드
    ↓
PatentPipeline.process()
    ├─ Step 1: Upstage Document Parse
    │   → extraction_full_text.txt 저장
    │
    ├─ Step 2: Multimodal 객체 추출
    │   → 이미지, 표, 수식
    │
    ├─ Step 3: 청킹
    │   → doc_chunk 테이블 (section_heading 포함)
    │
    ├─ Step 4: 임베딩
    │   → doc_embedding 테이블
    │
    ├─ Step 5: 인덱싱
    │   → pgvector + Full-Text Search
    │
    └─ Step 6: 특허 섹션 감지 (후처리)
        → patent_sections.json 저장
```

#### 9.2.2. PatentSectionDetector

**파일**: `backend/app/services/document/extraction/patent_section_detector.py`

**감지 가능한 섹션 (11개)**:

| 섹션 타입 | 한글 이름 | 우선순위 | 패턴 예시 |
|-----------|-----------|----------|-----------|
| `claims` | 청구항 | 0 (최고) | `[청구항]`, `[특허청구범위]` |
| `title` | 발명의 명칭 | 1 | `[발명의 명칭]` |
| `technical_field` | 기술분야 | 2 | `[기술 분야]` |
| `background` | 발명의 배경 | 3 | `[발명의 배경]` |
| `prior_art` | 선행기술문헌 | 4 | `[선행기술 문헌]` |
| `problem` | 해결하고자 하는 과제 | 5 | `[해결하고자 하는 과제]` |
| `solution` | 과제의 해결 수단 | 6 | `[과제의 해결 수단]` |
| `effects` | 발명의 효과 | 7 | `[발명의 효과]` |
| `brief_description_drawings` | 도면의 간단한 설명 | 8 | `[도면의 간단한 설명]` |
| `detailed_description` | 발명을 실시하기 위한 구체적인 내용 | 9 | `[발명을 실시하기 위한 구체적인 내용]` |
| `drawings` | 도면 | 10 | `[도 면]` |

**정규식 패턴**:
```python
SECTION_PATTERNS = [
    {
        "type": "claims",
        "patterns": [
            r"^\s*\[?\s*청\s*구\s*항?\s*\]?",
            r"^\s*\[?\s*특허\s*청구\s*범위\s*\]?",
            r"^\s*Claims?\s*:",
        ],
        "priority": 0
    },
    # ... (11개 섹션)
]
```

**청구항 파싱**:
```python
def _parse_claims(self, claims_text: str) -> List[PatentSection]:
    """
    청구항 텍스트에서 개별 항 파싱
    
    패턴:
    - "청구항 1"
    - "제1항"
    - "[청구항 1]"
    - "Claim 1."
    
    결과:
    [
        PatentSection(type="claim_1", title="청구항 1", content="..."),
        PatentSection(type="claim_2", title="청구항 2", content="..."),
        ...
    ]
    """
```

#### 9.2.3. 데이터 저장

**Blob Storage**: `intermediate/multimodal/{document_id}/patent_sections.json`

```json
{
  "sections": [
    {
      "section_type": "claims",
      "title": "청구항",
      "start_pos": 1234,
      "end_pos": 5678,
      "content": "청구항 1. ...",
      "content_length": 4444,
      "priority": 0,
      "subsections": [
        {
          "section_type": "claim_1",
          "title": "청구항 1",
          "content": "청구항 1. 서버와 통신하는...",
          "priority": 0
        },
        {
          "section_type": "claim_2",
          "title": "청구항 2",
          "content": "청구항 2. 제1항에 있어서...",
          "priority": 0
        }
      ]
    },
    {
      "section_type": "detailed_description",
      "title": "발명을 실시하기 위한 구체적인 내용",
      "start_pos": 5678,
      "end_pos": 12345,
      "content": "이하 실시예를 통해...",
      "priority": 9
    }
  ],
  "summary": {
    "total_sections": 8,
    "sections_found": ["청구항", "기술분야", "발명의 배경", ...],
    "claims_count": 15,
    "has_detailed_description": true,
    "has_drawings": true
  },
  "detected_at": "2025-12-24T14:30:00"
}
```

**PostgreSQL**: `doc_chunk` 테이블

```sql
SELECT 
    chunk_id,
    section_heading,  -- "청구항", "발명의 배경" 등
    LEFT(content_text, 100) AS preview,
    token_count,
    page_range
FROM doc_chunk
WHERE file_bss_info_sno = 12345  -- 특허 문서 ID
ORDER BY chunk_index;
```

### 9.3. 섹션별 청킹 전략

#### 9.3.1. 청구항 (Claims)

**전략**: 각 항을 독립 청크로

**이유**:
- 청구항은 법적으로 가장 중요한 요소
- 각 항은 독립적인 권리 범위를 정의
- 종속항은 상위 항을 참조 (제1항에 있어서)

**예시**:
```
입력:
[청구항]
청구항 1. 서버와 통신하는 단말장치...
청구항 2. 제1항에 있어서, 상기 프로세서는...
청구항 3. 제1항 또는 제2항에 있어서...

결과:
Chunk 1:
  content_text: "청구항 1. 서버와 통신하는..."
  section_heading: "청구항"
  
Chunk 2:
  content_text: "청구항 2. 제1항에 있어서..."
  section_heading: "청구항"
  
Chunk 3:
  content_text: "청구항 3. 제1항 또는 제2항에..."
  section_heading: "청구항"
```

#### 9.3.2. 상세한 설명 (Detailed Description)

**전략**: 단락 단위 청킹 (토큰 제한 고려)

**이유**:
- 일반적으로 가장 긴 섹션
- 실시예, 도면 설명 등이 포함
- 의미적 단락 경계 존중

**예시**:
```
Chunk N:
  content_text: "[발명을 실시하기 위한 구체적인 내용]\n이하 첨부 도면..."
  section_heading: "발명을 실시하기 위한 구체적인 내용"
  token_count: 380
```

#### 9.3.3. 기타 섹션

**전략**: 전체를 하나의 청크로

**이유**:
- 기술분야, 발명의 배경, 효과 등은 비교적 짧음
- 섹션 전체가 하나의 의미 단위

### 9.4. 검색 최적화

#### 9.4.1. 섹션 필터링

**예시: 청구항만 검색**
```python
# Vector Search with Filter
results = await search_service.search(
    query="무선 통신 방법",
    filters={
        "section_heading": "청구항"
    }
)
```

**예시: 상세 설명 검색**
```python
results = await search_service.search(
    query="실시예 구현 방법",
    filters={
        "section_heading": "발명을 실시하기 위한 구체적인 내용"
    }
)
```

#### 9.4.2. 섹션 가중치

**검색 시 우선순위 적용**:
```python
section_weights = {
    "청구항": 2.0,                      # 최고 우선순위
    "발명의 배경": 1.5,
    "기술분야": 1.3,
    "발명의 효과": 1.2,
    "default": 1.0
}

# 가중치 적용 검색
for result in results:
    section = result.section_heading
    weight = section_weights.get(section, 1.0)
    result.score *= weight
```

#### 9.4.3. 하이브리드 검색

**벡터 검색 + FTS 결합**:
```python
# 1. 벡터 검색 (의미 유사도)
vector_results = await vector_search(query, top_k=50)

# 2. Full-Text Search (키워드 매칭)
fts_results = await full_text_search(query, top_k=50)

# 3. 결과 병합 (Reciprocal Rank Fusion)
final_results = merge_results(vector_results, fts_results)

# 4. 섹션 필터 적용
filtered_results = [
    r for r in final_results 
    if r.section_heading in ["청구항", "기술분야"]
]
```

### 9.5. PipelineRouter 통합

**파일**: `backend/app/services/document/pipeline_router.py`

```python
from app.services.document.pipelines.patent_pipeline import PatentPipeline

PIPELINE_MAP: Dict[str, Type[DocumentPipeline]] = {
    DocumentType.GENERAL: GeneralPipeline,
    DocumentType.ACADEMIC_PAPER: AcademicPaperPipeline,
    DocumentType.PATENT: PatentPipeline,  # ✅ 2025-12-24
    DocumentType.UNSTRUCTURED_TEXT: GeneralPipeline,
}
```

**자동 라우팅**:
```python
# 사용자가 "📜 특허 문서" 선택하여 업로드
# → document_type="patent"
# → PipelineRouter.get_pipeline(document_type="patent")
# → PatentPipeline 인스턴스 반환
# → 특허 특화 처리 수행
```

### 9.6. 운영 및 모니터링

#### 9.6.1. 로깅

```python
logger.info("[PATENT-SECTION] 섹션 감지 시작 (텍스트 길이: 45,678자)")
logger.info("[PATENT-SECTION] 섹션 발견: claims (라인 12, 우선순위 0)")
logger.info("[PATENT-SECTION] 청구항 파싱: 12개 항 발견")
logger.info("[PATENT-SECTION] ✅ 8개 섹션 감지 완료")
logger.info("[PATENT-SECTION] 섹션 정보 저장(s3): multimodal/678/patent_sections.json")
```

#### 9.6.2. 품질 검증

**섹션 감지 정확도**:
- 재현율: 95%+ (한국 특허청 표준 양식)
- 정밀도: 98%+ (고정 패턴 기반)

**청킹 품질 비교**:
```
[GeneralPipeline]
- 청구항 + 명세서 혼재
- 섹션 경계 무시
- 평균 청크 크기: 400 토큰

[PatentPipeline]
- 청구항 각 항 독립 청크
- 섹션별 청킹
- 평균 청크 크기: 280 토큰 (청구항), 400 토큰 (명세서)
```

#### 9.6.3. 에러 처리

**섹션 감지 실패 시**:
```python
try:
    sections = detector.detect_sections(full_text)
except Exception as e:
    logger.warning(f"[PATENT-SECTION] 섹션 감지 실패: {e}")
    # GeneralPipeline 청킹 방식으로 폴백
    sections = []
```

**청구항 파싱 실패 시**:
```python
if not claims:
    logger.warning("[PATENT-CLAIMS] 청구항 번호를 찾을 수 없음")
    # 청구항 전체를 하나의 섹션으로 처리
    return [PatentSection(
        section_type="claims",
        title="청구항",
        content=claims_text
    )]
```

### 9.7. 향후 개선 방향

#### 9.7.1. 특허 서지정보 DB 저장

**목표**: PDF 본문에서 서지정보 추출

```python
async def _save_patent_bibliographic_info(self, full_text: str):
    # 출원번호 추출: "출원번호: 10-2023-0012345"
    application_number = self._extract_application_number(full_text)
    
    # 발명자/출원인 추출 (첫 페이지 파싱)
    inventors = self._extract_inventors(full_text)
    
    # IPC 분류 추출: "G06F 3/048"
    ipc_codes = self._extract_ipc_codes(full_text)
    
    # TbPatentBibliographicInfo 업데이트
    await self._upsert_patent_info(application_number, inventors, ipc_codes)
```

#### 9.7.2. 청구항 독립항/종속항 관계 그래프

**목표**: 청구항 간 인용 관계 시각화

```
청구항 1 (독립항)
  ├─ 청구항 2 (종속항: 제1항에 있어서)
  ├─ 청구항 3 (종속항: 제1항에 있어서)
  └─ 청구항 4 (종속항: 제1항 또는 제2항에 있어서)
```

#### 9.7.3. 도면 OCR 및 참조 연결

**목표**: 명세서의 "도 1을 참조하면" → 실제 도면 이미지 연결

```python
# 도면 참조 감지
references = re.findall(r'도\s*(\d+)', text)

# 도면 이미지와 매칭
for ref in references:
    figure_id = f"figure_{ref}"
    # Blob Storage에서 도면 이미지 로드
```

---

## 10. 참고 자료

### 10.1. 관련 문서
- `01.docs/02.document_ingestion_vectorstore.md` - 멀티모달 문서 처리
- `01.docs/archive/PATENT_COLLECTION_COMPLETE_2025-12-22.md` - 수집 시스템 완성 보고서
- `01.docs/archive/PATENT_STRUCTURED_PIPELINE_2025-12-22.md` - 구조화 파이프라인 구현

### 10.2. 코드 위치
```
backend/
├─ app/
│  ├─ api/v1/patent_collection.py          # API 엔드포인트
│  ├─ services/
│  │  └─ patent/
│  │     ├─ kipris_client.py               # KIPRIS API 클라이언트
│  │     └─ collection_service.py          # 수집 서비스
│  ├─ services/document/
│  │  ├─ pipelines/
│  │  │  └─ patent_pipeline.py            # 특허 파이프라인
│  │  └─ extraction/
│  │     └─ patent_section_detector.py    # 섹션 감지
│  ├─ models/
│  │  └─ patent/
│  │     ├─ tb_patent_bibliographic_info.py
│  │     ├─ tb_patent_collection_settings.py
│  │     └─ tb_patent_collection_logs.py
│  └─ tasks/
│     └─ patent_collection_tasks.py        # Celery 작업

frontend/
└─ src/pages/user/my-knowledge/components/
   ├─ PatentCollectionSettings.tsx         # 수집 설정 UI
   └─ PatentCollectionMonitor.tsx          # 모니터링 UI
```

---

## 11. 특허 수집 → 지식 컨테이너(MyKnowledge) 표출 흐름 (통합)

### 11.1 목표

- KIPRIS 등에서 **특허를 수집하면**
    - (A) 특허 서지정보 DB에 저장되고
    - (B) 동시에 해당 지식 컨테이너의 **문서 목록(MyKnowledge)** 에도 **일반 업로드 문서처럼** 표시되어
    - (C) 기존 **뷰어/다운로드 UX** 를 그대로 재사용한다.

### 11.2 정책(요구사항)

- (수집 경로 기본 정책) 특허는 공개 데이터이므로 시스템은 **원본 파일(PDF 등)을 저장하지 않고 URL만 저장**하는 것을 기본으로 한다.
    - 단, 운영/기능 옵션으로 PDF 다운로드/업로드를 수행할 수는 있다(설정/구현에 따라 다름).
- 하이브리드 검색을 위해 특허의 **서지정보(제목/초록 등)는 항상 색인 및 임베딩**한다.

### 11.3 중요: “수집”과 “업로드” 경로는 다르다

이 섹션은 기본적으로 **특허 수집(서지 중심) → MyKnowledge 표출** 흐름을 설명한다.

다만 사용자가 지식등록에서 PDF를 올리는 **특허 업로드(전문 기반)** 경로는 별도 파이프라인으로 동작한다:
- Router: `document_type=patent` → `PatentPipeline`
- 산출물(예: S3 사용 시)
    - `intermediate/multimodal/{document_id}/extraction_full_text.txt`
    - `intermediate/multimodal/{document_id}/patent_sections.json`
    - `derived/multimodal/{document_id}/chunks/...`

즉,
- 수집 경로: `title + abstract` 중심 1청크 임베딩/인덱싱(검색 최적화)
- 업로드 경로: 전문 추출 + 섹션 감지/구조화(특허 문서 이해/보고서 자동화 기반)

### 11.4 핵심 데이터 모델

#### 11.4.1 특허 서지정보 저장

- 테이블: `TbPatentBibliographicInfo` ([backend/app/models/patent/patent_models.py](../backend/app/models/patent/patent_models.py))
- 주요 필드
    - `application_number`, `publication_number`, `title`, `abstract`, 날짜 필드
    - `knowledge_container_id`: 수집 대상 지식 컨테이너
    - `data_source`: 기본 `KIPRIS`
    - `source_url`: 특허 원본 접근 URL(외부)

#### 11.4.2 지식 컨테이너 문서 목록 표출(통합 문서 테이블)

- 테이블: `TbFileBssInfo` ([backend/app/models/document/file_models.py](../backend/app/models/document/file_models.py))
- 특허 수집 시 생성되는 레코드 규칙
    - `document_type = "patent"`
    - `knowledge_container_id = (수집 대상 컨테이너)`
    - `path = (외부 URL)`
    - `file_extsn = "url"` (UI에서 확장자 표시 목적)
    - `processing_status = "completed"` (서지 임베딩/인덱싱이 끝난 상태를 의미)
    - `korean_metadata`에 최소 식별자 저장
        - `applicationNumber`, `publicationNumber`, `data_source`, `source_url`

#### 11.4.3 검색/임베딩

- 테이블
    - `TbDocumentSearchIndex` (서지 텍스트 인덱싱)
    - `DocChunkSession`, `DocChunk`, `DocEmbedding` (임베딩 파이프라인 호환)
- 전략
    - 특허는 파일 파싱 대신 `title + abstract`를 1개 청크로 생성
    - 임베딩 provider는 환경 설정(`default_embedding_provider`)에 따라 저장

### 11.5 처리 흐름(End-to-End)

#### 11.5.1 수집 설정 저장/실행

- API: `POST /api/v1/patent-collection/settings`
- 실행: `POST /api/v1/patent-collection/start` → Celery task 실행
- Task: `collect_patents_from_kipris` ([backend/app/tasks/patent_collection_tasks.py](../backend/app/tasks/patent_collection_tasks.py))

#### 11.5.2 KIPRIS 검색

- `KIPRISClient.search_patents()`가 최소 서지 필드 반환
    - `applicationNumber`, `publicationNumber`, `inventionTitle`, `abstract`, 날짜 등

#### 11.5.3 DB 저장(서지 + 문서 목록 엔트리)

- `PatentCollectionService.save_patent_to_database()` ([backend/app/services/patent/collection_service.py](../backend/app/services/patent/collection_service.py))
    1. `TbPatentBibliographicInfo` 레코드 생성 (중복 방지)
    2. `TbFileBssInfo` 레코드 생성 (MyKnowledge 목록에 보일 ‘문서’ 엔트리)
    3. `_generate_patent_embeddings()` 호출
         - `TbDocumentSearchIndex` + `DocChunk*` + `DocEmbedding` 저장

#### 11.5.4 MyKnowledge(지식 컨테이너) 문서 목록 노출

- 프론트: MyKnowledge는 `/api/v1/documents?container_id=...`로 목록을 가져옴
- 백엔드: `GET /api/v1/documents` ([backend/app/api/v1/documents.py](../backend/app/api/v1/documents.py))
    - 일반 문서는 provider 기반 필터(추출/세션 성공 등) 조건이 적용됨
    - **특허 문서(`document_type="patent"`)는 예외로 항상 표시**하여 컨테이너 문서 목록에 누락되지 않게 함

#### 11.5.5 뷰어/다운로드 재사용

- 뷰어
    - 프론트 FileViewer는 `GET /api/files/iframe-view/{file_id}?token=...` 사용
    - 백엔드가 `TbFileBssInfo.path`를 확인
        - 로컬/S3/Azure: 기존 방식
        - **http(s) URL:** iframe에서 열 수 있도록 리다이렉트 또는 링크 HTML 반환

- 다운로드
    - 프론트는 `GET /api/v1/documents/{id}/download`를 `blob`으로 다운로드
    - 외부 도메인으로의 리다이렉트는 CORS로 실패할 수 있으므로
        **특허는 URL을 담은 `.url`(바로가기) 파일을 다운로드로 제공**

### 11.6 중복/재수집 처리

- 기준: `TbPatentBibliographicInfo.application_number`로 중복 방지
- 재수집 시에도 `TbFileBssInfo(document_type=patent, applicationNumber=...)`가 없으면 생성하여
    “서지는 있는데 목록이 비는” 상태를 방지

### 11.7 권한/접근 제어

- 문서 목록/다운로드 권한은 기존 컨테이너 권한 로직을 그대로 사용
    - `knowledge_container_id` 기준

### 11.8 특허 수집 설정 및 작업 상태 관리 (2025-12-24 개선)

#### 11.8.1 배경
특허 수집 작업 실행 시 명확한 상태 구분과 결과 피드백이 필요했다. 특히 "0건 수집" 같은 경우 실패인지 검색 조건이 좁아서 그런 것인지 구분이 어려웠다.

#### 11.8.2 개선 사항

##### 1) 작업 상태 추적 강화
- **상태 정의** (`TbPatentCollectionTasks.status`)
    - `pending`: 작업이 대기열에 등록됨
    - `running`: KIPRIS 검색 및 데이터 수집 진행 중
    - `completed`: 수집 완료 (0건이어도 completed)
    - `failed`: 오류로 인한 실패

##### 2) 상태별 메시지 시스템
프론트엔드 `PatentCollectionSettings.tsx`의 `pollTask()` 함수가 상태에 따라 적절한 메시지를 생성:

```typescript
// 상태별 메시지 예시
if (status === 'completed') {
    if (collected === 0) {
        message = '⚠️ 검색 조건에 맞는 특허가 없습니다. 검색 조건을 조정해보세요.';
    } else {
        message = `✅ 수집 완료: ${collected}건 성공${errors > 0 ? `, ${errors}건 실패` : ''}`;
    }
} else if (status === 'failed') {
    message = '❌ 수집 작업이 실패했습니다.';
} else if (status === 'running') {
    message = `🔄 수집 중... (${current}/${total})`;
}
```

##### 3) 마지막 수집 결과 저장
설정별로 마지막 실행 정보를 추적하기 위해 DB 스키마 개선:

**모델 변경** (`TbPatentCollectionSettings`)
```python
last_collection_date = Column(DateTime(timezone=True), nullable=True)
last_collection_result = Column(JSONB, nullable=True)  # 새로 추가
# 저장 형식: {"collected": 25, "errors": 2}
```

**자동 업데이트 로직** (`PatentCollectionService.update_task_progress()`)
- 작업이 `completed` 상태가 되면:
    - `last_collection_date`: 현재 시각 저장
    - `last_collection_result`: `{collected, errors}` JSON 저장

##### 4) UI/UX 개선

**설정 카드 표시**
- 작업이 실행 중이지 않을 때: 마지막 수집 일시 및 결과 표시
    - 예: "마지막 수집: 2025-12-24 10:30 / 결과: 25건 수집, 2건 실패"
- 색상 구분:
    - 🟢 녹색: 수집 성공 (건수 > 0)
    - 🟡 노란색: 수집 완료이지만 0건 (조건 재검토 필요)
    - 🔴 빨간색: 실패

**진행 상태 UI**
- 실시간 프로그레스 바 (running 상태일 때만)
- 완료 후 결과 요약 박스 (5초간 표시 후 자동 제거)

##### 5) 구현 파일

**Backend**
- 모델: `backend/app/models/patent/collection_models.py`
    - `TbPatentCollectionSettings.last_collection_result` 추가
- API: `backend/app/api/v1/patent_collection.py`
    - Response 모델에 `last_collection_result` 포함
- 서비스: `backend/app/services/patent/collection_service.py`
    - `update_task_progress()`: completed 시 설정 업데이트 로직
- 마이그레이션: `backend/alembic/versions/20251224_add_last_collection_result.py`

**Frontend**
- 컴포넌트: `frontend/src/pages/user/PatentCollectionSettings.tsx`
    - `pollTask()`: 상태별 메시지 생성 및 자동 정리
    - UI: 마지막 수집 정보 카드, 진행 상태 표시 개선

#### 11.8.3 사용자 경험 흐름

1. **수집 시작**: "🚀 수집 작업을 시작합니다..."
2. **진행 중**: "🔄 수집 중... (15/100)" + 프로그레스 바
3. **완료 - 성공**: "✅ 수집 완료: 25건 성공" (녹색)
4. **완료 - 0건**: "⚠️ 검색 조건에 맞는 특허가 없습니다..." (노란색)
5. **실패**: "❌ 수집 작업이 실패했습니다" (빨간색)
6. **5초 후**: 상태 메시지 자동 제거, 마지막 수집 정보로 대체

---

## 12. 특허 문서 섹션 감지 품질 지표 (Patent Section Detection Quality Metrics)

**작성일:** 2025-12-24  
**목적:** 특허 문서 파이프라인의 섹션 감지 정확도 향상 및 국가별 차이 대응

### 12.1 문제점 분석

#### 12.1.1 현재 상황 (2025-12-24 테스트 결과)

**테스트 문서:** `1020230076057A.pdf` (한국 특허 공개공보)

**예상 섹션 (10개):**
- 발명의 명칭
- 기술분야
- 발명의 배경 (배경기술)
- 선행기술문헌
- 해결하려는 과제
- 과제의 해결 수단
- 발명의 효과
- 도면의 간단한 설명
- 발명을 실시하기 위한 구체적인 내용
- 청구범위 (청구항)

**실제 감지 결과:**
```
- 청구항: 2회 (중복)
- 과제의 해결 수단: 1회 ✅
- 발명의 효과: 1회 ✅
- 도면의 간단한 설명: 1회 ✅
- 발명을 실시하기 위한 구체적인 내용: 1회 ✅
- 도면: 6회 (반복)
```

**섹션 커버리지:** 4/10 = **40%** ❌

#### 12.1.2 추가 실측 (2025-12-28 업로드 파이프라인 기준)

**테스트 문서:** `backend/tests/1020240027504.pdf` (한국 특허 공개공보)

**실행 경로(지식등록/업로드와 동일):** `PatentPipeline` → `PatentSectionDetector`
- 산출물: `intermediate/multimodal/{document_id}/extraction_full_text.txt`, `intermediate/multimodal/{document_id}/patent_sections.json`
- 실측 document_id: 80

**예상 섹션 (10개):**
- 발명의 명칭
- 기술분야
- 발명의 배경 (배경기술)
- 선행기술문헌
- 해결하려는 과제
- 과제의 해결 수단
- 발명의 효과
- 도면의 간단한 설명
- 발명을 실시하기 위한 구체적인 내용
- 청구범위 (청구항)

**실제 감지 결과(요약):**
- 섹션 커버리지: 8/10 = **80.0%**
    - 감지됨: 발명의 명칭, 기술분야, 발명의 배경, 해결하려는 과제, 과제의 해결 수단, 도면의 간단한 설명, 발명을 실시하기 위한 구체적인 내용, 청구범위(청구항)
    - 누락됨: 선행기술문헌, 발명의 효과
- 중복 감지:
    - 도면(drawings): 5회
    - 청구항(claims): 3회
- 청구항 파싱:
    - `claims` 섹션이 3개로 감지됨
    - 개별 항 분리(subsections)는 **청구항 1**만 성공(= 1항)
    - `명세서`, `청구범위` 텍스트가 `claims`로 잘못 매핑되는 케이스 존재

#### 12.1.3 근본 원인

##### (1) 띄어쓰기 변형 미처리
```python
# 패턴: r"^\s*\[?\s*기술\s*분야\s*\]?"
# 실제 PDF: "기 술 분 야" (각 글자 사이 공백)
# 실제 PDF: "배 경 기 술" (각 글자 사이 공백)
```

한국 특허청 공보는 **각 글자 사이에 공백**을 넣는 경우가 많음:
- 정상: `기술분야`, `배경기술`
- 실제: `기 술 분 야`, `배 경 기 술`

##### (2) 청구항 중복 감지
- "청구범위" 섹션 헤더와 "청구항 1" 시작 부분을 각각 별도 섹션으로 인식
- 청구항 개별 파싱 로직이 실패 (`[PATENT-CLAIMS] 청구항 번호를 찾을 수 없음`)

##### (3) 도면 반복 감지
- 여러 페이지에 걸친 도면을 각각 별도 섹션으로 인식

### 12.2 품질 지표 정의

#### 12.2.1 핵심 지표 (KPI)

| 지표명 | 정의 | 목표 | 현재(2025-12-24) | 추가 실측(2025-12-28) |
|--------|------|------|------------------|----------------------|
| **섹션 커버리지 (Section Coverage)** | 감지된 표준 섹션 수 / 전체 표준 섹션 수 | ≥ 90% | 40% | 80% |
| **청구항 파싱 성공률 (Claim Parsing Rate)** | 개별 항 분리 성공 / 전체 청구항 수 | ≥ 95% | 0% | (부분 성공) 1항만 분리 |
| **섹션 중복도 (Section Duplication)** | 중복 감지된 섹션 수 | = 0 | 8회 | drawings 5회 + claims 3회 |
| **섹션 누락률 (Section Missing Rate)** | 누락된 필수 섹션 수 / 필수 섹션 수 | ≤ 10% | 60% | 20% |

#### 12.2.2 보조 지표

- **섹션 순서 정확도:** 한국 특허청 표준 순서와의 일치도
- **섹션 경계 정확도:** 시작/종료 위치의 정확성
- **다국어 지원률:** 미국/유럽/일본/중국 특허 감지 성공률

### 12.3 국가별 특허 문서 구조 차이

#### 12.3.1 한국 (KIPO - 한국특허청)

**표준 순서:**
```
1. 발명의 명칭
2. 기술분야
3. 발명의 배경 (또는 "배경기술")
4. 선행기술문헌
5. 해결하려는 과제 (또는 "발명이 해결하고자 하는 과제")
6. 과제의 해결 수단
7. 발명의 효과
8. 도면의 간단한 설명
9. 발명을 실시하기 위한 구체적인 내용 (또는 "발명의 상세한 설명")
10. 청구범위 (청구항)
```

**특이사항:**
- ✅ 각 섹션 헤더가 대괄호 `[  ]` 또는 없이 표기
- ✅ **띄어쓰기 변형 다양:** `기술분야` vs `기 술 분 야`
- ✅ 번호 표기: `[0001]`, `[0003]` 등 문단 번호 사용
- ✅ 청구항 번호: `청구항 1`, `청구항 2` (독립항/종속항 구분)

#### 12.3.2 미국 (USPTO - 미국특허청)

**표준 순서:**
```
1. Title of Invention
2. Abstract
3. Background of the Invention
     - Field of the Invention
     - Description of Related Art
4. Summary of the Invention
5. Brief Description of the Drawings
6. Detailed Description of the Preferred Embodiments
7. Claims
```

**특이사항:**
- 영문 표기, 섹션명 표준화
- Claims는 보통 문서 맨 끝
- 번호 체계: `[0001]`, `[0002]` 사용
- Dependent Claims는 "wherein", "according to claim" 키워드 사용

#### 12.3.3 유럽 (EPO - 유럽특허청)

**표준 순서:**
```
1. Title
2. Technical Field
3. Background Art (Prior Art)
4. Disclosure of Invention
     - Technical Problem
     - Technical Solution
     - Advantageous Effects
5. Brief Description of Drawings
6. Description of Embodiments
7. Claims
```

#### 12.3.4 일본 (JPO - 일본특허청)

**표준 순서:**
```
1. 発明の名称 (발명의 명칭)
2. 技術分野 (기술분야)
3. 背景技術 (배경기술)
4. 発明が解決しようとする課題 (해결하려는 과제)
5. 課題を解決するための手段 (과제 해결 수단)
6. 発明の効果 (발명의 효과)
7. 図面の簡単な説明 (도면 설명)
8. 発明を実施するための形態 (실시 형태)
9. 実施例 (실시예)
10. 産業上の利用可能性 (산업상 이용 가능성)
11. 符号の説明 (부호 설명)
12. 特許請求の範囲 (청구범위)
```

#### 12.3.5 중국 (CNIPA - 중국국가지식산권국)

**표준 순서:**
```
1. 发明名称 (발명명칭)
2. 技术领域 (기술영역)
3. 背景技术 (배경기술)
4. 发明内容 (발명내용)
     - 要解决的技术问题 (해결할 기술 문제)
     - 技术方案 (기술 방안)
     - 有益效果 (유익한 효과)
5. 附图说明 (첨부도 설명)
6. 具体实施方式 (구체적 실시방식)
7. 权利要求书 (권리요구서 = 청구범위)
```

### 12.4 개선 방안

#### 12.4.1 단기 개선 (High Priority)

##### (1) 띄어쓰기 유연 패턴 추가
```python
# 현재
r"^\s*\[?\s*기술\s*분야\s*\]?"

# 개선
r"^\s*\[?\s*기\s*술\s*분\s*야\s*\]?"  # 각 글자 사이 \s* 추가
```

##### (2) 청구항 중복 제거 로직
- "청구범위" 헤더 감지 시 이후 "청구항 1" 시작까지를 하나의 섹션으로 처리
- `_parse_claims()` 메서드 강화

##### (3) 도면 섹션 통합
- 여러 페이지에 걸친 도면을 하나의 섹션으로 통합
- "도 1", "도 2", "도 3" 등을 subsection으로 처리

#### 12.4.2 중기 개선 (Medium Priority)

##### (1) 품질 검증 파이프라인
```python
class PatentQualityValidator:
        """특허 문서 품질 검증"""
    
        def validate_section_coverage(
                self, 
                detected_sections: List[PatentSection],
                country: str = "KR"
        ) -> Dict:
                """
                섹션 커버리지 계산
        
                Returns:
                        {
                                "coverage_rate": 0.8,  # 80%
                                "missing_sections": ["기술분야", "배경기술"],
                                "duplicated_sections": ["청구항"],
                                "quality_score": 75.0  # 0-100
                        }
                """
```

##### (2) 섹션 경계 보정
- LLM 기반 섹션 경계 검증 (Claude/GPT)
- 잘못된 경계 자동 수정

##### (3) 국가별 패턴 분리
```python
class KoreanPatentDetector(PatentSectionDetector):
        """한국 특허 전용 감지기"""
        pass

class USPatentDetector(PatentSectionDetector):
        """미국 특허 전용 감지기"""
        pass
```

#### 12.4.3 장기 개선 (Low Priority)

##### (1) ML 기반 섹션 감지
- 특허 문서 대량 학습 데이터셋 구축
- BERT/RoBERTa 기반 섹션 분류 모델
- 정규식 패턴과 ML 앙상블

##### (2) 다국어 지원 확대
- 일본 특허: JPO 패턴 추가
- 중국 특허: CNIPA 패턴 추가
- 유럽 특허: EPO 다국어 패턴 추가

##### (3) 실시간 품질 모니터링
- 프로덕션 환경에서 섹션 감지 품질 지표 수집
- Grafana/Prometheus 대시보드
- 품질 저하 시 알림

### 12.5 테스트 체크리스트

#### 12.5.1 필수 테스트 케이스

**한국 특허:**
- [ ] 띄어쓰기 있는 섹션 헤더 (`기 술 분 야`)
- [ ] 띄어쓰기 없는 섹션 헤더 (`기술분야`)
- [ ] 청구항 10개 이상 (독립항 1개, 종속항 9개)
- [ ] 도면 5개 이상
- [ ] 선행기술문헌 없는 경우

**미국 특허:**
- [ ] Abstract 존재
- [ ] Claims 20개 이상
- [ ] Multiple Dependent Claims
- [ ] Drawings with Figure Legends

**품질 지표 검증:**
- [ ] 섹션 커버리지 ≥ 90%
- [ ] 청구항 파싱 성공률 ≥ 95%
- [ ] 섹션 중복도 = 0
- [ ] 섹션 누락률 ≤ 10%

#### 12.5.2 테스트 데이터셋

| 국가 | 문서 수 | 파일명 예시 | 특징 |
|------|---------|-------------|------|
| 한국 | 10 | `1020230076057A.pdf` | 공개공보, 띄어쓰기 변형 |
| 미국 | 10 | `US20230123456A1.pdf` | 영문, Claims 맨 뒤 |
| 유럽 | 5 | `EP1234567A1.pdf` | 다국어, 구조 엄격 |
| 일본 | 5 | `JP2023123456A.pdf` | 한자+가나, 번호 체계 다름 |
| 중국 | 5 | `CN112345678A.pdf` | 간체자, 순서 다름 |

---

## 13. 개정 이력

| 날짜 | 내용 |
| :--- | :--- |
| 2025-12-24 | 섹션 감지 품질 지표(초기) 정리 |
| 2025-12-28 | 문서 완전 통합(단일 정본) + 업로드 파이프라인 실측(1020240027504.pdf, document_id=80) 반영 |