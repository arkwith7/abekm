# PPT 생성 시스템 설계 및 구현 문서

## 1. 개요

본 문서는 PPT 생성 시스템의 현재 구현 상태를 정리한다.

### 1.1. 현재 구현 상태 (2025-12-03 Updated)

| 구분 | 현재 상태 |
|------|----------|
| **아키텍처 유형** | ✅ Agentic AI (ReAct/Plan-Execute)<br>Quick PPT + Template PPT 완전 통합 |
| **도구 선택** | ✅ LLM이 동적 결정<br>Quick/Template 모드 모두 지원 |
| **계획 수립** | ✅ ReAct Loop + Plan-Execute<br>패턴 선택 가능 (runtime 파라미터) |
| **중간 평가** | ✅ Observation → Action<br>도구 실행 결과 기반 다음 Action 결정 |
| **피드백 루프** | ✅ Smart Fallback + Quality Validation<br>품질 검증 도구 통합 |
| **레거시 정리** | ✅ archived 폴더 완전 삭제<br>미사용 import 제거 완료 |

**통합 달성**:
- ✅ 3개 Agent → 1개 Unified Agent (67% 코드 감소)
- ✅ Mode 파라미터: `quick` | `template`
- ✅ Pattern 파라미터: `react` | `plan_execute`
- ✅ 7개 도구를 동적으로 선택하여 사용
- ✅ **Quick PPT와 Template PPT 모두 unified_presentation_agent 사용 (100% 통합)**
- ✅ **레거시 Agent/Import 완전 삭제 (2025-12-03)**

**문서 최종 업데이트**: 2025-12-03 (레거시 코드 정리 완료)  
**구현 버전**: 4.2.0 (아키텍처 정리 + 레거시 삭제)

---

## 2. Unified Agentic AI 구현

### 2.1. 현재 구현: Unified Agentic AI PPT 생성

```
┌─────────────────────────────────────────────────────────────────────────┐
│         ✅ 현재 구현: Unified Agentic AI (ReAct/Plan-Execute)            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  사용자: "인슐린펌프 제품소개서 PPT 만들어줘"                           │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ LLM Agent (ReAct / Plan-and-Execute)                            │    │
│  │                                                                  │    │
│  │  💭 Thought: "제품소개서를 만들려면 먼저 제품 정보가 필요하다"   │    │
│  │  🔧 Action: RAG_search_tool("인슐린펌프 제품 정보")              │    │
│  │  📋 Observation: 제품 스펙, 기능, 규제 정보 등 검색됨            │    │
│  │                                                                  │    │
│  │  💭 Thought: "12개 섹션으로 구성하면 좋겠다"                     │    │
│  │  🔧 Action: outline_planning_tool(context, sections=12)         │    │
│  │  📋 Observation: 아웃라인 생성됨, 기술사양 섹션에 수치 많음      │    │
│  │                                                                  │    │
│  │  💭 Thought: "기술사양은 표로 표현하는게 좋겠다"                 │    │
│  │  🔧 Action: visualization_recommender_tool(slide_4)             │    │
│  │  📋 Observation: table 추천됨                                    │    │
│  │                                                                  │    │
│  │  💭 Thought: "기업 템플릿이 있는지 확인해보자"                   │    │
│  │  🔧 Action: template_search_tool(user_id)                       │    │
│  │  📋 Observation: "SK매직_공식템플릿.pptx" 발견                   │    │
│  │                                                                  │    │
│  │  💭 Thought: "이제 PPT를 생성하자"                               │    │
│  │  🔧 Action: pptx_builder_tool(outline, template, visualizations)│    │
│  │  📋 Observation: PPT 생성됨                                      │    │
│  │                                                                  │    │
│  │  💭 Thought: "품질을 검증해보자"                                 │    │
│  │  🔧 Action: quality_validator_tool(pptx_path)                   │    │
│  │  📋 Observation: 슬라이드 3에 텍스트 오버플로우 감지             │    │
│  │                                                                  │    │
│  │  💭 Thought: "슬라이드 3을 수정해야겠다"                         │    │
│  │  🔧 Action: slide_regenerator_tool(slide_3, fix_overflow=True)  │    │
│  │  📋 Observation: 수정 완료                                       │    │
│  │                                                                  │    │
│  │  ✅ Final Answer: "12슬라이드 제품소개서가 완성되었습니다"       │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  📎 quick_presentation_인슐린펌프_제품소개자료.pptx                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2. 핵심 특징

| 항목 | 구현 내용 |
|------|----------|
| **의사결정 주체** | LLM Agent |
| **도구 선택** | LLM이 상황에 맞게 선택 |
| **실행 흐름** | 동적 계획 및 실행 (ReAct/Plan-Execute) |
| **오류 처리** | 대안 전략 시도 (Smart Fallback) |
| **품질 보장** | 검증 → 수정 반복 (Quality Validator) |
| **컨텍스트 활용** | 이전 단계 결과 참조 (Observation 기반) |
| **통합 수준** | 1개 Unified Agent (67% 코드 감소) |

---

## 3. 시스템 아키텍처

### 3.1. 전체 구조도 (4-Layer Architecture)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Frontend UI Layer                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  [AI Agent 채팅]  ──→  "인슐린펌프 제품소개서 PPT 작성해 주세요"         │
│        ↓                                                                 │
│  [AI 답변] + [PPT 의도 감지] ──→  PresentationActionBar 표시            │
│        │                                                                 │
│        ├─────────────────────────┬───────────────────────────────┐       │
│        │                         │                               │       │
│        ▼                         ▼                               │       │
│  ┌──────────────────┐   ┌─────────────────────────────┐         │       │
│  │📊 PPT로 만들기   │   │📝 PPT 생성 설정             │         │       │
│  │                  │   │                             │         │       │
│  │ • 원클릭 생성    │   │ • 템플릿 선택               │         │       │
│  │ • Quick 모드     │   │ • AI 답변 탭                │         │       │
│  └────────┬─────────┘   │ • 매핑 편집 탭              │         │       │
│           │             └─────────────┬───────────────┘         │       │
│           │                           │                          │       │
│           ▼                           ▼                          │       │
│  ┌─────────────────┐     ┌───────────────────────────┐          │       │
│  │ usePresentation │     │ PresentationOutlineModal  │          │       │
│  │ .buildFromMsg() │     │ → buildWithOutline()      │          │       │
│  └────────┬────────┘     └─────────────┬─────────────┘          │       │
└───────────┼────────────────────────────┼────────────────────────┘       
            │                            │                                 
            ▼                            ▼                                 
┌─────────────────────────────────────────────────────────────────────────┐
│                    Layer 1: API Layer (presentation.py)                  │
│                         ⚡ 라우팅 + 입출력 처리만 담당                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ✅ 활성 엔드포인트:                                                     │
│  ├─ POST /agent/presentation/build-quick                                │
│  │    └─ Quick PPT 생성 (SSE 스트리밍)                                  │
│  ├─ POST /agent/presentation/build-with-template-react                  │
│  │    └─ Template PPT 생성 (ReAct 패턴)                                 │
│  ├─ POST /agent/presentation/build-with-template-plan-execute          │
│  │    └─ Template PPT 생성 (Plan-Execute 패턴)                          │
│  └─ POST /agent/presentation/build-unified                              │
│       └─ 🚀 통합 엔드포인트 (권장)                                      │
│                                                                          │
│  ⚠️ API Layer는 비즈니스 로직 없이 Agent Layer로 위임만 수행            │
│                                                                          │
│  💡 참고: agent.py vs presentation.py                                   │
│  - agent.py: 일반 채팅 및 의도 감지 (PaperSearchAgent)                  │
│  - presentation.py: PPT 생성 전용 엔드포인트 (UnifiedPresentationAgent) │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    Layer 2: Agent Layer (Orchestration)                  │
│                      🧠 LLM 기반 의사결정 + 도구 오케스트레이션           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ UnifiedPresentationAgent (v2.0.0)                                │   │
│  │                                                                  │   │
│  │ 📁 파일: unified_presentation_agent.py (~700 lines)             │   │
│  │                                                                  │   │
│  │ • Mode 분기: Quick | Template                                   │   │
│  │ • Pattern 분기: ReAct | Plan-Execute                            │   │
│  │ • 7개 LangChain Tools 등록 및 동적 선택                         │   │
│  │ • LLM 기반 의사결정 (Claude/GPT)                                │   │
│  │ • Auto-injection: deck_spec, mappings, template_structure       │   │
│  │ • Smart Fallback: JSON 파싱 실패 시 재생성                      │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ BaseAgent (공통 로직)                                            │   │
│  │                                                                  │   │
│  │ 📁 파일: base_agent.py (~200 lines)                             │   │
│  │                                                                  │   │
│  │ • _parse_agent_response(): LLM 응답 파싱 (다중 포맷 지원)       │   │
│  │ • _execute_tool(): 도구 실행 + 예외 처리                        │   │
│  │ • _log_step(): 실행 단계 로깅                                   │   │
│  │ • _init_execution(): 실행 초기화                                │   │
│  │ • _finalize_execution(): 메타데이터 추가                        │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    Layer 3: Tools Layer (LangChain Tools)                │
│                       🔧 단위 작업 수행 (Agent가 선택/실행)              │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  📁 위치: backend/app/tools/presentation/                               │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐     │
│  │ 7개 활성 도구 (Active Tools)                                   │     │
│  ├────────────────────────────────────────────────────────────────┤     │
│  │ 1. outline_generation_tool.py      # 아웃라인 생성 (LLM 호출)  │     │
│  │ 2. quick_pptx_builder_tool.py      # Quick PPT 빌드           │     │
│  │ 3. template_analyzer_tool.py       # 템플릿 분석              │     │
│  │ 4. content_mapping_tool.py         # 콘텐츠 ↔ 슬라이드 매핑   │     │
│  │ 5. templated_pptx_builder_tool.py  # Template PPT 빌드        │     │
│  │ 6. visualization_tool.py           # 시각화 유형 추천         │     │
│  │ 7. ppt_quality_validator_tool.py   # 품질 검증                │     │
│  └────────────────────────────────────────────────────────────────┘     │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────────┐     │
│  │ archived/ (12개 휴면 도구)                                     │     │
│  │ • assembly_tools.py, content_assembly_tool.py, ...             │     │
│  │ • 향후 확장 시 재활성화 가능                                   │     │
│  └────────────────────────────────────────────────────────────────┘     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    Layer 4: Services Layer (Business Logic)              │
│                       📦 실제 PPT 생성 + 파일 처리 로직                  │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  📁 위치: backend/app/services/presentation/                            │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 핵심 서비스 (Core Services)                                      │   │
│  ├──────────────────────────────────────────────────────────────────┤   │
│  │ • quick_ppt_generator_service.py      # Quick PPT 생성 핵심      │   │
│  │   - generate_fixed_outline()                                     │   │
│  │   - build_quick_pptx()                                           │   │
│  │   - _detect_visualization()                                      │   │
│  │                                                                  │   │
│  │ • templated_ppt_generator_service.py  # Template PPT 생성 핵심   │   │
│  │   - generate_enhanced_outline()                                  │   │
│  │   - build_templated_pptx()                                       │   │
│  │   - _build_with_mappings()                                       │   │
│  │                                                                  │   │
│  │ • ppt_template_manager.py             # 템플릿 관리              │   │
│  │   - get_template_details()                                       │   │
│  │   - list_templates()                                             │   │
│  │   - upload_template()                                            │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 지원 서비스 (Supporting Services)                                │   │
│  ├──────────────────────────────────────────────────────────────────┤   │
│  │ • ppt_models.py                  # 데이터 모델 (SlideSpec 등)    │   │
│  │ • enhanced_object_processor.py   # PPT 오브젝트 처리             │   │
│  │ • thumbnail_generator.py         # 썸네일 생성                   │   │
│  │ • template_content_cleaner.py    # 템플릿 콘텐츠 정리            │   │
│  │ • template_debugger.py           # 템플릿 디버깅                 │   │
│  │ • template_migration_service.py  # 템플릿 마이그레이션           │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ archived/ (4개 미사용 서비스)                                    │   │
│  │ • enhanced_ppt_generator_service.py, quick_html_presentation...  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.2. 계층 간 호출 규칙

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    AI Agent 아키텍처 설계 원칙                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ✅ 허용된 호출:                                                        │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │                                                                 │    │
│  │   API Layer ──────────────────────► Agent Layer                │    │
│  │       │                                  │                      │    │
│  │       │                                  ▼                      │    │
│  │       │                            Tools Layer                  │    │
│  │       │                                  │                      │    │
│  │       │                                  ▼                      │    │
│  │       └──────────────────────► Services Layer                  │    │
│  │                                                                 │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  ❌ 금지된 호출:                                                        │
│  • API Layer → Tools Layer (직접 호출 금지, Agent 통해야 함)            │
│  • Tools Layer → Agent Layer (역방향 호출 금지)                         │
│  • Services Layer → Agent Layer (역방향 호출 금지)                      │
│                                                                          │
│  ⚠️ 예외 (허용):                                                       │
│  • API Layer → Services Layer (템플릿 목록, 파일 다운로드 등 단순 조회) │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.3. 파일 구조

#### 3.3.1. Agent (2개 파일 - 활성)

```
backend/app/agents/presentation/
├── __init__.py                            # 패키지 초기화 + 레거시 호환
├── base_agent.py (200 lines)              # 공통 로직 ✅ 활성
└── unified_presentation_agent.py (700 lines)  # 통합 Agent ✅ 활성
                                           # - Quick PPT (mode="quick")
                                           # - Template PPT (mode="template")

# ⚠️ 삭제됨 (2025-12-03): archived/ 폴더 전체 삭제
# - presentation_agent.py              (Legacy Quick PPT Agent)
# - templated_ppt_react_agent.py       (Legacy Template ReAct Agent)
# - templated_ppt_plan_execute_agent.py (Legacy Plan-Execute Agent)
# - content_structurer.py              (Legacy HTML Pipeline)
# - html_generator.py                  (Legacy HTML Pipeline)
# - orchestrator.py                    (Legacy Orchestrator)
```

#### 3.3.2. Tools (7개 활성)

```
backend/app/tools/presentation/
├── __init__.py
├── outline_generation_tool.py         # 아웃라인 생성
├── quick_pptx_builder_tool.py         # Quick PPT 빌드 (Service 호출)
├── template_analyzer_tool.py          # 템플릿 구조 분석
├── content_mapping_tool.py            # 콘텐츠 ↔ 슬라이드 매핑
├── templated_pptx_builder_tool.py     # Template PPT 빌드 (Service 호출)
├── visualization_tool.py              # 시각화 유형 추천
├── ppt_quality_validator_tool.py      # 품질 검증
├── test_presentation_tools.py         # 도구 테스트
└── archived/                          # 12개 휴면 Tool (향후 확장용)
    ├── README.md
    ├── assembly_tools.py
    ├── content_assembly_tool.py
    ├── content_planning_tools.py
    ├── design_tools.py
    ├── pptxgenjs_tool.py
    ├── presentation_pipeline_tool.py
    ├── presentation_tools.py
    ├── style_analysis_tool.py
    ├── template_application_tool.py
    └── visualization_tools.py
```

#### 3.3.3. Services (12개 활성)

```
backend/app/services/presentation/
├── __init__.py
├── quick_ppt_generator_service.py     # Quick PPT 핵심 서비스
├── templated_ppt_generator_service.py # Template PPT 핵심 서비스
├── ppt_template_manager.py            # 템플릿 CRUD 관리
├── ppt_models.py                      # 데이터 모델 (SlideSpec, DeckSpec)
├── thumbnail_generator.py             # 슬라이드 썸네일 생성
├── template_migration_service.py      # 템플릿 마이그레이션
├── template_debugger.py               # 템플릿 디버깅 유틸리티
├── template_content_cleaner.py        # 템플릿 콘텐츠 정리
├── enhanced_object_processor.py       # PPT 오브젝트 고급 처리
├── dynamic_template_manager.py        # 동적 템플릿 관리
├── product_template_manager.py        # 제품별 템플릿 관리
├── ppt_template_extractor.py          # 템플릿 메타데이터 추출
└── archived/                          # 4개 미사용 Service
    ├── README.md
    ├── enhanced_ppt_generator_service.py
    ├── pptx_template_analyzer.py
    ├── quick_html_presentation_service.py
    └── template_metadata_extractor.py
```

#### 3.3.4. API Endpoints (presentation.py)

```
backend/app/api/v1/presentation.py (~2100 lines)

✅ 활성 엔드포인트 (PPT 생성):
├── POST /agent/presentation/build-quick              # Quick PPT (SSE)
├── POST /agent/presentation/build-with-template-react # Template PPT (ReAct)
├── POST /agent/presentation/build-with-template-plan-execute # (Plan-Execute)
├── POST /agent/presentation/build-unified            # 🚀 통합 (권장)
├── POST /agent/presentation/build-with-template      # Template PPT (표준)
└── POST /agent/presentation/build-from-outline       # 아웃라인 기반 생성

✅ 활성 엔드포인트 (템플릿 관리):
├── GET  /agent/presentation/templates                # 템플릿 목록
├── GET  /agent/presentation/templates/{id}           # 템플릿 상세
├── GET  /agent/presentation/templates/{id}/thumbnail # 썸네일
├── GET  /agent/presentation/templates/{id}/layouts   # 레이아웃 목록
├── POST /agent/presentation/templates/upload         # 템플릿 업로드
└── DELETE /agent/presentation/templates/{id}         # 템플릿 삭제

✅ 활성 엔드포인트 (유틸리티):
├── GET  /agent/presentation/view/{filename}          # HTML 뷰어
├── GET  /agent/presentation/outline/{filename}       # 아웃라인 조회
├── GET  /agent/presentation/download/{filename}      # 파일 다운로드
└── POST /agent/presentation/outline                  # 아웃라인 생성

❌ Deprecated 엔드포인트 (410 Gone):
├── POST /agent/presentation/generate   # Legacy HTML Pipeline
└── POST /agent/generate                # Legacy Agent Interface
```

---

## 4. 데이터 모델

### 4.1. 핵심 모델

```python
@dataclass
class SlideSpec:
    title: str                          # 슬라이드 제목
    key_message: str = ""               # 핵심 메시지
    bullets: List[str] = field(default_factory=list)  # 불릿 포인트
    layout: str = "title-and-content"   # 레이아웃 타입
    diagram: Optional[DiagramData] = None  # 다이어그램 데이터
    visual_suggestion: str = ""         # 시각화 제안
    speaker_notes: str = ""             # 발표자 노트

@dataclass
class DeckSpec:
    topic: str                          # 발표 주제
    slides: List[SlideSpec]             # 슬라이드 목록
    max_slides: int = 10                # 최대 슬라이드 수
    template_style: str = "business"    # 템플릿 스타일
```

### 4.2. API 요청 모델

```python
class UnifiedPresentationBuildRequest(BaseModel):
    session_id: str
    source_message_id: Optional[str] = None
    mode: str  # "quick" | "template"
    pattern: str  # "react" | "plan_execute"
    template_id: Optional[str] = None
    max_slides: int = 8
```

---

## 5. 통합 에이전트 구현 (2025-12-03 Updated)

### 5.1. Base Agent 클래스

**위치**: `/backend/app/agents/presentation/base_agent.py`

**공통 로직**:
- `_parse_agent_response()`: LLM 응답 파싱 (다중 JSON 포맷 지원)
- `_execute_tool()`: 도구 실행 및 예외 처리
- `_log_step()`: 실행 단계 로깅
- `_init_execution()`: 실행 초기화
- `_finalize_execution()`: 실행 종료 및 메타데이터 추가

**JSON 파싱 개선사항 (2025-12-03)**:
```python
# 다중 JSON 포맷 지원
1. 표준 JSON: {"action": "...", "action_input": {...}}
2. Markdown 블록: ```json\n{...}\n```
3. 줄바꿈 포함: action_input 내 실제 줄바꿈 처리
4. 부분 JSON: 불완전한 JSON에서 핵심 필드 추출
```

### 5.2. Unified Presentation Agent

**위치**: `/backend/app/agents/presentation/unified_presentation_agent.py`

**핵심 기능**:
```python
class UnifiedPresentationAgent(BaseAgent):
    """
    Quick PPT + Template PPT 통합 에이전트
    
    Features:
    - Mode 분기: "quick" | "template"
    - Pattern 분기: "react" | "plan_execute"
    - 7개 LangChain Tools 동적 선택
    - Auto-injection: deck_spec, mappings, template_structure
    - Smart Fallback: JSON 파싱 실패 시 재생성
    """
    
    async def run(
        self,
        mode: str,  # "quick" | "template"
        pattern: str,  # "react" | "plan_execute"
        topic: str,
        context_text: str,
        template_id: Optional[str] = None,
        max_slides: int = 8,
    ) -> Dict[str, Any]:
        """Mode와 Pattern에 따라 동적으로 분기"""
```

**지원 조합**:
| Mode | Pattern | 사용 사례 |
|------|---------|----------|
| quick | react | 빠른 PPT 생성 (기본) |
| quick | plan_execute | 계획 기반 빠른 생성 |
| template | react | 템플릿 기반 생성 (기본) |
| template | plan_execute | 복잡한 템플릿 작업 |

**Auto-injection 메커니즘**:
```python
# 이전 도구 결과를 다음 도구 입력에 자동 주입
- outline_generation → deck_spec 캡처
- content_mapping → mappings 캡처  
- template_analyzer → template_structure 캡처

# templated_pptx_builder 호출 시 자동 주입
if tool_name == "templated_pptx_builder":
    tool_input["deck_spec"] = self._captured_deck_spec
    tool_input["mappings"] = self._captured_mappings
    tool_input["template_structure"] = self._captured_template_structure
```

### 5.3. 통합 API 엔드포인트

**위치**: `/backend/app/api/v1/presentation.py`

```python
# Quick PPT 엔드포인트
@router.post("/agent/presentation/build-quick")
async def build_presentation_quick(req: QuickPresentationBuildRequest, ...):
    """Quick PPT 생성 (SSE 스트리밍)"""
    # 내부적으로 unified_presentation_agent.run() 호출
    # mode="quick", pattern="react"

# Template PPT 엔드포인트 (ReAct)
@router.post("/agent/presentation/build-with-template-react")
async def build_presentation_with_template_react(req: TemplatedPresentationBuildRequest, ...):
    """Template PPT 생성 (ReAct 패턴)"""
    # 내부적으로 unified_presentation_agent.run() 호출
    # mode="template", pattern="react"

# Template PPT 엔드포인트 (Plan-Execute)
@router.post("/agent/presentation/build-with-template-plan-execute")
async def build_presentation_with_template_plan_execute(req: TemplatedPresentationBuildRequest, ...):
    """Template PPT 생성 (Plan-Execute 패턴)"""
    # 내부적으로 unified_presentation_agent.run() 호출
    # mode="template", pattern="plan_execute"

# 🚀 통합 엔드포인트 (권장)
@router.post("/agent/presentation/build-unified")
async def build_presentation_unified(req: UnifiedPresentationRequest, ...):
    """
    통합 에이전트 기반 PPT 생성
    
    Parameters:
    - mode: "quick" | "template"
    - pattern: "react" | "plan_execute"
    - template_id: 템플릿 ID (mode="template"인 경우 필수)
    """
```

**핵심 포인트**:
- ✅ **모든 PPT 생성이 `unified_presentation_agent`를 통해 처리됨**
- ✅ 엔드포인트는 사용자 편의를 위해 분리 유지
- ✅ 내부 구현은 완전히 통합됨 (코드 중복 제거)
- ✅ SSE 스트리밍으로 실시간 진행 상황 전달

### 5.4. 삭제된 레거시 코드 (2025-12-03)

**삭제된 Import (presentation.py)**:
```python
# ❌ 제거됨: 삭제된 archived/ 모듈 참조
from app.agents.presentation.archived.content_structurer import structure_markdown_to_outline
from app.agents.presentation.archived.html_generator import generate_presentation_html
from app.agents.presentation.archived.orchestrator import presentation_agent
from app.agents.presentation.archived.presentation_agent import quick_ppt_react_agent
from app.agents.presentation.archived.templated_ppt_react_agent import templated_ppt_react_agent
from app.agents.presentation.archived.templated_ppt_plan_execute_agent import templated_ppt_plan_execute_agent
```

**Deprecated 엔드포인트 (410 Gone)**:
```python
@router.post("/agent/presentation/generate", deprecated=True)
async def generate_agent_presentation(...):
    """[DEPRECATED] HTML 파이프라인 - 더 이상 지원하지 않음"""
    raise HTTPException(status_code=410, detail={
        "error": "This endpoint has been deprecated and removed.",
        "alternatives": ["/build-quick", "/build-unified"]
    })

@router.post("/agent/generate", deprecated=True)
async def generate_presentation_with_agent(...):
    """[DEPRECATED] 레거시 에이전트 - 더 이상 지원하지 않음"""
    raise HTTPException(status_code=410, detail={
        "error": "This endpoint has been deprecated and removed.",
        "alternatives": ["/build-quick", "/build-unified"]
    })
```

### 5.5. 개선 효과

| 항목 | Before | After | 개선율 |
|------|--------|-------|--------|
| **코드 중복** | 40-60% | <10% | 83% 감소 |
| **Agent 파일** | 3개 (+ 6개 archived) | 2개 | 78% 감소 |
| **레거시 Import** | 6개 | 0개 | 100% 제거 |
| **API 복잡도** | 분산된 로직 | 통합된 라우팅 | 단순화 |
| **유지보수성** | 낮음 | 높음 | 대폭 개선 |

---

## 6. 성능 지표

### 6.1. 측정 결과

| 서비스 | 평균 시간 | 슬라이드 수 | 파일 크기 |
|--------|-----------|-------------|-----------|
| Quick PPT | 0.04초 | 3-12개 | 30-50KB |
| Template PPT | 0.08-0.15초 | 3-15개 | 45-100KB |

### 6.2. 품질 개선

| 항목 | 개선 내용 |
|------|-----------|
| **Smart Fallback** | 파싱 실패 시 LLM 재생성 |
| **Layout Tag** | 다양한 레이아웃 지원 |
| **Title Cleaning** | 불필요한 접두사 제거 |
| **Reproducibility** | Temperature 0.0 고정 |

---

## 7. 구현 현황

### 7.1. 완료된 작업 ✅

| 항목 | 상태 | 날짜 |
|------|------|------|
| Unified Agent 구현 | ✅ 완료 | 2025-12-02 |
| Base Agent 클래스 | ✅ 완료 | 2025-12-02 |
| Quick PPT 통합 | ✅ 완료 | 2025-12-02 |
| Template PPT 통합 | ✅ 완료 | 2025-12-02 |
| Service 레이어 일관성 | ✅ 완료 | 2025-12-02 |
| SSE 스트리밍 | ✅ 완료 | 2025-12-02 |
| 품질 검증 도구 | ✅ 완료 | 2025-12-02 |
| **agents/presentation/archived/ 삭제** | ✅ 완료 | 2025-12-03 |
| **레거시 Import 제거** | ✅ 완료 | 2025-12-03 |
| **Deprecated 엔드포인트 처리 (410)** | ✅ 완료 | 2025-12-03 |
| **4-Layer 아키텍처 문서화** | ✅ 완료 | 2025-12-03 |
| **Base Agent JSON 파싱 개선** | ✅ 완료 | 2025-12-03 |
| **Content Mapping Tool 필드 호환성** | ✅ 완료 | 2025-12-03 |
| **프론트엔드 진행 메시지 개선** | ✅ 완료 | 2025-12-03 |

### 7.2. 삭제된 파일 목록 (2025-12-03)

| 위치 | 파일/폴더 | 이유 |
|------|----------|------|
| `agents/presentation/archived/` | 전체 폴더 | unified_agent로 통합됨 |
| `agents/presentation_agent.py.backup` | 백업 파일 | 불필요한 백업 |
| `agents/tools/` | 빈 디렉토리 | 미사용 |

### 7.3. 알려진 제한사항

| 항목 | 설명 | 우회 방법 |
|------|------|----------|
| 컨텍스트 길이 제한 | AI 답변이 너무 길면 일부 잘림 | 슬라이드 12개 이하로 요청 |
| 복잡한 다이어그램 | 플로우차트 등 미지원 | 표 형태로 대체 |
| 이미지 삽입 | 외부 이미지 자동 삽입 미지원 | 수동 추가 |
| 템플릿 호환성 | 일부 복잡한 템플릿 레이아웃 | 단순 레이아웃 권장 |

---

## 8. 관련 문서

- [UNIFIED_AGENT_IMPLEMENTATION_COMPLETE.md](/home/admin/Dev/abekm/UNIFIED_AGENT_IMPLEMENTATION_COMPLETE.md) - 통합 에이전트 구현 완료 보고서
- [PRESENTATION_API_MIGRATION_GUIDE.md](/home/admin/Dev/abekm/PRESENTATION_API_MIGRATION_GUIDE.md) - API 마이그레이션 가이드
- [AGENT_TOOL_ARCHITECTURE_REPORT.md](/home/admin/Dev/abekm/AGENT_TOOL_ARCHITECTURE_REPORT.md) - Agent/Tool 아키텍처 분석
- [AGENT_FRONTEND_INTEGRATION_GUIDE.md](/home/admin/Dev/abekm/AGENT_FRONTEND_INTEGRATION_GUIDE.md) - 프론트엔드 통합 가이드

---

## 9. 변경 이력

| 날짜 | 버전 | 변경 내용 |
|------|------|----------|
| 2025-12-02 | 4.1.0 | Quick PPT + Template PPT 완전 통합 |
| 2025-12-03 | 4.2.0 | 레거시 코드 정리, 4-Layer 아키텍처 문서화 |

---

## 부록 A: 코드 참조

### A.1. 프론트엔드 핵심 파일

| 파일 | 역할 |
|------|------|
| `pages/user/AgentChatPage.tsx` | PPT 생성 이벤트 리스너 |
| `chat/components/presentation/PresentationActionBar.tsx` | 📊, 📝 버튼 |
| `chat/components/presentation/PresentationOutlineModal.tsx` | PPT 생성 설정 모달 |
| `chat/components/presentation/usePresentation.ts` | API 호출 훅 |

### A.2. 백엔드 핵심 파일

| 파일 | 역할 | 상태 |
|------|------|------|
| `agents/presentation/unified_presentation_agent.py` | 통합 Agent (Quick + Template) | ✅ 활성 |
| `agents/presentation/base_agent.py` | Base Agent 클래스 | ✅ 활성 |
| `agents/presentation/__init__.py` | 패키지 초기화 + 레거시 호환 | ✅ 활성 |
| `api/v1/presentation.py` | API 엔드포인트 | ✅ 활성 |
| `services/presentation/quick_ppt_generator_service.py` | Quick PPT 서비스 | ✅ 활성 |
| `services/presentation/templated_ppt_generator_service.py` | Template PPT 서비스 | ✅ 활성 |
| `services/presentation/ppt_template_manager.py` | 템플릿 관리 | ✅ 활성 |
| `tools/presentation/outline_generation_tool.py` | 아웃라인 생성 도구 | ✅ 활성 |
| `tools/presentation/quick_pptx_builder_tool.py` | Quick PPT 빌드 도구 | ✅ 활성 |
| `tools/presentation/template_analyzer_tool.py` | 템플릿 분석 도구 | ✅ 활성 |
| `tools/presentation/content_mapping_tool.py` | 콘텐츠 매핑 도구 | ✅ 활성 |
| `tools/presentation/templated_pptx_builder_tool.py` | Template PPT 빌드 도구 | ✅ 활성 |
| `tools/presentation/visualization_tool.py` | 시각화 추천 도구 | ✅ 활성 |
| `tools/presentation/ppt_quality_validator_tool.py` | 품질 검증 도구 | ✅ 활성 |

### A.3. 아카이브된 파일 (휴면 상태)

| 위치 | 파일 수 | 설명 | 상태 |
|------|---------|------|------|
| `tools/presentation/archived/` | 12개 | 휴면 Tool 파일 | ⚠️ 향후 확장용 |
| `services/presentation/archived/` | 4개 | 미사용 Service 파일 | ⚠️ 향후 확장용 |

### A.4. 삭제된 파일 (2025-12-03)

| 위치 | 파일 | 삭제 이유 |
|------|------|----------|
| `agents/presentation/archived/` | 전체 폴더 | unified_agent로 통합됨 |
| `agents/presentation_agent.py.backup` | 백업 파일 | 불필요한 백업 |
| `agents/tools/` | 빈 디렉토리 | 미사용 |

---

## 부록 B: ReAct 패턴 참고

### B.1. ReAct Loop

```
Thought → Action → Observation → Thought → Action → ...
```

**참고 자료**:
- [ReAct: Synergizing Reasoning and Acting in Language Models](https://arxiv.org/abs/2210.03629)
- [LangChain Agent Documentation](https://python.langchain.com/docs/modules/agents/)

### B.2. Plan-and-Execute

```
Plan: [Step1, Step2, Step3, ...]
Execute: Step1 → Result → (Replan if needed) → Step2 → ...
```

**참고 자료**:
- [LangGraph Plan-and-Execute](https://langchain-ai.github.io/langgraph/tutorials/plan-and-execute/plan-and-execute/)
- [Plan-and-Solve Prompting](https://arxiv.org/abs/2305.04091)
