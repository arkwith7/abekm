# PPT 생성 시스템 설계 및 구현 문서

## 1. 개요

본 문서는 PPT 생성 시스템의 아키텍처 설계와 현재 구현 상태를 정리한다. 

### ⚠️ 현재 구현 상태 정직한 평가

| 구분 | 현재 상태 | 목표 상태 |
|------|----------|----------|
| **아키텍처 유형** | 서비스 기반 자동화 (Pipeline) | Agentic AI (ReAct/Plan-and-Execute) |
| **도구 선택** | 하드코딩 / UI 버튼 | LLM이 동적 결정 |
| **계획 수립** | 없음 (고정 파이프라인) | LLM이 상황에 맞게 계획 |
| **중간 평가** | 없음 | Observation → 다음 Action 결정 |
| **피드백 루프** | 없음 | 품질 검증 → 수정 반복 |

**현재 구현은 "Agentic AI"가 아닌 "서비스 기반 자동화 파이프라인"입니다.**

**문서 최종 업데이트**: 2025-12-01  
**구현 버전**: 2.2.0  
**구현 상태**: ✅ Production Ready (서비스 기반) / 🔲 Agentic AI 전환 필요

---

## 2. 현재 구현 vs Agentic AI 비교

### 2.1. 현재 구현: 서비스 기반 자동화 (Pipeline)

```
┌─────────────────────────────────────────────────────────────────────────┐
│              현재 구현: 고정 파이프라인 (Non-Agentic)                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  사용자 ──→ 버튼 클릭 ──→ API 직접 호출 ──→ 서비스 실행 ──→ 결과 반환  │
│                                                                          │
│  ❌ LLM이 도구를 선택하지 않음 - if-else로 하드코딩                     │
│  ❌ 계획 수립 없음 - 고정된 순서로 실행                                 │
│  ❌ 중간 결과 평가 없음 - 실패해도 재시도 불가                          │
│  ❌ 피드백 루프 없음 - 품질 검증 후 수정 불가                           │
│                                                                          │
│  코드 예시 (orchestrator.py):                                           │
│  ┌────────────────────────────────────────────────────────────────────┐ │
│  │ async def _execute_quick_strategy(...):                            │ │
│  │     # 1단계: 아웃라인 생성 (항상 실행)                              │ │
│  │     outline_result = await outline_generation_tool._arun(...)      │ │
│  │                                                                     │ │
│  │     # 2단계: 시각화 (항상 실행)                                     │ │
│  │     viz_result = await visualization_tool._arun(...)               │ │
│  │                                                                     │ │
│  │     # 3단계: 빌더 (항상 실행)                                       │ │
│  │     build_result = await quick_pptx_builder_tool._arun(...)        │ │
│  └────────────────────────────────────────────────────────────────────┘ │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.2. 목표: 진정한 Agentic AI PPT 생성

```
┌─────────────────────────────────────────────────────────────────────────┐
│              목표 구현: Agentic AI (ReAct Pattern)                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  사용자: "인슐린펌프 제품소개서 PPT 만들어줘"                           │
│                                                                          │
│  ┌─────────────────────────────────────────────────────────────────┐    │
│  │ LLM Agent (ReAct / Plan-and-Execute)                            │    │
│  │                                                                  │    │
│  │  💭 Thought: "제품소개서를 만들려면 먼저 제품 정보가 필요하다"   │    │
│  │  🔧 Action: RAG_search_tool("인슐린펌프 제품 정보")              │    │
│  │  📋 Observation: 제품 스펙, 기능, 규제 정보 등 검색됨            │    │
│  │                                                                  │    │
│  │  💭 Thought: "12개 섹션으로 구성하면 좋겠다"                     │    │
│  │  🔧 Action: outline_planning_tool(context, sections=12)         │    │
│  │  📋 Observation: 아웃라인 생성됨, 기술사양 섹션에 수치 많음      │    │
│  │                                                                  │    │
│  │  💭 Thought: "기술사양은 표로 표현하는게 좋겠다"                 │    │
│  │  🔧 Action: visualization_recommender_tool(slide_4)             │    │
│  │  📋 Observation: table 추천됨                                    │    │
│  │                                                                  │    │
│  │  💭 Thought: "기업 템플릿이 있는지 확인해보자"                   │    │
│  │  🔧 Action: template_search_tool(user_id)                       │    │
│  │  📋 Observation: "SK매직_공식템플릿.pptx" 발견                   │    │
│  │                                                                  │    │
│  │  💭 Thought: "이제 PPT를 생성하자"                               │    │
│  │  🔧 Action: pptx_builder_tool(outline, template, visualizations)│    │
│  │  📋 Observation: PPT 생성됨                                      │    │
│  │                                                                  │    │
│  │  💭 Thought: "품질을 검증해보자"                                 │    │
│  │  🔧 Action: quality_validator_tool(pptx_path)                   │    │
│  │  📋 Observation: 슬라이드 3에 텍스트 오버플로우 감지             │    │
│  │                                                                  │    │
│  │  💭 Thought: "슬라이드 3을 수정해야겠다"                         │    │
│  │  🔧 Action: slide_regenerator_tool(slide_3, fix_overflow=True)  │    │
│  │  📋 Observation: 수정 완료                                       │    │
│  │                                                                  │    │
│  │  ✅ Final Answer: "12슬라이드 제품소개서가 완성되었습니다"       │    │
│  └─────────────────────────────────────────────────────────────────┘    │
│                                                                          │
│  📎 quick_presentation_인슐린펌프_제품소개자료.pptx                     │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 2.3. 핵심 차이점

| 항목 | 현재 (서비스 기반) | 목표 (Agentic AI) |
|------|-------------------|-------------------|
| **의사결정 주체** | 코드 로직 (if-else) | LLM Agent |
| **도구 선택** | 하드코딩된 순서 | LLM이 상황에 맞게 선택 |
| **실행 흐름** | 고정 파이프라인 | 동적 계획 및 실행 |
| **오류 처리** | 실패 시 종료 | 대안 전략 시도 |
| **품질 보장** | 없음 | 검증 → 수정 반복 |
| **컨텍스트 활용** | 제한적 | 이전 단계 결과 참조 |

---

## 3. 현재 서비스 라인 (하이브리드 모드)

### 3.1. 하이브리드 PPT 생성 서비스 라인 (2025-12-01 업데이트)

**🎯 핵심 개선**: Quick PPT가 **자동 생성 + 재생성 옵션** 제공 방식으로 변경

| 서비스 라인 | UI 흐름 | 설명 | 최적 사용 시나리오 |
|------------|---------|------|-------------------|
| **Quick PPT (하이브리드)** | 자동 생성 → 📝 구조 확인 및 재생성 | ✅ RAG 검색 → 즉시 PPT 생성<br>✅ 구조화 답변 메타데이터 저장<br>✅ 필요시 템플릿 변경 후 재생성 | 빠른 발표자료 + 품질 조정 가능 |
| **Template PPT** | 📝 PPT 생성 설정 → 템플릿 선택 → 생성 | 템플릿 선택 및 매핑 편집 후 생성 (기존 동일) | 기업 템플릿 적용, 정교한 레이아웃 필요 시 |

### 3.2. 하이브리드 모드 아키텍처 (Quick PPT 개선)

```
사용자 질의: "자동차산업 특허분석 PPT 만들어 주세요" + PPT 에이전트 선택
    ↓
┌──────────────────────────────────────────────────────────────────┐
│ 백엔드: /api/v1/agent/chat/stream (PPT_GENERATION 의도 감지)    │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Step 1: RAG 기반 검색 (2-3초)                                  │
│  ├─ paper_search_agent.execute_strategy()                       │
│  ├─ 전략: [keyword, fulltext, deduplicate, rerank, context]     │
│  └─ 결과: chunks (500-1000자 청크들)                            │
│                                                                  │
│  Step 2: 구조화 답변 생성 (3-5초)                               │
│  ├─ paper_search_agent.generate_answer()                        │
│  ├─ 입력: chunks + 사용자 질의                                  │
│  └─ 결과: structured_answer (마크다운 형식, 1500-3000자)        │
│                                                                  │
│  Step 3: ReAct Agent PPT 생성 (5-8초)                           │
│  ├─ quick_ppt_react_agent.run()                                 │
│  ├─ 입력: context_text = structured_answer                      │
│  └─ 워크플로우:                                                 │
│      ├─ Iteration 1: outline_generation_tool (아웃라인)         │
│      ├─ Iteration 2: quick_pptx_builder_tool (PPTX 생성)        │
│      └─ Iteration 3: Final Answer (파일 경로 반환)              │
│                                                                  │
│  Step 4: 응답 전송                                              │
│  ├─ content: "✅ PPT 생성 완료! [다운로드 링크]"                │
│  └─ metadata:                                                   │
│      ├─ ppt_file_url: "/download/파일명.pptx"                  │
│      ├─ ppt_file_name: "quick_presentation_....pptx"           │
│      └─ structured_content: "[구조화된 답변 전문]"              │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
    ↓
┌──────────────────────────────────────────────────────────────────┐
│ 프론트엔드: AgentChatPage                                        │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  채팅 화면 표시:                                                 │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ ✅ **PPT 생성 완료!**                                       │ │
│  │                                                             │ │
│  │ 📎 [quick_presentation_자동차산업_특허_분석.pptx]          │ │
│  │                                                             │ │
│  │ [📝 구조 확인 및 재생성] ← 버튼 1개만                      │ │
│  └────────────────────────────────────────────────────────────┘ │
│                                                                  │
│  사용자 행동:                                                    │
│  ├─ Option 1: 다운로드 링크 클릭 → 즉시 다운로드               │
│  └─ Option 2: "구조 확인 및 재생성" 클릭                        │
│      └─ PresentationOutlineModal 열림                           │
│          ├─ [AI 답변] 탭: metadata.structured_content 표시     │
│          ├─ [템플릿 관리] 탭: 템플릿 선택                       │
│          └─ "PPT 생성하기" → 템플릿 적용 후 재생성              │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

**주요 개선 사항**:
- ✅ **사용자 클릭 제거**: "📊 PPT로 만들기" 버튼 없이 자동 생성
- ✅ **RAG 검색 통합**: 사용자 질의 기반 문서 검색 → 콘텐츠 반영
- ✅ **ReAct Agent**: LLM이 도구를 동적으로 선택하여 실행
- ✅ **구조화 답변 저장**: 메타데이터에 백업하여 재생성 시 활용
- ✅ **UX 간소화**: 버튼 1개로 명확한 의도 전달

### 3.3. PPT 의도 자동 감지 & 강제 실행 로직

- **프론트엔드**
    - `PresentationAgentChatPage` 는 모든 전송 요청에 `tool='ppt'` 를 강제로 포함 → 사용자가 별도 도구 선택을 하지 않아도 PPT 에이전트 호출
    - `MessageComposer` 에서 선택한 도구가 있는 경우 `useAgentChat.sendMessage()` 로 전달 → 백엔드가 해당 도구를 즉시 실행
    - `MessageBubble` 은 `metadata.ppt_file_url` 이 존재할 때만 "📝 구조 확인 및 재생성" 버튼을 노출하여 불필요한 버튼 혼잡 제거
- **백엔드**
    - `agent.py` 상단에 `_should_force_ppt_generation()` 휴리스틱 추가
        - 조건: (1) 도구 파라미터가 `ppt` 이거나 (2) 질의 안에 `ppt/프레젠테이션/발표자료/슬라이드` + `만들/작성/생성` 패턴이 동시에 포함될 때
    - 분류 이후 `intent` 를 강제로 `AgentIntent.PPT_GENERATION` 으로 전환하고 즉시 하이브리드 플로우 진입
    - 메타데이터(`metadata.structured_content`, `metadata.ppt_file_url`)를 SSE 메시지에 포함 → 프론트에서 즉시 활용

이 로직 덕분에 **"PPT 만들어줘"** 와 같이 명시적으로 작성된 질의는 별도 버튼 클릭 없이 곧바로 RAG → ReAct → PPT 생성 파이프라인을 탄다.

### 3.2. UseCase 시나리오

#### UseCase 1: Quick PPT (원클릭 생성)

**시나리오**: "인슐린펌프 제품소개서 PPT 작성해 주세요"

```
사용자 → AI 채팅에서 질의
    ↓
AI Agent → 제품소개서 내용 생성 (참고자료 기반)
    ↓
[슬라이드 1] 인슐린펌프 제품소개 SK매직
[슬라이드 2] 목차
  1. 제품 개요
  2. 주요 기능
  3. 기술 사양
  ... (총 11개 섹션)
[슬라이드 3~12] 각 섹션별 상세 내용
    ↓
사용자 → "📊 PPT로 만들기" 버튼 클릭
    ↓
시스템 → 즉시 PPTX 파일 생성 (0.04초)
    ↓
결과 → 📎 quick_presentation_인슐린펌프_제품소개자료.pptx
```

**특징**:
- AI 답변의 구조를 자동으로 슬라이드화
- 번호 섹션([슬라이드 N])을 파싱하여 개별 슬라이드 생성
- 시각화 힌트 자동 감지 (차트, 표, 다이어그램)
- 기본 비즈니스 템플릿 적용

#### UseCase 2: Template PPT (템플릿 기반 생성)

**시나리오**: 기업 공식 템플릿으로 제품소개서 생성

```
사용자 → AI 답변 확인 후 "📝 PPT 생성 설정" 버튼 클릭
    ↓
팝업 모달 열림: "PPT 생성 설정"
    ├─ [AI 답변] 탭: 원본 AI 답변 확인
    ├─ [매핑 편집] 탭: 콘텐츠-슬라이드 매핑 조정
    └─ [템플릿 관리] 탭: 템플릿 업로드/선택
    ↓
사용자 → 템플릿 선택 (우측 상단 드롭다운)
    ↓
사용자 → 필요시 매핑 편집
    ↓
사용자 → "PPT 생성하기" 버튼 클릭
    ↓
시스템 → 템플릿 기반 PPTX 생성
    ↓
결과 → 기업 템플릿이 적용된 PPTX 파일
```

**특징**:
- 3개 탭 UI로 세밀한 제어 가능
- 사용자 업로드 템플릿 지원
- 슬라이드별 레이아웃 선택
- 콘텐츠 세그먼트와 텍스트박스 매핑

---

## 3. 시스템 아키텍처

### 3.1. 전체 구조도

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         Frontend UI Layer                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  [AI Agent 채팅]  ──→  "인슐린펌프 제품소개서 PPT 작성해 주세요"         │
│        ↓                                                                 │
│  [AI 답변] + [PPT 의도 감지] ──→  PresentationActionBar 표시            │
│        │                                                                 │
│        ├─────────────────────────┬───────────────────────────────┐       │
│        │                         │                               │       │
│        ▼                         ▼                               │       │
│  ┌──────────────────┐   ┌─────────────────────────────┐         │       │
│  │📊 PPT로 만들기   │   │📝 PPT 생성 설정             │         │       │
│  │                  │   │                             │         │       │
│  │ • 원클릭 생성    │   │ • 템플릿 선택               │         │       │
│  │ • Quick 모드     │   │ • AI 답변 탭                │         │       │
│  │ • 0.04초 생성    │   │ • 매핑 편집 탭              │         │       │
│  └────────┬─────────┘   │ • 템플릿 관리 탭            │         │       │
│           │             └─────────────┬───────────────┘         │       │
│           │                           │                          │       │
│           ▼                           ▼                          │       │
│  ┌─────────────────┐     ┌───────────────────────────┐          │       │
│  │ usePresentation │     │ PresentationOutlineModal  │          │       │
│  │ .buildFromMsg() │     │ → buildWithOutline()      │          │       │
│  └────────┬────────┘     └─────────────┬─────────────┘          │       │
└───────────┼────────────────────────────┼────────────────────────┘       
            │                            │                                 
            ▼                            ▼                                 
┌─────────────────────────────────────────────────────────────────────────┐
│                         Backend API Layer                                │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  POST /api/v1/agent/presentation/build-quick                            │
│    └─ QuickPresentationBuildRequest                                     │
│         ├─ session_id, source_message_id                                │
│         └─ message (폴백용)                                             │
│                                                                          │
│  POST /api/v1/agent/presentation/build-with-template                    │
│    └─ TemplatedPresentationBuildRequest                                 │
│         ├─ session_id, source_message_id                                │
│         ├─ template_id                                                  │
│         ├─ outline (아웃라인 + 매핑)                                    │
│         └─ slide_management (슬라이드 관리 정보)                        │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                         Service Layer                                    │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│  ┌──────────────────────────────┐  ┌────────────────────────────────┐   │
│  │ QuickPPTGeneratorService     │  │ TemplatedPPTGeneratorService   │   │
│  │                              │  │                                │   │
│  │ • generate_fixed_outline()  │  │ • generate_enhanced_outline() │   │
│  │ • build_quick_pptx()        │  │ • build_templated_pptx()      │   │
│  │ • _detect_visualization()   │  │ • build_enhanced_pptx_with_   │   │
│  │ • _create_sample_chart()    │  │   slide_management()          │   │
│  │                              │  │ • _build_with_mappings()      │   │
│  └──────────────────────────────┘  └────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────┐  ┌────────────────────────────────┐   │
│  │ PPTTemplateManager           │  │ EnhancedPPTObjectProcessor    │   │
│  │                              │  │                                │   │
│  │ • get_template_details()    │  │ • apply_mappings()            │   │
│  │ • list_templates()          │  │ • process_slide_management()  │   │
│  └──────────────────────────────┘  └────────────────────────────────┘   │
│                                                                          │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ ppt_models.py: DeckSpec, SlideSpec, DiagramData, ChartData       │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 프론트엔드 구현 상세

### 4.1. 파일 구조

```
frontend/src/pages/user/chat/components/presentation/
├── PresentationActionBar.tsx      # 📊, 📝 버튼 컴포넌트
├── PresentationOutlineModal.tsx   # PPT 생성 설정 팝업 모달
├── AnswerTab.tsx                  # AI 답변 탭
├── TemplateManager.tsx            # 템플릿 관리 탭
├── usePresentation.ts             # PPT 생성 훅 (SSE 스트리밍)
└── ...

frontend/src/components/presentation/
├── PPTMappingWithSlideManager.tsx # 매핑 편집 UI
└── ...
```

### 4.2. PresentationActionBar 컴포넌트

```tsx
// frontend/src/pages/user/chat/components/presentation/PresentationActionBar.tsx
const PresentationActionBar: React.FC<Props> = ({ 
    sourceMessageId, 
    sessionId, 
    onBuildOneClick, 
    onOpenOutline 
}) => {
    return (
        <div className="flex items-center gap-2">
            {/* Quick PPT 버튼 */}
            <button
                onClick={() => onBuildOneClick(sourceMessageId, "general")}
                title="일반 PPT로 바로 생성"
            >
                📊 PPT로 만들기
            </button>
            
            {/* Template PPT 버튼 */}
            <button
                onClick={() => onOpenOutline(sourceMessageId, "general")}
                title="PPT 생성 설정 및 템플릿 선택"
            >
                📝 PPT 생성 설정
            </button>
        </div>
    );
};
```

### 4.3. usePresentation 훅

두 가지 메서드로 서비스 라인을 분리:

```typescript
// frontend/src/pages/user/chat/components/presentation/usePresentation.ts
export function usePresentation(sessionId: string) {
    
    // 🔵 서비스 라인 1: Quick PPT
    const buildFromMessage = async (
        sourceMessageId: string,
        opts?: {
            onProgress?: (p: BuildProgress) => void;
            onComplete?: (fileUrl: string, fileName?: string) => void;
            messageContent?: string;
        }
    ) => {
        // POST /api/v1/agent/presentation/build-quick
        const response = await fetch('/api/v1/agent/presentation/build-quick', {
            method: 'POST',
            body: JSON.stringify({
                session_id: sessionId,
                source_message_id: sourceMessageId,
                message: opts?.messageContent  // 폴백용
            })
        });
        // SSE 스트리밍 처리...
    };
    
    // 🟢 서비스 라인 2: Template PPT
    const buildWithOutline = async (
        sourceMessageId: string,
        outline: any,
        templateId?: string,
        opts?: { onProgress?: ...; onComplete?: ...; }
    ) => {
        // POST /api/v1/agent/presentation/build-with-template
        const response = await fetch('/api/v1/agent/presentation/build-with-template', {
            method: 'POST',
            body: JSON.stringify({
                session_id: sessionId,
                source_message_id: sourceMessageId,
                template_id: templateId,
                outline,
                slide_management: outline?.slide_management,
                object_mappings: outline?.object_mappings
            })
        });
        // SSE 스트리밍 처리...
    };
    
    return { buildFromMessage, buildWithOutline, getOutline, loading, error };
}
```

### 4.4. PresentationOutlineModal 컴포넌트

Template PPT 서비스 라인의 핵심 UI:

```tsx
// frontend/src/pages/user/chat/components/presentation/PresentationOutlineModal.tsx
const PresentationOutlineModal: React.FC<Props> = ({ open, onClose, ... }) => {
    const [primaryTab, setPrimaryTab] = useState<PrimaryTab>('answer');
    
    return (
        <div className="modal">
            {/* 헤더: 템플릿 선택 드롭다운 */}
            <div className="header">
                <h2>PPT 생성 설정</h2>
                <select value={selectedTemplateId} onChange={...}>
                    {allTemplates.map(t => (
                        <option key={t.id} value={t.id}>{t.name}</option>
                    ))}
                </select>
            </div>
            
            {/* 탭 네비게이션 */}
            <div className="tabs">
                <button onClick={() => setPrimaryTab('answer')}>AI 답변</button>
                <button onClick={() => setPrimaryTab('mapping')}>매핑 편집</button>
                <button onClick={() => setPrimaryTab('template')}>템플릿 관리</button>
            </div>
            
            {/* 탭 콘텐츠 */}
            {primaryTab === 'answer' && <AnswerTab sourceContent={sourceContent} />}
            {primaryTab === 'mapping' && <PPTMappingWithSlideManager ... />}
            {primaryTab === 'template' && <TemplateManager ... />}
            
            {/* 푸터: PPT 생성하기 버튼 */}
            <div className="footer">
                <button onClick={handleConfirm}>PPT 생성하기</button>
            </div>
        </div>
    );
};
```

### 4.5. AgentChatPage 이벤트 리스너

CustomEvent를 통한 PPT 생성 이벤트 처리:

```tsx
// frontend/src/pages/user/AgentChatPage.tsx
useEffect(() => {
    // 📝 PPT 생성 설정 이벤트
    const handleOpenOutline = (e: CustomEvent) => {
        const { sourceMessageId } = e.detail;
        setTargetMessageId(sourceMessageId);
        setOutlineModalOpen(true);
    };

    // 📊 PPT 바로 생성 이벤트
    const handleBuildOneClick = (e: CustomEvent) => {
        const { sourceMessageId, presentationType } = e.detail;
        const msg = messages.find(m => m.message_id === sourceMessageId);
        
        buildFromMessage(sourceMessageId, {
            onComplete: (fileUrl, fileName) => {
                // 다운로드 링크를 채팅 메시지로 추가
                addAssistantMessage(`📎 [${fileName}](${fileUrl})`);
            },
            messageContent: msg?.content
        });
    };

    window.addEventListener('presentation:openOutline', handleOpenOutline);
    window.addEventListener('presentation:buildOneClick', handleBuildOneClick);
    
    return () => {
        window.removeEventListener('presentation:openOutline', handleOpenOutline);
        window.removeEventListener('presentation:buildOneClick', handleBuildOneClick);
    };
}, [buildFromMessage, addAssistantMessage, messages]);
```

---

## 5. 백엔드 구현 상세

### 5.1. 파일 구조

```
backend/app/
├── api/v1/
│   └── presentation.py              # API 엔드포인트 정의
│
├── services/presentation/
│   ├── quick_ppt_generator_service.py    # Quick PPT 서비스 (원클릭)
│   ├── templated_ppt_generator_service.py # Template PPT 서비스
│   ├── ppt_template_manager.py           # 템플릿 관리
│   ├── enhanced_object_processor.py      # 오브젝트 매핑 처리
│   ├── ppt_models.py                     # 데이터 모델
│   └── ...
│
└── tools/presentation/
    ├── presentation_pipeline_tool.py     # 파이프라인 도구
    ├── quick_pptx_builder_tool.py        # 빌더 도구
    ├── ppt_quality_validator_tool.py     # 품질 검증 도구
    └── ...
```

### 5.2. Quick PPT API 엔드포인트

```python
# backend/app/api/v1/presentation.py

class QuickPresentationBuildRequest(BaseModel):
    session_id: str
    source_message_id: Optional[str] = None
    message: Optional[str] = None
    max_slides: int = 8

@router.post("/agent/presentation/build-quick")
async def build_presentation_quick(
    req: QuickPresentationBuildRequest,
    current_user: User = Depends(get_current_user),
    chat_manager: RedisChatManager = Depends(get_redis_chat_manager)
):
    async def stream():
        try:
            yield f"data: {json.dumps({'type': 'start'})}\n\n"
            
            # 1. 메시지 소스 추출
            if req.source_message_id:
                source_msg, msgs = await _get_message_by_id(...)
                topic, context_text, referenced_documents = _compose_context_from_messages(...)
            elif req.message:
                topic = req.message[:80]
                context_text = req.message
            
            # 2. 아웃라인 생성
            yield f"data: {json.dumps({'type': 'structuring'})}\n\n"
            deck = quick_ppt_service.generate_fixed_outline(
                topic=topic, 
                context_text=context_text, 
                max_slides=req.max_slides
            )
            
            # 3. PPTX 빌드
            yield f"data: {json.dumps({'type': 'building'})}\n\n"
            file_name = quick_ppt_service.build_quick_pptx(deck)
            file_url = f"/api/v1/agent/presentation/download/{file_name}"
            
            # 4. 완료
            yield f"data: {json.dumps({'type': 'complete', 'file_url': file_url, 'file_name': file_name})}\n\n"
            yield "data: [DONE]\n\n"
            
        except Exception as e:
            yield f"data: {json.dumps({'type': 'error', 'message': str(e)})}\n\n"
    
    return StreamingResponse(stream(), media_type="text/event-stream")
```

### 5.3. Template PPT API 엔드포인트

```python
# backend/app/api/v1/presentation.py

class TemplatedPresentationBuildRequest(BaseModel):
    session_id: str
    source_message_id: str
    template_id: str
    outline: Dict[str, Any]
    slide_management: Optional[List[SlideManagementInfo]] = None
    object_mappings: Optional[List[Dict[str, Any]]] = None
    content_segments: Optional[List[Dict[str, Any]]] = None

@router.post("/agent/presentation/build-with-template")
async def build_presentation_with_template(
    req: TemplatedPresentationBuildRequest,
    current_user: User = Depends(get_current_user)
):
    async def stream():
        try:
            yield f"data: {json.dumps({'type': 'start'})}\n\n"
            
            # 1. 템플릿 조회
            tpl = template_manager.get_template_details(req.template_id)
            template_path = tpl.get('cleaned_template_path') or tpl.get('path')
            
            yield f"data: {json.dumps({'type': 'outline_ready'})}\n\n"
            yield f"data: {json.dumps({'type': 'template_loading'})}\n\n"
            
            # 2. 아웃라인 → DeckSpec 변환
            deck = templated_ppt_service._parse_ai_response(...)
            
            # 3. 매핑 정보 추출
            text_box_mappings = req.object_mappings or req.outline.get('textBoxMappings')
            slide_management_info = req.outline.get('slide_management')
            
            yield f"data: {json.dumps({'type': 'building'})}\n\n"
            
            # 4. 매핑 기반 PPTX 빌드
            file_path = templated_ppt_service.build_enhanced_pptx_with_slide_management(
                deck,
                custom_template_path=template_path,
                text_box_mappings=text_box_mappings,
                slide_management=slide_management_info
            )
            
            # 5. 완료
            file_url = f"/api/v1/agent/presentation/download/{os.path.basename(file_path)}"
            yield f"data: {json.dumps({'type': 'complete', 'file_url': file_url})}\n\n"
            yield "data: [DONE]\n\n"
            
        except Exception as e:
            yield f"data: {json.dumps({'type': 'error', 'message': str(e)})}\n\n"
    
    return StreamingResponse(stream(), media_type="text/event-stream")
```

### 5.4. QuickPPTGeneratorService

```python
# backend/app/services/presentation/quick_ppt_generator_service.py

class QuickPPTGeneratorService:
    """
    general.prompt [발표 자료 생성 모드] 규칙 기반 원클릭 PPT 생성 서비스
    
    지원하는 구조:
    1. 제목 슬라이드 (필수) - ## 제목 + ### 📋 발표 개요
    2. 목차 슬라이드 (5개 이상 슬라이드시 필수)
    3. 본문 슬라이드들 - ### 제목 + 🔑 키 메시지 + 📝 상세 설명
    4. 마무리 슬라이드 (필수) - ### 감사합니다
    """
    
    def generate_fixed_outline(self, topic: str, context_text: str, max_slides: int = 8) -> DeckSpec:
        """AI 답변 텍스트를 파싱하여 DeckSpec 생성"""
        # 1. [슬라이드 N] 패턴 파싱
        # 2. 섹션별 제목, 키메시지, 불릿 추출
        # 3. 시각화 힌트 자동 감지
        pass
    
    def build_quick_pptx(self, deck: DeckSpec) -> str:
        """DeckSpec을 PPTX 파일로 변환"""
        prs = Presentation()
        for slide_spec in deck.slides:
            # 슬라이드 생성
            # 시각화 적용 (차트, 표)
            pass
        prs.save(output_path)
        return filename
    
    def _detect_visualization_hints(self, slide_spec: SlideSpec) -> Dict:
        """슬라이드 내용에서 시각화 힌트 감지"""
        # 차트 키워드: 증가, 감소, 비율, %
        # 표 키워드: 항목, 사양, 스펙
        # 프로세스 키워드: 단계, 과정, 절차
        pass
```

### 5.5. TemplatedPPTGeneratorService

```python
# backend/app/services/presentation/templated_ppt_generator_service.py

class TemplatedPPTGeneratorService:
    """템플릿 기반 PPT 생성 전용 서비스"""
    
    async def generate_enhanced_outline(self, topic: str, context_text: str, ...) -> DeckSpec:
        """AI 기반 향상된 아웃라인 생성"""
        # AI 호출로 구조화된 아웃라인 생성
        pass
    
    def build_enhanced_pptx_with_slide_management(
        self, 
        spec: DeckSpec,
        custom_template_path: Optional[str] = None,
        text_box_mappings: Optional[List] = None,
        slide_management: Optional[List] = None
    ) -> str:
        """슬라이드 관리가 포함된 Enhanced PPT 빌드"""
        
        if custom_template_path and (text_box_mappings or slide_management):
            # 매핑 기반 템플릿 빌드
            return self._build_with_mappings(...)
        else:
            # 기본 템플릿 빌드
            return self.build_templated_pptx(...)
```

---

## 6. 데이터 모델

### 6.1. 핵심 모델

```python
# backend/app/services/presentation/ppt_models.py

@dataclass
class SlideSpec:
    title: str                          # 슬라이드 제목
    key_message: str = ""               # 핵심 메시지
    bullets: List[str] = field(default_factory=list)  # 불릿 포인트
    layout: str = "title-and-content"   # 레이아웃 타입
    diagram: Optional[DiagramData] = None  # 다이어그램 데이터
    visual_suggestion: str = ""         # 시각화 제안
    speaker_notes: str = ""             # 발표자 노트

@dataclass
class DeckSpec:
    topic: str                          # 발표 주제
    slides: List[SlideSpec]             # 슬라이드 목록
    max_slides: int = 10                # 최대 슬라이드 수
    template_style: str = "business"    # 템플릿 스타일

@dataclass
class DiagramData:
    type: str                           # flowchart, timeline, etc.
    data: Optional[Dict] = None         # 다이어그램 데이터
    chart: Optional[ChartData] = None   # 차트 데이터

@dataclass
class ChartData:
    type: str                           # column, pie, line
    title: str = ""                     # 차트 제목
    categories: List[str] = field(default_factory=list)  # 범주
    series: List[Dict] = field(default_factory=list)     # 시리즈 데이터
```

### 6.2. API 요청/응답 모델

```python
# Quick PPT 요청
class QuickPresentationBuildRequest(BaseModel):
    session_id: str
    source_message_id: Optional[str] = None
    message: Optional[str] = None
    max_slides: int = 8

# Template PPT 요청
class TemplatedPresentationBuildRequest(BaseModel):
    session_id: str
    source_message_id: str
    template_id: str
    outline: Dict[str, Any]
    slide_management: Optional[List[SlideManagementInfo]] = None
    object_mappings: Optional[List[Dict[str, Any]]] = None

# SSE 이벤트 타입
# { type: 'start' }
# { type: 'structuring' }
# { type: 'outline_ready' }
# { type: 'template_loading' }
# { type: 'building' }
# { type: 'complete', file_url: '...', file_name: '...' }
# { type: 'error', message: '...' }
```

---

## 7. 서비스 라인별 상세 흐름

### 7.1. Quick PPT 서비스 라인 흐름

```
[Frontend]                          [Backend]
    │                                   │
    │  📊 PPT로 만들기 클릭              │
    ├──────────────────────────────────→│
    │  POST /build-quick                │
    │  { session_id, source_message_id }│
    │                                   │
    │←─────────────────────────────────┤ SSE: { type: 'start' }
    │                                   │
    │                                   │ Redis에서 메시지 조회
    │                                   │ context_text 추출
    │                                   │
    │←─────────────────────────────────┤ SSE: { type: 'structuring' }
    │                                   │
    │                                   │ quick_ppt_service.generate_fixed_outline()
    │                                   │   └─ 슬라이드 패턴 파싱
    │                                   │   └─ 시각화 힌트 감지
    │                                   │
    │←─────────────────────────────────┤ SSE: { type: 'building' }
    │                                   │
    │                                   │ quick_ppt_service.build_quick_pptx()
    │                                   │   └─ python-pptx로 PPTX 생성
    │                                   │   └─ 차트/표 자동 삽입
    │                                   │
    │←─────────────────────────────────┤ SSE: { type: 'complete', 
    │                                   │        file_url, file_name }
    │                                   │
    │  다운로드 링크 채팅에 추가         │
    │  📎 quick_presentation_xxx.pptx   │
    ▼                                   ▼
```

### 7.2. Template PPT 서비스 라인 흐름

```
[Frontend]                          [Backend]
    │                                   │
    │  📝 PPT 생성 설정 클릭             │
    │                                   │
    │  PresentationOutlineModal 열림    │
    │  ├─ AI 답변 탭 표시               │
    │  ├─ 템플릿 목록 로드              │
    ├──────────────────────────────────→│ GET /templates
    │←─────────────────────────────────┤ { templates: [...] }
    │                                   │
    │  사용자: 템플릿 선택               │
    ├──────────────────────────────────→│ GET /templates/{id}/simple-metadata
    │←─────────────────────────────────┤ { metadata: {...} }
    │                                   │
    │  매핑 편집 탭에서 조정 (optional)  │
    │                                   │
    │  PPT 생성하기 클릭                 │
    ├──────────────────────────────────→│ POST /build-with-template
    │  { session_id,                    │
    │    source_message_id,             │
    │    template_id,                   │
    │    outline,                       │
    │    slide_management }             │
    │                                   │
    │←─────────────────────────────────┤ SSE: { type: 'start' }
    │←─────────────────────────────────┤ SSE: { type: 'outline_ready' }
    │←─────────────────────────────────┤ SSE: { type: 'template_loading' }
    │←─────────────────────────────────┤ SSE: { type: 'building' }
    │                                   │
    │                                   │ templated_ppt_service
    │                                   │   .build_enhanced_pptx_with_slide_management()
    │                                   │   └─ 템플릿 로드
    │                                   │   └─ 매핑 적용
    │                                   │   └─ 슬라이드 생성
    │                                   │
    │←─────────────────────────────────┤ SSE: { type: 'complete',
    │                                   │        file_url, file_name }
    │                                   │
    │  다운로드 링크 제공                │
    ▼                                   ▼
```

---

## 8. 성능 지표

### 8.1. 측정 결과

| 서비스 라인 | 평균 시간 | 슬라이드 수 | 파일 크기 |
|------------|-----------|-------------|-----------|
| Quick PPT | 0.04초 | 3-12개 | 30-50KB |
| Template PPT | 0.08-0.15초 | 3-15개 | 45-100KB |

### 8.2. 목표 KPI

| 지표 | 목표 |
|------|------|
| 생성 성공률 | 95% 이상 |
| Quick Mode 시간 | 0.5초 이하 |
| Template Mode 시간 | 2초 이하 |
| 재생성 빈도 | 10% 이하 |

---

## 9. 구현 현황

### 9.1. 완료된 작업 ✅

| 항목 | 상태 | 설명 |
|------|------|------|
| Quick PPT 서비스 라인 | ✅ 완료 | `build-quick` API + QuickPPTGeneratorService |
| Template PPT 서비스 라인 | ✅ 완료 | `build-with-template` API + TemplatedPPTGeneratorService |
| 프론트엔드 UI | ✅ 완료 | PresentationActionBar, PresentationOutlineModal |
| SSE 스트리밍 | ✅ 완료 | 실시간 진행 상태 전송 |
| 템플릿 관리 | ✅ 완료 | 업로드, 목록, 메타데이터 조회 |
| 매핑 편집 UI | ✅ 완료 | PPTMappingWithSlideManager |
| 시각화 자동 감지 | ✅ 완료 | 차트, 표, 다이어그램 힌트 |
| 품질 검증 도구 | ✅ 완료 | ppt_quality_validator_tool.py |

### 9.2. 알려진 제한사항 ⚠️

| 항목 | 설명 | 우회 방법 |
|------|------|----------|
| 컨텍스트 길이 제한 | AI 답변이 너무 길면 일부 잘림 | 슬라이드 12개 이하로 요청 |
| 복잡한 다이어그램 | 플로우차트 등 고급 다이어그램 미지원 | 표 형태로 대체 |
| 이미지 삽입 | 외부 이미지 자동 삽입 미지원 | 수동으로 이미지 추가 |

---

## 10. 개선 방안

### 10.1. 단기 개선 (1-2주)

| 항목 | 우선순위 | 설명 |
|------|----------|------|
| **컨텍스트 잘림 경고** | 높음 | AI 답변이 잘렸을 때 사용자에게 명확히 알림 |
| **슬라이드 미리보기** | 높음 | 생성 전 썸네일 미리보기 제공 |
| **템플릿 썸네일** | 중간 | 템플릿 선택 시 시각적 미리보기 |
| **오류 메시지 개선** | 중간 | 사용자 친화적 오류 메시지 |

### 10.2. 중기 개선 (1-2개월)

| 항목 | 우선순위 | 설명 |
|------|----------|------|
| **실시간 편집** | 높음 | 생성된 PPT 온라인 편집 기능 |
| **슬라이드 재생성** | 높음 | 특정 슬라이드만 다시 생성 |
| **스타일 전이** | 중간 | 참조 PPT에서 스타일 복사 적용 |
| **차트 데이터 편집** | 중간 | 생성된 차트 데이터 수정 UI |

### 10.3. 장기 개선 (3개월+)

| 항목 | 우선순위 | 설명 |
|------|----------|------|
| **AI 기반 디자인 추천** | 중간 | 콘텐츠에 맞는 레이아웃 자동 추천 |
| **협업 편집** | 낮음 | 다중 사용자 동시 편집 |
| **버전 관리** | 낮음 | PPT 생성 이력 및 버전 관리 |
| **외부 이미지 검색** | 낮음 | 키워드 기반 스톡 이미지 자동 삽입 |

### 10.4. 아키텍처 개선 제안 (서비스 레벨)

```
현재:
  API → Service (Quick/Templated) → python-pptx

제안:
  API → Orchestrator → Strategy Selection
                     ├─ QuickStrategy → QuickPPTService
                     ├─ TemplateStrategy → TemplatedPPTService
                     ├─ StyleTransferStrategy → StyleAnalysisTool + Builder
                     └─ AIDesignStrategy → AI Design Recommender + Builder
```

---

## 11. 🚀 Agentic AI 전환 로드맵

### 11.1. 왜 Agentic AI가 필요한가?

현재 시스템의 한계:
- 사용자가 직접 버튼을 눌러 서비스를 선택해야 함
- 실패 시 복구 메커니즘 없음
- 콘텐츠 품질 검증 및 자동 수정 불가
- 복잡한 요청에 대한 다단계 추론 불가

Agentic AI의 장점:
- **자율적 의사결정**: "PPT 만들어줘" 한마디로 최적의 전략 선택
- **적응적 실행**: 중간 결과에 따라 계획 조정
- **품질 보장**: 생성 → 검증 → 수정 루프
- **복잡한 요청 처리**: "이전 보고서 스타일로 제품소개서 만들어줘"

### 11.2. 전환 단계별 계획

#### Phase 1: ReAct Agent 기반 전환 (2-3주)

```python
# 목표: LLM이 도구 선택을 직접 수행

class PresentationReActAgent:
    """ReAct 패턴 기반 PPT 생성 에이전트"""
    
    tools = [
        RAGSearchTool(),           # 문서 검색
        OutlinePlanningTool(),     # 아웃라인 생성
        VisualizationTool(),       # 시각화 추천
        TemplateSearchTool(),      # 템플릿 검색
        PPTXBuilderTool(),         # PPT 생성
        QualityValidatorTool(),    # 품질 검증
        SlideRegeneratorTool(),    # 슬라이드 재생성
    ]
    
    async def run(self, user_request: str) -> PPTResult:
        """
        ReAct Loop:
        1. Thought: 현재 상황 분석
        2. Action: 도구 선택 및 실행
        3. Observation: 결과 확인
        4. 반복 또는 종료
        """
        while not done:
            # LLM이 다음 행동 결정
            thought = await self.llm.think(context)
            action = await self.llm.select_action(thought, self.tools)
            observation = await action.execute()
            context.add(thought, action, observation)
            
            if self.is_complete(observation):
                break
        
        return self.finalize(context)
```

**구현 항목**:
- [ ] LangChain/LangGraph 기반 ReAct Agent 구현
- [ ] 기존 도구들을 Agent Tool로 래핑
- [ ] 도구 설명(description) 개선으로 LLM 선택 정확도 향상
- [ ] Agent Chat에서 ReAct Agent 호출 연동

#### Phase 2: Plan-and-Execute 패턴 추가 (2-3주)

```python
# 목표: 복잡한 요청에 대한 계획 수립 후 실행

class PresentationPlanExecuteAgent:
    """Plan-and-Execute 패턴 기반 에이전트"""
    
    async def run(self, user_request: str) -> PPTResult:
        # 1. 계획 수립
        plan = await self.planner.create_plan(user_request)
        # 예: ["1. 문서 검색", "2. 아웃라인 생성", "3. 템플릿 선택", ...]
        
        # 2. 계획 실행 (각 단계마다 결과 평가)
        for step in plan.steps:
            result = await self.executor.execute_step(step)
            
            # 3. 필요시 계획 수정
            if not result.success:
                plan = await self.planner.replan(plan, result)
        
        return self.finalize()
```

**구현 항목**:
- [ ] Planner 모듈 구현 (계획 수립 LLM 호출)
- [ ] Executor 모듈 구현 (단계별 실행)
- [ ] Replanner 로직 (실패 시 대안 계획)
- [ ] 실행 상태 추적 및 사용자 피드백

#### Phase 3: 피드백 루프 및 품질 보장 (2-3주)

```python
# 목표: 생성 결과 검증 및 자동 수정

class QualityAssuranceLoop:
    """품질 검증 → 수정 반복 루프"""
    
    async def ensure_quality(self, pptx_path: str) -> PPTResult:
        max_iterations = 3
        
        for i in range(max_iterations):
            # 1. 품질 검증
            issues = await self.validator.validate(pptx_path)
            
            if not issues:
                return PPTResult(success=True, path=pptx_path)
            
            # 2. 문제 해결 계획
            fixes = await self.llm.plan_fixes(issues)
            
            # 3. 수정 실행
            for fix in fixes:
                pptx_path = await self.fixer.apply_fix(pptx_path, fix)
        
        return PPTResult(success=True, path=pptx_path, warnings=issues)
```

**구현 항목**:
- [ ] 품질 검증기 강화 (텍스트 오버플로우, 레이아웃 문제 등)
- [ ] 자동 수정 도구 구현 (텍스트 축약, 폰트 조정)
- [ ] 반복 루프 및 최대 시도 횟수 제한
- [ ] 사용자에게 품질 이슈 리포트 제공

### 11.3. 기술 스택 선택

| 옵션 | 장점 | 단점 | 추천 |
|------|------|------|------|
| **LangChain Agent** | 기존 도구 재사용, 빠른 구현 | 복잡한 흐름 제어 어려움 | Phase 1 |
| **LangGraph** | 상태 기계 기반 정교한 제어 | 학습 곡선 | Phase 2-3 |
| **자체 구현** | 완전한 제어권 | 개발 시간 많음 | 필요시 |

### 11.4. 예상 사용자 경험 변화

**현재**:
```
사용자: "인슐린펌프 PPT 만들어줘"
시스템: [AI 답변 생성]
사용자: [📊 PPT로 만들기 버튼 클릭]  ← 수동 개입 필요
시스템: [PPT 생성]
결과: PPT 파일 (품질 미검증)
```

**Agentic AI 전환 후**:
```
사용자: "인슐린펌프 PPT 만들어줘"
시스템: 
  💭 "제품 정보를 먼저 검색해야겠다"
  🔧 [RAG 검색 실행]
  💭 "12개 섹션이 적당하겠다"
  🔧 [아웃라인 생성]
  💭 "기술사양은 표로 표현하자"
  🔧 [시각화 적용]
  💭 "사용자 템플릿이 있나 확인"
  🔧 [템플릿 검색 → 발견!]
  💭 "PPT 생성 시작"
  🔧 [PPTX 빌드]
  💭 "품질 검증해보자"
  🔧 [검증 → 슬라이드 3 오버플로우]
  💭 "텍스트를 줄여야겠다"
  🔧 [자동 수정]
  ✅ "12슬라이드 PPT가 완성되었습니다!"
결과: 품질 검증된 PPT 파일 (사용자 개입 없음)
```

### 11.5. 마일스톤 및 일정

| 마일스톤 | 기간 | 목표 |
|----------|------|------|
| **M1: ReAct Agent MVP** | Week 1-2 | LLM이 도구 선택, 기본 흐름 작동 |
| **M2: Plan-Execute** | Week 3-4 | 계획 수립 및 실행, 재계획 기능 |
| **M3: Quality Loop** | Week 5-6 | 품질 검증 및 자동 수정 |
| **M4: Integration** | Week 7-8 | UI 연동, 사용자 피드백, 최적화 |

---

## 12. 관련 문서

- [PRESENTATION_API_MIGRATION_GUIDE.md](/home/admin/Dev/abekm/PRESENTATION_API_MIGRATION_GUIDE.md)
- [AGENT_TOOL_ARCHITECTURE_REPORT.md](/home/admin/Dev/abekm/AGENT_TOOL_ARCHITECTURE_REPORT.md)
- [PPT_AGENT_DEPENDENCY_ANALYSIS.md](/home/admin/Dev/abekm/PPT_AGENT_DEPENDENCY_ANALYSIS.md)
- [PPT_QUALITY_VALIDATION_REPORT.md](/home/admin/Dev/abekm/PPT_QUALITY_VALIDATION_REPORT.md)

---

## 부록 A: 코드 참조 위치

### A.1. 프론트엔드 핵심 파일

| 파일 | 역할 |
|------|------|
| `pages/user/AgentChatPage.tsx` | PPT 생성 이벤트 리스너 |
| `chat/components/presentation/PresentationActionBar.tsx` | 📊, 📝 버튼 |
| `chat/components/presentation/PresentationOutlineModal.tsx` | PPT 생성 설정 모달 |
| `chat/components/presentation/usePresentation.ts` | API 호출 훅 |
| `chat/components/MessageBubble.tsx` | PPT 의도 감지 및 버튼 표시 |

### A.2. 백엔드 핵심 파일

| 파일 | 역할 |
|------|------|
| `api/v1/presentation.py` | API 엔드포인트 (build-quick, build-with-template) |
| `services/presentation/quick_ppt_generator_service.py` | Quick PPT 생성 서비스 |
| `services/presentation/templated_ppt_generator_service.py` | Template PPT 생성 서비스 |
| `services/presentation/ppt_template_manager.py` | 템플릿 관리 |
| `services/presentation/ppt_models.py` | 데이터 모델 (DeckSpec, SlideSpec) |
| `tools/presentation/ppt_quality_validator_tool.py` | 품질 검증 도구 |

### A.3. 현재 "에이전트" 코드 (실제로는 파이프라인)

| 파일 | 설명 | Agentic 여부 |
|------|------|-------------|
| `agents/presentation/orchestrator.py` | 고정 파이프라인 실행 | ❌ Non-Agentic |
| `agents/presentation/presentation_agent.py` | LangChain Tool 래퍼 | ❌ 단순 래퍼 |
| `api/v1/agent.py` (line 624-627) | 의도 감지 후 직접 호출 | ❌ if-else 분기 |

---

## 부록 B: Agentic AI 참고 자료

### B.1. ReAct 패턴 참고

```
Thought → Action → Observation → Thought → Action → ...
```

- [ReAct: Synergizing Reasoning and Acting in Language Models](https://arxiv.org/abs/2210.03629)
- [LangChain Agent Documentation](https://python.langchain.com/docs/modules/agents/)

### B.2. Plan-and-Execute 패턴 참고

```
Plan: [Step1, Step2, Step3, ...]
Execute: Step1 → Result → (Replan if needed) → Step2 → ...
```

- [LangGraph Plan-and-Execute](https://langchain-ai.github.io/langgraph/tutorials/plan-and-execute/plan-and-execute/)
- [Plan-and-Solve Prompting](https://arxiv.org/abs/2305.04091)

### B.3. 품질 보장 루프 참고

```
Generate → Validate → Fix → Validate → ... → Accept
```

- [Self-Refine: Iterative Refinement with Self-Feedback](https://arxiv.org/abs/2303.17651)
- [Constitutional AI](https://arxiv.org/abs/2212.08073)
