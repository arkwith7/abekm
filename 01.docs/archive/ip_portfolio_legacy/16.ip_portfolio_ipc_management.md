# IP 포트폴리오 IPC 권한 관리 시스템

> **작성일**: 2026-01-04  
> **버전**: 1.0  
> **시스템**: IPBridge Knowledge Management System  
> **업종**: 반도체 장비 제조

---

## 📋 문서 개요

### 목적

본 문서는 **IPC(International Patent Classification) 기반 IP 포트폴리오 관리 시스템**의 설계 및 구현 가이드입니다.

**주요 기능**:
- ✅ IPC 코드 기반 특허 분류 및 권한 관리
- ✅ 사용자별 IPC 코드 접근 권한 세밀 제어
- ✅ 반도체 장비 업종 특화 IPC 코드 체계
- ✅ 관리자 UI를 통한 권한 할당/조회/수정/삭제

### 적용 범위

- IPC 권한 관리 Admin API (`/api/v1/admin/ipc-permissions`)
- IPC 권한 관리 프론트엔드 페이지 (`/admin/ipc-permissions`)
- IPC 코드 마스터 데이터 관리
- 반도체 장비 업종 IPC 코드 초기 데이터
- 권한 기반 특허 조회 필터링

---

## 1. 시스템 아키텍처

### 1.1 백엔드 구조

#### 1.1.1 데이터베이스 모델

**tb_ipc_permissions** (IPC 권한 테이블)
```python
class TbIpcPermissions(Base):
    __tablename__ = "tb_ipc_permissions"
    
    permission_id: Mapped[int] = mapped_column(primary_key=True)
    user_emp_no: Mapped[str] = mapped_column(String(20), index=True)
    ipc_code: Mapped[str] = mapped_column(String(50), index=True)
    role_id: Mapped[Optional[str]] = mapped_column(String(50))
    is_active: Mapped[bool] = mapped_column(default=True)
    created_at: Mapped[datetime]
    created_by: Mapped[Optional[str]]
    updated_at: Mapped[Optional[datetime]]
    updated_by: Mapped[Optional[str]]
```

**tb_ipc_code** (IPC 코드 마스터)
```python
class TbIpcCode(Base):
    __tablename__ = "tb_ipc_code"
    
    ipc_code: Mapped[str] = mapped_column(String(50), primary_key=True)
    ipc_section: Mapped[Optional[str]]
    ipc_class: Mapped[Optional[str]]
    ipc_subclass: Mapped[Optional[str]]
    ipc_group: Mapped[Optional[str]]
    ipc_subgroup: Mapped[Optional[str]]
    korean_name: Mapped[Optional[str]]
    english_name: Mapped[Optional[str]]
    is_active: Mapped[bool] = mapped_column(default=True)
```

#### 1.1.2 API 엔드포인트

| 메서드 | 경로 | 설명 |
|--------|------|------|
| GET | `/api/v1/admin/ipc-permissions` | IPC 권한 목록 조회 (페이징, 필터링) |
| POST | `/api/v1/admin/ipc-permissions` | 신규 IPC 권한 생성 |
| PUT | `/api/v1/admin/ipc-permissions/{permission_id}` | IPC 권한 수정 |
| DELETE | `/api/v1/admin/ipc-permissions/{permission_id}` | IPC 권한 삭제 |
| GET | `/api/v1/admin/ipc-permissions/users` | 권한 할당 가능 사용자 목록 |
| GET | `/api/v1/admin/ipc-permissions/ipc-codes` | IPC 코드 목록 (자동완성) |
| GET | `/api/v1/admin/ipc-permissions/roles` | 역할 목록 |

**파일 위치**: `/home/arkwith/Dev/abekm/backend/app/api/v1/admin_ipc_permissions.py`

#### 1.1.3 권한 검증 서비스

**IpcPermissionService**
```python
class IpcPermissionService:
    """IPC 권한 검증 및 관리 서비스"""
    
    async def has_ipc_permission(
        self, 
        user_emp_no: str, 
        ipc_code: str, 
        required_role: str = "VIEWER"
    ) -> bool:
        """사용자의 IPC 코드 접근 권한 확인"""
        
    async def get_user_ipc_permissions(
        self, 
        user_emp_no: str
    ) -> List[str]:
        """사용자가 접근 가능한 IPC 코드 목록 조회"""
```

**파일 위치**: `/home/arkwith/Dev/abekm/backend/app/services/auth/ipc_permission_service.py`

### 1.2 프론트엔드 구조

#### 1.2.1 IPC 권한 관리 페이지

**컴포넌트**: `IpcPermissionManagement.tsx`  
**경로**: `/admin/ipc-permissions`  
**파일 위치**: `/home/arkwith/Dev/abekm/frontend/src/pages/admin/IpcPermissionManagement.tsx`

**주요 기능**:
- ✅ IPC 권한 목록 테이블 (페이징, 정렬, 필터링)
- ✅ 필터: 사번, IPC 코드, 역할, 활성 상태
- ✅ 권한 생성 모달 (사용자 선택, IPC 코드 입력, 역할 선택)
- ✅ 권한 수정 모달
- ✅ 권한 삭제 (확인 다이얼로그)
- ✅ 실시간 알림 (성공/에러 메시지)
- ✅ 역할별 색상 코딩 (ADMIN: 파란색, EDITOR: 녹색, VIEWER: 회색)

#### 1.2.2 관리자 메뉴 구조

**AdminLayout.tsx** 메뉴 항목:
```typescript
// 👥 사용자 & 권한 관리
{ 
  name: '사용자 관리', 
  path: '/admin/users', 
  icon: Users, 
  id: 'users' 
},
{ 
  name: 'IPC 권한 관리', 
  path: '/admin/ipc-permissions', 
  icon: Key, 
  id: 'ipc-permissions' 
}
```

---

## 2. 반도체 장비 업종 IPC 코드 체계

### 2.1 핵심 IPC 섹션

반도체 장비 제조업은 주로 다음 IPC 섹션에 특허를 보유합니다:

| IPC 섹션 | 명칭 | 반도체 장비 관련 분야 |
|----------|------|----------------------|
| **H** | 전기 | 반도체 제조 장비 (에칭, 증착, 검사) |
| **G** | 물리학 | 광학 검사, 측정, 센서 장비 |
| **B** | 처리 조작 | 웨이퍼 핸들링, 클리닝, CMP |
| **C** | 화학, 야금 | CVD, ALD, 화학 기계적 연마 |

### 2.2 상세 IPC 코드 매핑

#### 2.2.1 H 섹션 (전기) - 반도체 제조 핵심

| IPC 코드 | 한글명 | 영문명 | 설명 |
|----------|--------|--------|------|
| **H01L** | 반도체 소자, 전기적 고체 소자 | Semiconductor devices; Electric solid-state devices | 반도체 칩, 패키징, 웨이퍼 제조 |
| **H01L 21/00** | 반도체 또는 고체상태 소자 제조 공정 | Processes for manufacture of semiconductors | 포토리소그래피, 에칭, 증착 |
| **H01L 21/02** | 반도체 재료의 제조 또는 처리 | Manufacture or treatment of semiconductor materials | 웨이퍼 제조, 도핑, 열처리 |
| **H01L 21/67** | 반도체 장치 제조 장비 | Apparatus for semiconductor device manufacture | 반도체 제조 장비 일반 |
| **H01L 21/677** | 반도체 웨이퍼 이동 장치 | Apparatus for conveying wafers | 웨이퍼 핸들러, 로봇 |
| **H01L 21/683** | 반도체 웨이퍼 처리 챔버 | Apparatus for processing wafers | 프로세스 챔버, 진공 시스템 |

#### 2.2.2 G 섹션 (물리학) - 검사 및 측정

| IPC 코드 | 한글명 | 영문명 | 설명 |
|----------|--------|--------|------|
| **G01N** | 재료의 화학적/물리적 성질 조사 | Investigating chemical or physical properties | 웨이퍼 검사, 결함 검출 |
| **G01N 21/95** | 반도체 웨이퍼 광학 검사 | Optical inspection of semiconductor wafers | 결함 검사, 패턴 검증 |
| **G01B** | 길이, 두께, 각도 측정 | Measuring length, thickness, or angles | CD(Critical Dimension) 측정 |
| **G02F** | 광학 소자 | Optical devices | 광학 검사 장비 광학계 |

#### 2.2.3 B 섹션 (처리 조작) - 웨이퍼 핸들링

| IPC 코드 | 한글명 | 영문명 | 설명 |
|----------|--------|--------|------|
| **B24B** | 연마, 연삭 | Grinding, polishing | CMP(화학적 기계적 연마) |
| **B24B 37/04** | CMP 장치 | Chemical-mechanical polishing machines | CMP 장비 |
| **B08B** | 세정 일반 | Cleaning in general | 웨이퍼 세정 장비 |
| **B65G** | 운반 장치 | Transport or storage devices | FOUP, EFEM |

#### 2.2.4 C 섹션 (화학, 야금) - 증착 및 코팅

| IPC 코드 | 한글명 | 영문명 | 설명 |
|----------|--------|--------|------|
| **C23C** | 금속 재료의 코팅 | Coating metallic material | PVD, CVD, ALD |
| **C23C 16/00** | 화학 증착 | Chemical vapor deposition (CVD) | CVD 장비 |
| **C23C 14/00** | 물리 증착 | Physical vapor deposition (PVD) | 스퍼터링, 증착 |

### 2.3 반도체 장비 업종 IPC 트리 구조

```
반도체 장비 제조 IP 포트폴리오
│
├── H (전기) - 71%
│   ├── H01L (반도체 소자) - 45%
│   │   ├── H01L 21/00 (제조 공정) - 30%
│   │   ├── H01L 21/67 (제조 장비) - 10%
│   │   └── H01L 21/683 (처리 챔버) - 5%
│   │
│   ├── H05H (플라즈마 기술) - 15%
│   │   └── H05H 1/46 (플라즈마 발생) - 15%
│   │
│   └── H01J (전자관) - 11%
│       └── H01J 37/32 (플라즈마 에칭) - 11%
│
├── G (물리학) - 18%
│   ├── G01N (검사) - 10%
│   │   └── G01N 21/95 (광학 검사) - 10%
│   │
│   └── G01B (측정) - 8%
│
├── B (처리 조작) - 7%
│   ├── B24B (연마) - 4%
│   └── B08B (세정) - 3%
│
└── C (화학, 야금) - 4%
    └── C23C (코팅) - 4%
        ├── C23C 16/00 (CVD) - 2%
        └── C23C 14/00 (PVD) - 2%
```

---

## 3. 초기 데이터 적재 방안

### 3.1 IPC 코드 마스터 데이터

#### 3.1.1 CSV 파일 기반 적재

**파일 경로**: `/home/arkwith/Dev/abekm/backend/data/ipc_codes_semiconductor.csv`

```csv
ipc_code,ipc_section,ipc_class,ipc_subclass,ipc_group,ipc_subgroup,korean_name,english_name,is_active
H,H,,,,,"전기","Electricity",true
H01L,H,01,L,,,"반도체 소자, 전기적 고체 소자","Semiconductor devices; Electric solid-state devices",true
H01L21/00,H,01,L,21,00,"반도체 또는 고체상태 소자 제조 공정","Processes for manufacture of semiconductors",true
H01L21/02,H,01,L,21,02,"반도체 재료의 제조 또는 처리","Manufacture or treatment of semiconductor materials",true
H01L21/67,H,01,L,21,67,"반도체 장치 제조 장비","Apparatus for semiconductor device manufacture",true
H01L21/677,H,01,L,21,677,"반도체 웨이퍼 이동 장치","Apparatus for conveying wafers",true
H01L21/683,H,01,L,21,683,"반도체 웨이퍼 처리 챔버","Apparatus for processing wafers",true
H05H,H,05,H,,,"플라즈마 기술","Plasma technique",true
H05H1/46,H,05,H,1,46,"플라즈마 발생","Generating plasma",true
H01J,H,01,J,,,"전자관 또는 방전관","Electric discharge tubes or discharge lamps",true
H01J37/32,H,01,J,37,32,"플라즈마 에칭","Plasma etching",true
G,G,,,,,"물리학","Physics",true
G01N,G,01,N,,,"재료의 화학적 또는 물리적 성질 조사","Investigating chemical or physical properties",true
G01N21/95,G,01,N,21,95,"반도체 웨이퍼 광학 검사","Optical inspection of semiconductor wafers",true
G01B,G,01,B,,,"길이, 두께, 각도 측정","Measuring length, thickness, or angles",true
B,B,,,,,"처리 조작; 운반","Performing operations; Transporting",true
B24B,B,24,B,,,"연마, 연삭","Grinding, polishing",true
B24B37/04,B,24,B,37,04,"CMP 장치","Chemical-mechanical polishing machines",true
B08B,B,08,B,,,"세정 일반","Cleaning in general",true
C,C,,,,,"화학; 야금","Chemistry; Metallurgy",true
C23C,C,23,C,,,"금속 재료의 코팅","Coating metallic material",true
C23C16/00,C,23,C,16,00,"화학 증착 (CVD)","Chemical vapor deposition (CVD)",true
C23C14/00,C,23,C,14,00,"물리 증착 (PVD)","Physical vapor deposition (PVD)",true
```

#### 3.1.2 적재 스크립트

**파일 경로**: `/home/arkwith/Dev/abekm/backend/scripts/load_ipc_codes.py`

```python
"""
IPC 코드 마스터 데이터 적재 스크립트
반도체 장비 업종 특화 IPC 코드 23개 적재
"""
import asyncio
import csv
from pathlib import Path
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_async_session
from app.models.patent.ipc_models import TbIpcCode

async def load_ipc_codes():
    """CSV 파일에서 IPC 코드 마스터 데이터 로드"""
    csv_path = Path(__file__).parent.parent / "data" / "ipc_codes_semiconductor.csv"
    
    async with get_async_session() as session:
        with open(csv_path, 'r', encoding='utf-8') as f:
            reader = csv.DictReader(f)
            count = 0
            
            for row in reader:
                ipc_code = TbIpcCode(
                    ipc_code=row['ipc_code'],
                    ipc_section=row['ipc_section'] or None,
                    ipc_class=row['ipc_class'] or None,
                    ipc_subclass=row['ipc_subclass'] or None,
                    ipc_group=row['ipc_group'] or None,
                    ipc_subgroup=row['ipc_subgroup'] or None,
                    korean_name=row['korean_name'],
                    english_name=row['english_name'],
                    is_active=row['is_active'].lower() == 'true'
                )
                session.add(ipc_code)
                count += 1
            
            await session.commit()
            print(f"✅ {count}개 IPC 코드 적재 완료")

if __name__ == "__main__":
    asyncio.run(load_ipc_codes())
```

**실행 방법**:
```bash
cd /home/arkwith/Dev/abekm/backend
python scripts/load_ipc_codes.py
```

### 3.2 초기 IPC 권한 할당

#### 3.2.1 조직별 IPC 권한 매핑

**파일 경로**: `/home/arkwith/Dev/abekm/backend/data/ipc_permissions_initial.csv`

```csv
user_emp_no,ipc_code,role_id,created_by
admin001,H,ADMIN,SYSTEM
admin001,G,ADMIN,SYSTEM
admin001,B,ADMIN,SYSTEM
admin001,C,ADMIN,SYSTEM
tech001,H01L,EDITOR,SYSTEM
tech001,H05H,EDITOR,SYSTEM
tech002,G01N,EDITOR,SYSTEM
tech002,G01B,EDITOR,SYSTEM
tech003,B24B,EDITOR,SYSTEM
tech003,C23C,EDITOR,SYSTEM
user001,H01L21/00,VIEWER,SYSTEM
user002,G01N21/95,VIEWER,SYSTEM
```

#### 3.2.2 적재 스크립트

**파일 경로**: `/home/arkwith/Dev/abekm/backend/scripts/load_ipc_permissions.py`

```python
"""
IPC 권한 초기 데이터 적재 스크립트
조직별 IPC 코드 권한 할당
"""
import asyncio
import csv
from datetime import datetime
from pathlib import Path
from sqlalchemy.ext.asyncio import AsyncSession
from app.core.database import get_async_session
from app.models.patent.ipc_models import TbIpcPermissions

async def load_ipc_permissions():
    """CSV 파일에서 IPC 권한 초기 데이터 로드"""
    csv_path = Path(__file__).parent.parent / "data" / "ipc_permissions_initial.csv"
    
    async with get_async_session() as session:
        with open(csv_path, 'r', encoding='utf-8') as f:
            reader = csv.DictReader(f)
            count = 0
            
            for row in reader:
                permission = TbIpcPermissions(
                    user_emp_no=row['user_emp_no'],
                    ipc_code=row['ipc_code'],
                    role_id=row['role_id'],
                    is_active=True,
                    created_at=datetime.now(),
                    created_by=row['created_by']
                )
                session.add(permission)
                count += 1
            
            await session.commit()
            print(f"✅ {count}개 IPC 권한 할당 완료")

if __name__ == "__main__":
    asyncio.run(load_ipc_permissions())
```

**실행 방법**:
```bash
cd /home/arkwith/Dev/abekm/backend
python scripts/load_ipc_permissions.py
```

### 3.3 데이터베이스 마이그레이션

#### 3.3.1 Alembic 마이그레이션 생성

```bash
cd /home/arkwith/Dev/abekm/backend
alembic revision -m "Add IPC permission tables"
```

**마이그레이션 파일**: `alembic/versions/xxxx_add_ipc_permission_tables.py`

```python
"""Add IPC permission tables

Revision ID: xxxx
Revises: yyyy
Create Date: 2026-01-04
"""
from alembic import op
import sqlalchemy as sa

def upgrade():
    # tb_ipc_code 테이블 생성
    op.create_table(
        'tb_ipc_code',
        sa.Column('ipc_code', sa.String(50), primary_key=True),
        sa.Column('ipc_section', sa.String(10), nullable=True),
        sa.Column('ipc_class', sa.String(10), nullable=True),
        sa.Column('ipc_subclass', sa.String(10), nullable=True),
        sa.Column('ipc_group', sa.String(20), nullable=True),
        sa.Column('ipc_subgroup', sa.String(20), nullable=True),
        sa.Column('korean_name', sa.String(500), nullable=True),
        sa.Column('english_name', sa.String(500), nullable=True),
        sa.Column('is_active', sa.Boolean, default=True)
    )
    
    # tb_ipc_permissions 테이블 생성
    op.create_table(
        'tb_ipc_permissions',
        sa.Column('permission_id', sa.Integer, primary_key=True, autoincrement=True),
        sa.Column('user_emp_no', sa.String(20), nullable=False),
        sa.Column('ipc_code', sa.String(50), nullable=False),
        sa.Column('role_id', sa.String(50), nullable=True),
        sa.Column('is_active', sa.Boolean, default=True),
        sa.Column('created_at', sa.DateTime, nullable=False),
        sa.Column('created_by', sa.String(50), nullable=True),
        sa.Column('updated_at', sa.DateTime, nullable=True),
        sa.Column('updated_by', sa.String(50), nullable=True)
    )
    
    # 인덱스 생성
    op.create_index('idx_ipc_permissions_emp_no', 'tb_ipc_permissions', ['user_emp_no'])
    op.create_index('idx_ipc_permissions_code', 'tb_ipc_permissions', ['ipc_code'])

def downgrade():
    op.drop_table('tb_ipc_permissions')
    op.drop_table('tb_ipc_code')
```

#### 3.3.2 마이그레이션 실행

```bash
cd /home/arkwith/Dev/abekm/backend
alembic upgrade head
```

---

## 4. IP 포트폴리오 메뉴 구성 제안

### 4.1 메뉴 구조 (반도체 장비 업종 특화)

```
📊 IP 포트폴리오
│
├── 🗂️ 기술 분야별 특허
│   ├── H (전기) - 반도체 제조 장비
│   │   ├── H01L (반도체 소자)
│   │   ├── H05H (플라즈마)
│   │   └── H01J (전자관)
│   │
│   ├── G (물리학) - 검사/측정
│   │   ├── G01N (검사)
│   │   └── G01B (측정)
│   │
│   ├── B (처리 조작) - 핸들링
│   │   ├── B24B (CMP)
│   │   └── B08B (세정)
│   │
│   └── C (화학) - 증착/코팅
│       └── C23C (CVD/PVD)
│
├── 📈 IP 대시보드
│   ├── 기술 분야별 특허 현황
│   ├── 출원 트렌드 (연도별)
│   ├── 핵심 IPC 분포
│   └── 발명자 통계
│
├── 🔍 특허 검색
│   ├── IPC 코드 검색
│   ├── 키워드 검색 (AI 시맨틱)
│   ├── 발명자 검색
│   └── 출원일/등록일 범위
│
├── 📝 특허 관리
│   ├── 신규 특허 등록
│   ├── 특허 상태 업데이트
│   └── 특허 파일 업로드
│
├── 🎯 전략 분석 (AI Agent)
│   ├── 기술 백공간 분석
│   ├── 경쟁사 특허 분석
│   ├── 인용 관계 분석
│   └── 특허 가치 평가
│
└── ⚙️ 관리자
    ├── IPC 권한 관리 ✅ (구현 완료)
    ├── IPC 코드 관리
    └── 특허 메타데이터 관리
```

### 4.2 프론트엔드 라우팅 구조

**파일 경로**: `/home/arkwith/Dev/abekm/frontend/src/App.tsx`

```typescript
// IP 포트폴리오 라우트 (신규)
<Route path="/ip-portfolio" element={<IpPortfolioLayout />}>
  {/* 기술 분야별 특허 */}
  <Route path="tech/:ipcSection" element={<TechPatentList />} />
  <Route path="tech/:ipcSection/:ipcCode" element={<PatentDetail />} />
  
  {/* IP 대시보드 */}
  <Route path="dashboard" element={<IpDashboard />} />
  
  {/* 특허 검색 */}
  <Route path="search" element={<PatentSearch />} />
  
  {/* 특허 관리 */}
  <Route path="manage" element={<PatentManagement />} />
  
  {/* 전략 분석 (AI Agent) */}
  <Route path="analysis" element={<StrategicAnalysis />} />
</Route>
```

### 4.3 메인 네비게이션 메뉴

**파일 경로**: `/home/arkwith/Dev/abekm/frontend/src/layouts/MainLayout.tsx`

```typescript
const mainMenuItems = [
  // ... 기존 메뉴 (지식 검색, AI Agent 등)
  
  // 신규: IP 포트폴리오 메뉴
  {
    name: 'IP 포트폴리오',
    path: '/ip-portfolio/dashboard',
    icon: Shield,  // lucide-react 아이콘
    id: 'ip-portfolio',
    badge: 'NEW',
    children: [
      {
        name: '기술 분야별 특허',
        path: '/ip-portfolio/tech/H',
        icon: FolderTree,
        id: 'tech-patents'
      },
      {
        name: 'IP 대시보드',
        path: '/ip-portfolio/dashboard',
        icon: BarChart3,
        id: 'ip-dashboard'
      },
      {
        name: '특허 검색',
        path: '/ip-portfolio/search',
        icon: Search,
        id: 'patent-search'
      },
      {
        name: '전략 분석',
        path: '/ip-portfolio/analysis',
        icon: TrendingUp,
        id: 'strategic-analysis'
      }
    ]
  }
];
```

### 4.4 IPC 트리 네비게이션 컴포넌트 (제안)

**파일 경로**: `/home/arkwith/Dev/abekm/frontend/src/components/ip/IpcTreeNavigation.tsx`

```typescript
interface IpcTreeNode {
  code: string;
  name: string;
  level: 'section' | 'class' | 'subclass' | 'group' | 'subgroup';
  children?: IpcTreeNode[];
  patentCount: number;
}

export const IpcTreeNavigation: React.FC = () => {
  const [expandedNodes, setExpandedNodes] = useState<string[]>(['H']);
  const [selectedNode, setSelectedNode] = useState<string | null>(null);
  
  // 반도체 장비 IPC 트리 데이터
  const ipcTree: IpcTreeNode[] = [
    {
      code: 'H',
      name: '전기',
      level: 'section',
      patentCount: 850,
      children: [
        {
          code: 'H01L',
          name: '반도체 소자',
          level: 'class',
          patentCount: 540,
          children: [
            {
              code: 'H01L21/00',
              name: '반도체 제조 공정',
              level: 'group',
              patentCount: 360
            },
            {
              code: 'H01L21/67',
              name: '제조 장비',
              level: 'group',
              patentCount: 120
            }
          ]
        },
        {
          code: 'H05H',
          name: '플라즈마 기술',
          level: 'class',
          patentCount: 180
        }
      ]
    },
    // ... G, B, C 섹션
  ];
  
  return (
    <div className="ipc-tree-navigation">
      {/* 재귀적 트리 렌더링 */}
      {ipcTree.map(node => (
        <IpcTreeNode 
          key={node.code} 
          node={node} 
          expanded={expandedNodes.includes(node.code)}
          selected={selectedNode === node.code}
          onToggle={(code) => toggleNode(code)}
          onSelect={(code) => setSelectedNode(code)}
        />
      ))}
    </div>
  );
};
```

---

## 5. 권한 기반 특허 조회 필터링

### 5.1 백엔드 권한 필터링

**파일 경로**: `/home/arkwith/Dev/abekm/backend/app/api/v1/ip_portfolio.py`

```python
@router.get("/patents", summary="IPC 권한 기반 특허 조회")
async def get_filtered_patents(
    ipc_code: Optional[str] = None,
    current_user: User = Depends(get_current_user),
    db: AsyncSession = Depends(get_db)
):
    """
    사용자가 접근 가능한 IPC 코드의 특허만 조회
    - 시스템 관리자: 모든 특허 조회 가능
    - 일반 사용자: IPC 권한이 있는 특허만 조회
    """
    # 1. 사용자 권한 확인
    if not is_admin(current_user):
        # 사용자의 IPC 권한 조회
        allowed_ipc_codes = await ipc_permission_service.get_user_ipc_permissions(
            user_emp_no=current_user.emp_no,
            db=db
        )
        
        if not allowed_ipc_codes:
            return {"patents": [], "total_count": 0}
        
        # IPC 권한 필터링
        query = select(TbPatentMetadata).where(
            TbPatentMetadata.main_ipc_code.in_(allowed_ipc_codes)
        )
    else:
        # 관리자는 모든 특허 조회
        query = select(TbPatentMetadata)
    
    # 2. 추가 필터 적용
    if ipc_code:
        query = query.where(TbPatentMetadata.main_ipc_code == ipc_code)
    
    # 3. 실행 및 반환
    result = await db.execute(query)
    patents = result.scalars().all()
    
    return {
        "patents": [patent.to_dict() for patent in patents],
        "total_count": len(patents),
        "user_ipc_permissions": allowed_ipc_codes if not is_admin(current_user) else "ALL"
    }
```

### 5.2 프론트엔드 IPC 필터 컴포넌트

```typescript
export const IpcFilter: React.FC = () => {
  const [userIpcCodes, setUserIpcCodes] = useState<string[]>([]);
  const [selectedIpc, setSelectedIpc] = useState<string | null>(null);
  
  useEffect(() => {
    // 사용자 IPC 권한 조회
    adminService.getMyIpcPermissions().then(codes => {
      setUserIpcCodes(codes);
    });
  }, []);
  
  return (
    <div className="ipc-filter">
      <label>접근 가능한 기술 분야</label>
      <select 
        value={selectedIpc || ''} 
        onChange={(e) => setSelectedIpc(e.target.value)}
      >
        <option value="">전체</option>
        {userIpcCodes.map(code => (
          <option key={code} value={code}>
            {code} - {getIpcName(code)}
          </option>
        ))}
      </select>
    </div>
  );
};
```

---

## 6. 검증 및 테스트

### 6.1 초기 데이터 검증

**검증 스크립트**: `/home/arkwith/Dev/abekm/backend/scripts/verify_ipc_data.py`

```python
"""IPC 데이터 검증 스크립트"""
import asyncio
from sqlalchemy import select, func
from app.core.database import get_async_session
from app.models.patent.ipc_models import TbIpcCode, TbIpcPermissions

async def verify_ipc_data():
    async with get_async_session() as session:
        # 1. IPC 코드 수 확인
        ipc_count = await session.execute(
            select(func.count()).select_from(TbIpcCode)
        )
        print(f"✅ IPC 코드: {ipc_count.scalar()} 개")
        
        # 2. IPC 권한 수 확인
        perm_count = await session.execute(
            select(func.count()).select_from(TbIpcPermissions)
        )
        print(f"✅ IPC 권한: {perm_count.scalar()} 개")
        
        # 3. 섹션별 IPC 코드 분포
        sections = await session.execute(
            select(
                TbIpcCode.ipc_section,
                func.count().label('count')
            ).group_by(TbIpcCode.ipc_section)
        )
        print("\n섹션별 IPC 코드:")
        for section, count in sections:
            print(f"  {section}: {count}개")
        
        # 4. 역할별 권한 분포
        roles = await session.execute(
            select(
                TbIpcPermissions.role_id,
                func.count().label('count')
            ).group_by(TbIpcPermissions.role_id)
        )
        print("\n역할별 권한:")
        for role, count in roles:
            print(f"  {role}: {count}개")

if __name__ == "__main__":
    asyncio.run(verify_ipc_data())
```

### 6.2 프론트엔드 테스트 시나리오

#### 테스트 계정 및 권한

| 사번 | 역할 | IPC 권한 | 테스트 목적 |
|------|------|----------|------------|
| admin001 | ADMIN | H, G, B, C (전체) | 전체 특허 조회 |
| tech001 | EDITOR | H01L, H05H | 반도체 제조 장비 편집 |
| tech002 | EDITOR | G01N, G01B | 검사/측정 장비 편집 |
| user001 | VIEWER | H01L21/00 | 제조 공정 특허 조회 |

#### 테스트 케이스

1. **IPC 권한 생성**
   - 관리자 로그인 → IPC 권한 관리 → 신규 생성
   - 사용자: user003, IPC: B24B37/04, 역할: VIEWER
   - 예상 결과: 권한 생성 성공, 알림 표시

2. **IPC 권한 수정**
   - 기존 권한 선택 → 역할 변경 (VIEWER → EDITOR)
   - 예상 결과: 권한 업데이트 성공

3. **IPC 권한 삭제**
   - 기존 권한 선택 → 삭제 버튼 → 확인
   - 예상 결과: 권한 삭제 성공, 목록에서 제거

4. **필터링 테스트**
   - 사번 필터: user001
   - IPC 코드 필터: H01L
   - 역할 필터: EDITOR
   - 예상 결과: 조건에 맞는 권한만 표시

5. **페이징 테스트**
   - 20개 이상 권한 생성
   - 페이지 전환 (이전/다음)
   - 예상 결과: 올바른 페이지 데이터 표시

---

## 7. 향후 확장 계획

### 7.1 단기 (1-2개월)

1. **IPC 계층 권한 상속**
   - H 섹션 권한 → 하위 H01L, H05H 자동 권한 부여
   - 명시적 권한 우선 (오버라이드)

2. **IPC 코드 자동완성 개선**
   - 한글/영문 검색 지원
   - 최근 사용 IPC 코드 우선 표시
   - 인기 IPC 코드 추천

3. **권한 승인 워크플로우**
   - 사용자 권한 요청 → 관리자 승인
   - 이메일 알림 연동

### 7.2 중기 (3-6개월)

1. **CPC(Cooperative Patent Classification) 통합**
   - IPC + CPC 하이브리드 검색
   - CPC 코드 마스터 데이터 추가

2. **특허 인용 네트워크 분석**
   - Forward/Backward Citation 그래프
   - 핵심 특허 식별

3. **AI 기반 특허 분류**
   - 특허 명세서 → IPC 코드 자동 분류
   - 기존 특허 IPC 코드 검증 및 제안

### 7.3 장기 (6개월 이상)

1. **글로벌 특허청 연동**
   - USPTO, EPO, JPO API 연동
   - 실시간 특허 상태 업데이트

2. **경쟁사 특허 모니터링**
   - 경쟁사 IPC 코드 추적
   - 신규 출원 알림

3. **특허 가치 평가 모델**
   - 인용 횟수, 패밀리 크기, 기술 분야 등 고려
   - AI 기반 특허 가치 점수 산출

---

## 8. 부록

### 8.1 IPC 코드 체계 상세 (예시)

**H01L 21/683 (반도체 웨이퍼 처리 챔버) 분해**:
- **H**: 섹션 (Section) - 전기
- **01**: 클래스 (Class) - 기본 전기 소자
- **L**: 서브클래스 (Subclass) - 반도체 소자
- **21**: 그룹 (Group) - 반도체 제조
- **683**: 서브그룹 (Subgroup) - 처리 챔버

### 8.2 역할 정의

| 역할 ID | 역할명 | 권한 |
|---------|--------|------|
| ADMIN | 관리자 | 특허 CRUD, 권한 부여, 메타데이터 수정 |
| EDITOR | 편집자 | 특허 CRU (삭제 불가), 파일 업로드 |
| VIEWER | 조회자 | 특허 조회, 검색, 다운로드 |

### 8.3 참고 자료

- **WIPO IPC 공식 사이트**: https://www.wipo.int/classifications/ipc/en/
- **KIPRIS IPC 검색**: https://www.kipris.or.kr/
- **반도체 장비 산업 분석**: 한국반도체산업협회
- **특허 분류 가이드**: 특허청 출원인 교육 자료

---

## 📝 변경 이력

| 버전 | 날짜 | 작성자 | 변경 내역 |
|------|------|--------|----------|
| 1.0 | 2026-01-04 | AI 시스템 | 초기 문서 작성 |

