# 13.1 Template PPT Generation Design

> **버전**: 1.3.0  
> **작성일**: 2025-12-03  
> **최종 수정**: 2025-12-10  
> **작성자**: AI Assistant  
> **상태**: AI-First 파이프라인 구현 완료, 사용자 UI 통합

---

## 1. 개요

### 1.1 목적

Template PPT 생성 시스템은 **기업의 브랜드 아이덴티티(로고, 색상, 폰트, 레이아웃)를 유지**하면서 **AI가 콘텐츠만 생성**하여 일관된 스타일의 프레젠테이션을 자동으로 만드는 것을 목표로 합니다.

### 1.2 핵심 개념

```
┌─────────────────────────────────────────────────────────────────┐
│                        Template PPT                              │
│                                                                  │
│   템플릿(껍데기)  +  AI 생성 콘텐츠(내용)  =  최종 PPT           │
│                                                                  │
│   ┌──────────┐      ┌──────────┐         ┌──────────┐           │
│   │ 디자인   │  +   │ 텍스트   │    =    │ 완성된   │           │
│   │ 레이아웃 │      │ 내용     │         │ 프레젠   │           │
│   │ 스타일   │      │          │         │ 테이션   │           │
│   └──────────┘      └──────────┘         └──────────┘           │
│                                                                  │
│   🎨 사용자/회사    🤖 AI Agent          📊 결과물              │
│      제공              생성                                      │
└─────────────────────────────────────────────────────────────────┘
```

### 1.3 Quick PPT vs Template PPT

| 구분 | Quick PPT | Template PPT |
|------|-----------|--------------|
| 디자인 | AI가 기본 디자인 적용 | **회사 브랜드 디자인 유지** |
| 일관성 | 매번 다를 수 있음 | **일관된 스타일 보장** |
| 사용성 | 빠른 생성 (원클릭) | 템플릿 선택 + 검토 |
| 적합 용도 | 빠른 초안, 개인용 | **기업 공식 문서, 브랜드 준수** |

### 1.4 Human in the Loop

사용자 만족도를 높이기 위해 **완전 자동화 + 사용자 확인/수정** 방식을 구현했습니다.

1. AI가 자동으로 콘텐츠 생성 및 매핑
2. **사용자가 검토 및 필요시 수정** ✅ 구현 완료
3. 확정 후 최종 PPT 생성

---

## 2. 시스템 아키텍처

### 2.1 전체 시스템 구성

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     Template PPT 시스템 아키텍처                             │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                    1️⃣ 템플릿 관리 모듈                                  │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │ │
│  │  │  템플릿 등록  │  │  템플릿 삭제  │  │  템플릿 목록  │                  │ │
│  │  │  (Upload)    │  │  (Delete)    │  │  (List)      │                  │ │
│  │  └──────┬───────┘  └──────────────┘  └──────────────┘                  │ │
│  │         │                                                               │ │
│  │         ▼                                                               │ │
│  │  ┌──────────────────────────────────────────────────────────────────┐  │ │
│  │  │                    템플릿 파싱 & 분석                              │  │ │
│  │  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐               │  │ │
│  │  │  │ PPTX 파싱   │→ │ 슬라이드별  │→ │ Metadata    │               │  │ │
│  │  │  │ (구조 추출) │  │ PDF 변환    │  │ JSON 생성   │               │  │ │
│  │  │  └─────────────┘  └─────────────┘  └─────────────┘               │  │ │
│  │  └──────────────────────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                    │                                         │
│                                    ▼                                         │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                    2️⃣ 템플릿 저장소                                     │ │
│  │                                                                         │ │
│  │   /uploads/templates/                                                   │ │
│  │   ├── users/                        ← 사용자별 템플릿 (주 저장소)       │ │
│  │   │   └── {user_id}/                ← 사용자 ID별 폴더                  │ │
│  │   │       ├── 템플릿파일.pptx       ← 원본 템플릿                       │ │
│  │   │       ├── config.json           ← 사용자 설정 (기본 템플릿 등)      │ │
│  │   │       └── metadata/             ← 메타데이터 폴더                   │ │
│  │   │           └── 템플릿_metadata.json                                  │ │
│  │   ├── thumbnails/                   ← 썸네일 캐시 (전역)                │ │
│  │   ├── metadata/                     ← 레거시 메타데이터                 │ │
│  │   └── user_templates/               ← 레거시 폴더 (사용 안 함)          │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                    │                                         │
│                                    ▼                                         │
│  ┌────────────────────────────────────────────────────────────────────────┐ │
│  │                    3️⃣ PPT 생성 모듈                                     │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐                  │ │
│  │  │  AI 콘텐츠   │→ │  자동 매핑   │→ │  PPT 생성    │                  │ │
│  │  │  생성        │  │  (JSON 기반) │  │  (PPTX 수정) │                  │ │
│  │  └──────────────┘  └──────────────┘  └──────────────┘                  │ │
│  └────────────────────────────────────────────────────────────────────────┘ │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 절충형 AI-First Template PPT 아키텍처 (v1.3.0)

> **최종 수정일**: 2025-12-10  
> **상태**: ✅ 구현 완료

사용자 UI 위자드를 통한 4단계 PPT 생성 워크플로우입니다.

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        1️⃣ 템플릿 등록 (관리자/사용자)                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   📤 사용자 UI: 템플릿 PPT 파일 업로드                                        │
│        ↓                                                                    │
│   ppt_template_extractor.py                                                 │
│   (템플릿 분석, 슬라이드 역할/요소 분류)                                        │
│        ↓ generates                                                          │
│   {template}_metadata.json                                                  │
│   (element_id, element_role, is_fixed, original_name, visualization_style)  │
│        ↓                                                                    │
│   💾 템플릿 저장소 (시스템/사용자 템플릿)                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
                           (템플릿 선택 가능)
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                    2️⃣ PPT 생성 위자드 (콘텐츠 생성)                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   📝 사용자 UI: 주제 입력 + 템플릿 선택                                        │
│        ↓                                                                    │
│   POST /api/v1/agent/presentation/templates/{id}/generate-content           │
│        ↓                                                                    │
│   unified_presentation_agent.py (use_ai_first=True)                         │
│        ↓                                                                    │
│   ai_direct_mapping_tool.py (545줄)                                         │
│   + ai_direct_mapping_system.prompt                                         │
│   (AI가 메타데이터 기반으로 element_id ↔ content 매핑 생성)                    │
│        ↓ produces                                                           │
│   UI 편집용 slides 데이터 + mappings                                         │
│   {slides: [{index, role, elements: [{id, text, role, original_text}]}]}    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                    3️⃣ PPT 생성 위자드 (텍스트 확인/편집)                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   ✏️ 사용자 UI: 슬라이드별 콘텐츠 미리보기 및 편집                              │
│   (PresentationOutlineModal.tsx - renderEditor)                             │
│        │                                                                    │
│        ├─ 좌측: 슬라이드 목록 네비게이션                                       │
│        ├─ 상단: 템플릿 썸네일 미리보기                                         │
│        └─ 하단: 요소별 텍스트 편집 폼                                          │
│             - element_id, role 표시                                          │
│             - 텍스트 편집 가능                                                │
│             - original_text 참조 표시                                        │
│        ↓ 사용자 편집 반영                                                    │
│   수정된 slides 데이터                                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
                                    ↓
┌─────────────────────────────────────────────────────────────────────────────┐
│                    4️⃣ PPT 생성 위자드 (최종 생성/저장)                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│   💾 사용자 UI: "다음: PPT 생성" 버튼 클릭                                     │
│        ↓                                                                    │
│   POST /api/v1/agent/presentation/templates/{id}/build-from-data            │
│   {slides: [...], output_filename: "프레젠테이션 제목", session_id}            │
│        ↓                                                                    │
│   unified_presentation_agent.py (use_ai_builder=True)                       │
│        ↓                                                                    │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  _enrich_mappings_with_original_names()                         │       │
│   │  (메타데이터에서 elementId → originalName 매핑 조회)             │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│        ↓                                                                    │
│   ┌─────────────────────────────────────────────────────────────────┐       │
│   │  ai_ppt_builder.py (AIPPTBuilder)                               │       │
│   │  ├─ _convert_mappings(): isEnabled 필터링, 형식 변환            │       │
│   │  └─ simple_ppt_builder.py (SimplePPTBuilder)                    │       │
│   │      ├─ original_name 기반 Shape 매칭                           │       │
│   │      ├─ 스타일 100% 보존 (폰트, 색상, 크기)                      │       │
│   │      ├─ 테이블 데이터 교체 (_apply_table_mapping)               │       │
│   │      └─ is_fixed 요소 보호 (isEnabled=False 스킵)               │       │
│   └─────────────────────────────────────────────────────────────────┘       │
│        ↓                                                                    │
│   📄 완성된 PPT 파일 (.pptx)                                                 │
│        ↓                                                                    │
│   📥 사용자 UI: 미리보기 (Google Docs Viewer) + 다운로드                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

#### 핵심 컴포넌트 요약

| 단계 | 컴포넌트 | 역할 |
|------|----------|------|
| 1️⃣ 등록 | `ppt_template_extractor.py` | 템플릿 분석 → 메타데이터 생성 |
| 2️⃣ 생성 | `ai_direct_mapping_tool.py` | AI 콘텐츠 매핑 생성 |
| 2️⃣ 생성 | `ai_direct_mapping_system.prompt` | AI 프롬프트 (외부 파일) |
| 3️⃣ 편집 | `PresentationOutlineModal.tsx` | 사용자 편집 UI |
| 4️⃣ 저장 | `ai_ppt_builder.py` | AI-First 빌더 래퍼 |
| 4️⃣ 저장 | `simple_ppt_builder.py` | 스타일 보존 PPT 생성 |

#### 프론트엔드 ↔ 백엔드 API 흐름

```
[Frontend]                              [Backend]
    │
    │ POST /generate-content
    │ {user_query, session_id, use_rag}
    │─────────────────────────────────────▶│
    │                                      │ unified_presentation_agent
    │                                      │ .generate_content_for_template()
    │                                      │   ↓
    │                                      │ AIDirectMappingTool._arun()
    │◀─────────────────────────────────────│
    │ {success, slides, presentation_title}
    │
    │ [사용자 편집]
    │
    │ POST /build-from-data
  │ {slides, output_filename, session_id}
    │─────────────────────────────────────▶│
    │                                      │ unified_presentation_agent
    │                                      │ .build_ppt_from_ui_data()
    │                                      │   ↓
    │                                      │ _enrich_mappings_with_original_names()
    │                                      │   ↓
    │                                      │ AIPPTBuilder.build()
    │                                      │   ↓
    │                                      │ SimplePPTBuilder.build()
    │◀─────────────────────────────────────│
    │ {file_url, file_path, file_name}
    │
    │ GET /download/{filename}
    │─────────────────────────────────────▶│
    │◀─────────────────────────────────────│
    │ [PPTX File Stream]

  > 참고:
  > - `session_id`는 서버에서 LangGraph `thread_id`로 사용되어, “생성→편집→최종 생성” 흐름의 상태를 요청 단위로 격리/재개할 수 있습니다.
  > - `output_filename`은 기본 파일명 베이스로 사용되며, 동시/반복 생성에서 덮어쓰기를 방지하기 위해 서버에서 suffix가 추가될 수 있습니다.
```

#### 데이터 구조

**UI 슬라이드 형식** (프론트엔드 ↔ 백엔드 공통):
```typescript
interface SlideContent {
    index: number;           // 1-based 슬라이드 번호
    role: string;            // 'title' | 'toc' | 'content' | 'thanks'
    elements: SlideElement[];
    note?: string;
}

interface SlideElement {
    id: string;              // 'textbox-0-0', 'table-4-0' 등
    text: string;            // AI 생성 또는 사용자 편집 콘텐츠
    role?: string;           // 'main_title', 'body_content' 등
    original_text?: string;  // 템플릿 원본 텍스트
}
```

**내부 매핑 형식** (백엔드):
```python
{
    "slideIndex": 0,         # 0-based
    "elementId": "textbox-0-0",
    "originalName": "TextBox 8",   # PPT Shape 이름 (매칭용)
    "newContent": "AI 생성 콘텐츠",
    "objectType": "textbox",       # 'textbox' | 'table' | 'shape'
    "isEnabled": True,             # False면 스킵 (is_fixed 보호)
}
```

### 2.3 디렉토리 구조

> **최종 수정일**: 2025-12-09  
> **코드 기준**: `user_template_manager.py`, `ppt_template_manager.py`

```text
/backend/uploads/
├── templates/                              # 템플릿 관련 파일
│   ├── users/                              # 🔑 사용자별 템플릿 (주 저장소)
│   │   └── {user_id}/                      # 사용자 ID별 폴더 (예: 8, 77107791)
│   │       ├── 제품소개서 샘플.pptx        # 원본 템플릿 파일 (.pptx)
│   │       ├── config.json                 # 사용자 설정 (default_template_id)
│   │       └── metadata/                   # 템플릿 메타데이터 폴더
│   │           └── {template_id}_metadata.json  # 구조 메타데이터
│   │
│   ├── thumbnails/                         # 썸네일 캐시 (전역, PDF → PNG 변환)
│   ├── metadata/                           # 레거시 메타데이터 (하위 호환용)
│   └── user_templates/                     # 레거시 폴더 (사용 안 함)
│
├── presentations/                          # 생성된 PPT 파일
│   ├── pptx/                               # 최종 생성된 PPTX 파일
│   ├── html/                               # HTML 미리보기 (옵션)
│   └── outline/                            # 아웃라인 JSON (옵션)
│
├── chat_attachments/                       # 채팅 첨부 파일
│
└── pdf_cache/                              # PDF 미리보기 캐시
```

#### 주요 파일 설명

| 경로 | 설명 | 생성 시점 |
|------|------|-----------|
| `users/{user_id}/` | 사용자별 템플릿 저장소 | 첫 템플릿 업로드 시 |
| `users/{user_id}/{파일명}.pptx` | 업로드된 원본 템플릿 | 템플릿 업로드 API |
| `users/{user_id}/config.json` | 사용자 설정 (기본 템플릿 ID) | 기본 템플릿 설정 시 |
| `users/{user_id}/metadata/{id}_metadata.json` | 템플릿 구조 메타데이터 | 템플릿 업로드 시 자동 생성 |
| `thumbnails/{template_id}.png` | 템플릿 썸네일 이미지 | 템플릿 등록 시 자동 생성 |
| `presentations/pptx/*.pptx` | 최종 생성된 PPT 파일 | PPT 생성 완료 시 |

#### 관련 코드 파일

```text
backend/app/services/presentation/
├── user_template_manager.py       # 🔑 사용자별 템플릿 관리 (업로드, 삭제, 목록)
├── ppt_template_manager.py        # 템플릿 레지스트리 및 분석
├── ppt_template_extractor.py      # 템플릿 메타데이터 추출
├── templated_ppt_generator_service.py  # Template PPT 생성
└── template_content_cleaner.py    # 템플릿 텍스트 정리
```

---

## 3. Metadata JSON 스키마

### 3.1 전체 구조

```json
{
  "template_id": "제품소개서_샘플",
  "file_name": "제품소개서 샘플.pptx",
  "file_path": "/uploads/templates/제품소개서 샘플.pptx",
  "created_at": "2025-12-03T10:00:00Z",
  "updated_at": "2025-12-03T10:00:00Z",
  "slide_count": 9,
  "slide_width": 960.17,
  "slide_height": 720,
  "slides": [
    {
      "index": 0,
      "slide_number": 1,
      "layout_name": "제목 슬라이드",
      "role": "title",
      "role_description": "표지 슬라이드",
      "editable_elements": [...],
      "fixed_elements": [...],
      "preview_url": "/api/v1/templates/제품소개서_샘플/preview/1"
    },
    ...
  ]
}
```

### 3.2 슬라이드 역할 (Role) 정의

| Role | 설명 | 특징 |
|------|------|------|
| `title` | 표지 슬라이드 | 메인 제목, 부제, 발표자 정보 |
| `toc` | 목차 슬라이드 | 섹션 목록, 페이지 번호 |
| `section` | 섹션 구분 슬라이드 | 챕터 제목 |
| `content` | 내용 슬라이드 | 본문 텍스트, 불릿 포인트 |
| `image` | 이미지 중심 슬라이드 | 제품 사진, 다이어그램 |
| `chart` | 차트/그래프 슬라이드 | 데이터 시각화 |
| `comparison` | 비교 슬라이드 | 2열 레이아웃 |
| `closing` | 마무리 슬라이드 | 감사 인사, 연락처 |

### 3.3 편집 가능 요소 (Editable Element) 스키마

```json
{
  "id": "textbox-2-0",
  "type": "textbox",
  "element_role": "main_title",
  "original_text": "제품 개요",
  "position": {
    "x": 32.33,
    "y": 139.66,
    "width": 400,
    "height": 60
  },
  "style": {
    "font_name": "맑은 고딕",
    "font_size": 44,
    "bold": true,
    "color": "#333333"
  },
  "constraints": {
    "max_chars": 50,
    "max_lines": 2,
    "preserve_style": true
  }
}
```

### 3.4 요소 역할 (Element Role) 정의

| Element Role | 설명 | AI 콘텐츠 매핑 |
|--------------|------|----------------|
| `main_title` | 슬라이드 메인 제목 | 아웃라인 섹션 제목 |
| `subtitle` | 부제목 | 아웃라인 섹션 부제 |
| `body_text` | 본문 텍스트 | 불릿 포인트 내용 |
| `bullet_point` | 불릿 항목 | 개별 불릿 |
| `highlight_box` | 강조 박스 | 핵심 메시지 |
| `caption` | 캡션/설명 | 이미지 설명 |
| `footer` | 푸터 정보 | 페이지 번호, 날짜 |
| `logo` | 로고 (고정) | - |
| `company_name` | 회사명 (고정) | - |

---

## 4. 워크플로우 상세

### 4.1 전체 워크플로우

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                   Template PPT 생성 워크플로우                               │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Phase 0: 사전 준비 (관리자)                                                │
│  ═══════════════════════════                                                │
│  1. 템플릿 PPTX 업로드                                                      │
│  2. 자동 파싱 → Metadata JSON 생성                                          │
│  3. 슬라이드별 이미지 미리보기 생성                                         │
│  4. 슬라이드 역할/편집영역 검토 및 조정 (선택사항)                          │
│                                                                              │
│                           ↓                                                  │
│                                                                              │
│  Phase 1: 사용자 요청 → AI 답변                                             │
│  ═══════════════════════════════                                            │
│  1. 채팅창에서 PPT 관련 질문                                                │
│  2. RAG 기반 컨텍스트 검색                                                  │
│  3. AI가 구조화된 답변 생성 (H2, H3, bullets)                               │
│  4. [📝 구조 확인 및 재생성] 버튼 표시                                      │
│                                                                              │
│                           ↓                                                  │
│                                                                              │
│  Phase 2: 팝업 모달 - 템플릿 선택 & 자동 매핑                               │
│  ════════════════════════════════════════════                               │
│  1. 팝업 모달 열림                                                          │
│  2. [아웃라인 탭] AI 생성 구조 확인                                         │
│  3. 템플릿 선택 (드롭다운)                                                  │
│  4. "Template PPT 생성" 클릭                                                │
│  5. 백그라운드: Metadata JSON 기반 자동 매핑 실행                           │
│                                                                              │
│                           ↓                                                  │
│                                                                              │
│  Phase 3: 생성 및 결과 확인 (Current Implementation)                        │
│  ═══════════════════════════════════════════════════                        │
│  1. [생성 중 화면] 단계별 진행상황 표시 (SSE 스트리밍)                      │
│     - 아웃라인 분석 → 템플릿 분석 → 콘텐츠 매핑 → 파일 생성                 │
│  2. [결과 미리보기 탭] 이동                                                 │
│  3. Office Online Viewer / Google Docs Viewer로 결과 확인                   │
│  4. [다운로드] 버튼으로 PPTX 파일 저장                                      │
│                                                                              │
│  ⚠️ 참고: 상세 편집 기능(Human in the Loop)은 Future Plan으로 이동됨        │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 Phase별 상세

#### Phase 0: 템플릿 등록 프로세스

```
사용자: PPTX 파일 업로드
        │
        ▼
┌──────────────────────────────────────────────────────────────────────┐
│ Step 1: 파일 저장                                                    │
│ /uploads/templates/제품소개서_샘플.pptx                              │
└──────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────────────┐
│ Step 2: PPTX 구조 파싱 (python-pptx)                                 │
│                                                                      │
│ 추출 정보:                                                           │
│ • 슬라이드 수, 레이아웃 이름                                         │
│ • 각 슬라이드의 Shape 목록 (텍스트박스, 이미지, 도형 등)             │
│ • Shape별 속성 (위치, 크기, 텍스트, 폰트 등)                         │
│ • 슬라이드 역할 추론 (AI 또는 규칙 기반)                             │
└──────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────────────┐
│ Step 3: Metadata JSON 생성                                           │
│ /uploads/templates/metadata/제품소개서_샘플_metadata.json            │
└──────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────────────┐
│ Step 4: 슬라이드별 이미지 미리보기 생성 (LibreOffice)                │
│                                                                      │
│ /uploads/templates/previews/제품소개서_샘플/                         │
│ ├── slide_1.png                                                      │
│ ├── slide_2.png                                                      │
│ └── ...                                                              │
│                                                                      │
│ 방법: PPTX → PDF 변환 → 페이지별 PNG 이미지 추출                     │
└──────────────────────────────────────────────────────────────────────┘
        │
        ▼
┌──────────────────────────────────────────────────────────────────────┐
│ Step 5: 등록 완료                                                    │
└──────────────────────────────────────────────────────────────────────┘
```

#### Phase 3: 결과 확인 UI (Current)

```
┌─────────────────────────────────────────────────────────────────────────┐
│  PPT 생성 완료                                                   [X]   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  [아웃라인 📋]  [템플릿 선택 🎨]  [결과 미리보기 🖼️]                    │
│                                    ════════════════                     │
│                                                                         │
│  ┌──────────────────────────────────────────────────────────────────┐  │
│  │                                                                  │  │
│  │   [ Office Online / Google Docs Viewer ]                         │  │
│  │                                                                  │  │
│  │   ┌──────────────────────────────────────────────────────────┐   │  │
│  │   │                                                          │   │  │
│  │   │   [PPT 미리보기 화면]                                    │   │  │
│  │   │                                                          │   │  │
│  │   │   • 실제 생성된 PPTX 파일 렌더링                         │   │  │
│  │   │   • 스크롤하여 전체 슬라이드 확인 가능                   │   │  │
│  │   │                                                          │   │  │
│  │   └──────────────────────────────────────────────────────────┘   │  │
│  │                                                                  │  │
│  └──────────────────────────────────────────────────────────────────┘  │
│                                                                         │
│  [◀ 다시 생성]                                     [📥 PPT 다운로드]   │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 5. 미리보기 시스템

### 5.1 이중 미리보기 전략

| 뷰 타입 | 📊 편집 뷰 | 🎨 디자인 뷰 |
|---------|-----------|-------------|
| **데이터 소스** | Metadata JSON | 슬라이드 이미지 (PNG) |
| **용도** | 텍스트 편집/수정 | 실제 디자인 확인 |
| **인터랙션** | 클릭하여 편집 가능 | 읽기 전용 |
| **장점** | 실시간 편집, 빠름 | 정확한 시각적 표현 |
| **구현** | React 컴포넌트 렌더링 | 이미지 표시 |

### 5.2 편집 뷰 (속성 기반)

```
┌──────────────────────────────────────────────────────────────────────┐
│                         편집 뷰 렌더링                               │
│                                                                      │
│   Metadata JSON의 position 정보를 사용하여                          │
│   각 편집 가능 요소를 상대적 위치에 렌더링                          │
│                                                                      │
│   ┌────────────────────────────────────────────────────────────┐    │
│   │                                                            │    │
│   │   ┌──────────────────────────────────────┐                │    │
│   │   │ [메인 제목]                          │ ← 클릭 시 편집 │    │
│   │   │ 제품 개요                            │                │    │
│   │   └──────────────────────────────────────┘                │    │
│   │                                                            │    │
│   │   ┌──────────────────────────────────────┐                │    │
│   │   │ [본문 영역]                          │                │    │
│   │   │ • CGM 연동 자동 인슐린 주입          │                │    │
│   │   │ • 실시간 혈당 모니터링               │                │    │
│   │   └──────────────────────────────────────┘                │    │
│   │                                                            │    │
│   │   [Logo]  [Company]  ← 고정 요소 (회색, 편집 불가)        │    │
│   │                                                            │    │
│   └────────────────────────────────────────────────────────────┘    │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

### 5.3 디자인 뷰 (이미지 기반)

```
┌──────────────────────────────────────────────────────────────────────┐
│                         디자인 뷰 렌더링                             │
│                                                                      │
│   /uploads/templates/previews/제품소개서_샘플/slide_3.png           │
│                                                                      │
│   ┌────────────────────────────────────────────────────────────┐    │
│   │                                                            │    │
│   │                  [PNG 이미지 표시]                         │    │
│   │                                                            │    │
│   │          실제 PPT 슬라이드의 렌더링 결과                   │    │
│   │          폰트, 색상, 레이아웃 모두 포함                    │    │
│   │                                                            │    │
│   └────────────────────────────────────────────────────────────┘    │
│                                                                      │
│   💡 편집 후 미리보기 새로고침 시:                                  │
│      1. 현재 매핑 데이터로 임시 PPTX 생성                           │
│      2. 해당 슬라이드만 PNG로 변환                                  │
│      3. 이미지 업데이트                                             │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 6. API 설계

### 6.1 템플릿 관리 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| `POST` | `/api/v1/templates/upload` | 템플릿 업로드 & 파싱 |
| `GET` | `/api/v1/templates` | 템플릿 목록 조회 |
| `GET` | `/api/v1/templates/{id}` | 템플릿 상세 조회 |
| `GET` | `/api/v1/templates/{id}/metadata` | 메타데이터 JSON 조회 |
| `GET` | `/api/v1/templates/{id}/preview/{slide}` | 슬라이드 이미지 미리보기 |
| `PUT` | `/api/v1/templates/{id}` | 템플릿 정보 수정 |
| `DELETE` | `/api/v1/templates/{id}` | 템플릿 삭제 |
| `POST` | `/api/v1/templates/{id}/regenerate-previews` | 미리보기 재생성 |

### 6.2 PPT 생성 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| `POST` | `/api/v1/presentation/auto-mapping` | AI 자동 매핑 실행 |
| `POST` | `/api/v1/presentation/generate-template` | 최종 PPT 생성 |
| `POST` | `/api/v1/presentation/preview-slide` | 단일 슬라이드 미리보기 |

### 6.3 API 요청/응답 예시

#### 자동 매핑 API

**Request:**
```json
POST /api/v1/presentation/auto-mapping
{
  "template_id": "제품소개서_샘플",
  "outline": {
    "title": "스마트 인슐린 펌프",
    "sections": [
      {
        "title": "제품 개요",
        "bullets": [
          "CGM 연동 자동 인슐린 주입",
          "실시간 혈당 모니터링"
        ]
      }
    ]
  },
  "context": "RAG 컨텍스트 텍스트..."
}
```

**Response:**
```json
{
  "success": true,
  "mappings": [
    {
      "slide_index": 0,
      "slide_role": "title",
      "elements": [
        {
          "element_id": "textbox-0-0",
          "element_role": "main_title",
          "source": "ai_generated",
          "content": "스마트 인슐린 펌프",
          "original_content": "제품 제목"
        }
      ]
    }
  ],
  "preview_urls": [
    "/api/v1/templates/제품소개서_샘플/preview/1",
    "/api/v1/templates/제품소개서_샘플/preview/2"
  ]
}
```

#### 최종 PPT 생성 API

**Request:**
```json
POST /api/v1/presentation/generate-template
{
  "template_id": "제품소개서_샘플",
  "mappings": [
    {
      "slide_index": 0,
      "elements": [
        {
          "element_id": "textbox-0-0",
          "source": "ai_generated",
          "content": "스마트 인슐린 펌프"
        },
        {
          "element_id": "textbox-0-1",
          "source": "user_modified",
          "content": "사용자가 수정한 부제"
        }
      ]
    }
  ]
}
```

**Response:**
```json
{
  "success": true,
  "file_url": "/uploads/mapped_presentation_스마트_인슐린_펌프.pptx",
  "file_name": "mapped_presentation_스마트_인슐린_펌프.pptx",
  "slide_count": 9,
  "ai_applied_count": 7,
  "user_modified_count": 2
}
```

---

## 7. 프론트엔드 컴포넌트 구조

### 7.1 컴포넌트 트리

```
PresentationOutlineModal/
├── OutlineTab/                    # 아웃라인 탭
│   ├── OutlineEditor              # 아웃라인 편집기
│   └── TemplateSelector           # 템플릿 선택 드롭다운
│
├── SlidePreviewTab/               # 슬라이드 미리보기 탭
│   ├── SlideNavigator             # 좌측 슬라이드 네비게이션
│   ├── SlidePreviewArea/          # 미리보기 영역
│   │   ├── EditView               # 📊 편집 뷰 (속성 기반)
│   │   └── DesignView             # 🎨 디자인 뷰 (이미지)
│   └── SlideStatusIndicator       # 슬라이드 상태 표시
│
├── DetailEditTab/                 # 상세 편집 탭
│   ├── ElementEditor              # 개별 요소 편집기
│   │   ├── ContentSourceSelector  # AI추천/원본/직접입력 선택
│   │   └── TextEditor             # 텍스트 편집 영역
│   └── SlideNavigation            # 슬라이드 이동 버튼
│
└── ActionButtons/                 # 하단 액션 버튼
    ├── CancelButton
    └── GenerateButton             # PPT 생성 확정
```

### 7.2 상태 관리

```typescript
interface TemplatePPTState {
  // 템플릿 정보
  selectedTemplate: TemplateInfo | null;
  templateMetadata: TemplateMetadata | null;
  
  // 아웃라인
  outline: PPTOutline;
  
  // 매핑 데이터
  mappings: SlideMapping[];
  
  // UI 상태
  activeTab: 'outline' | 'preview' | 'edit';
  currentSlideIndex: number;
  previewMode: 'edit' | 'design';
  
  // 진행 상태
  isAutoMapping: boolean;
  isGenerating: boolean;
  
  // 슬라이드별 상태
  slideStatuses: {
    [slideIndex: number]: 'ai_applied' | 'user_modified' | 'pending';
  };
}

interface SlideMapping {
  slideIndex: number;
  slideRole: string;
  elements: ElementMapping[];
}

interface ElementMapping {
  elementId: string;
  elementRole: string;
  source: 'ai_generated' | 'original' | 'user_modified';
  content: string;
  originalContent: string;
}
```

---

## 8. 자동 매핑 로직

### 8.1 매핑 알고리즘

```python
def auto_map_content(template_metadata: dict, outline: dict) -> list:
    """
    AI 아웃라인을 템플릿 슬라이드에 자동 매핑
    """
    mappings = []
    outline_sections = outline.get('sections', [])
    section_index = 0
    
    for slide in template_metadata['slides']:
        slide_mapping = {
            'slide_index': slide['index'],
            'slide_role': slide['role'],
            'elements': []
        }
        
        # 역할별 매핑 전략
        if slide['role'] == 'title':
            # 표지 슬라이드: 메인 제목, 부제
            map_title_slide(slide, outline, slide_mapping)
            
        elif slide['role'] == 'toc':
            # 목차 슬라이드: 섹션 목록
            map_toc_slide(slide, outline_sections, slide_mapping)
            
        elif slide['role'] == 'content':
            # 내용 슬라이드: 순차적으로 섹션 매핑
            if section_index < len(outline_sections):
                map_content_slide(slide, outline_sections[section_index], slide_mapping)
                section_index += 1
                
        elif slide['role'] == 'closing':
            # 마무리 슬라이드: 고정 또는 기본값
            map_closing_slide(slide, slide_mapping)
        
        mappings.append(slide_mapping)
    
    return mappings
```

### 8.2 역할별 매핑 규칙

| 슬라이드 역할 | 매핑 규칙 |
|--------------|----------|
| `title` | 아웃라인 title → main_title, subtitle |
| `toc` | 아웃라인 sections 제목들 → 목차 항목 |
| `content` | 섹션별 title → main_title, bullets → body_text |
| `closing` | 기본값 유지 (감사합니다 등) |

---

## 9. 구현 우선순위

### 9.1 Phase 1: 기반 구축 (필수)

| 순서 | 기능 | 설명 | 복잡도 | 예상 시간 |
|------|------|------|--------|----------|
| 1-1 | Metadata JSON 스키마 개선 | role, element_role 필드 추가 | 중 | 2h |
| 1-2 | 슬라이드 역할 추론 | 레이아웃 이름 기반 역할 자동 분류 | 중 | 2h |
| 1-3 | 슬라이드별 이미지 미리보기 생성 | LibreOffice → PNG | 중 | 3h |
| 1-4 | 템플릿 등록 API 개선 | 미리보기 생성 포함 | 중 | 2h |

### 9.2 Phase 2: 자동 매핑 (핵심)

| 순서 | 기능 | 설명 | 복잡도 | 예상 시간 |
|------|------|------|--------|----------|
| 2-1 | 자동 매핑 로직 | 아웃라인 → 슬라이드 매핑 | 상 | 4h |
| 2-2 | 자동 매핑 API | `/api/v1/presentation/auto-mapping` | 중 | 2h |
| 2-3 | 매핑 기반 PPT 생성 개선 | 기존 로직 리팩토링 | 중 | 3h |

### 9.3 Phase 3: 프론트엔드 UI (Viewer & Download)

| 순서 | 기능 | 설명 | 복잡도 | 예상 시간 | 상태 |
|------|------|------|--------|----------|------|
| 3-1 | 생성 진행 상태 표시 | SSE 스트리밍 연동 | 중 | 3h | ✅ 완료 |
| 3-2 | 결과 미리보기 탭 | Office Online Viewer 연동 | 중 | 3h | ✅ 완료 |
| 3-3 | 다운로드 기능 | PPTX 파일 다운로드 | 하 | 1h | ✅ 완료 |
| 3-4 | 상세 편집 UI | (Human in the Loop) | 상 | - | 🔜 Future |

### 9.4 Phase 4: 마무리

| 순서 | 기능 | 설명 | 복잡도 | 예상 시간 |
|------|------|------|--------|----------|
| 4-1 | AI 사고 과정 UI | 접이식 패널 (이미 작업 중) | 하 | 1h |
| 4-2 | 템플릿 관리 UI | 관리자용 CRUD (선택사항) | 중 | 4h |
| 4-3 | 에러 처리 & 로깅 | 안정성 개선 | 중 | 2h |

---

## 10. 파일 구조 (예상)

### 10.1 백엔드

```
backend/app/
├── services/presentation/
│   ├── ppt_template_manager.py          # 기존 (개선)
│   ├── template_metadata_generator.py   # 신규: 메타데이터 생성
│   ├── template_preview_generator.py    # 신규: 미리보기 생성
│   ├── auto_mapping_service.py          # 신규: 자동 매핑
│   ├── templated_ppt_generator_service.py  # 기존 (개선)
│   └── enhanced_object_processor.py     # 개선: 테이블 셀 초기화 로직 추가
│
├── tools/presentation/                   # 🆕 Agent Tool 디렉토리
│   ├── outline_generation_tool.py       # 개선: 제목 정제 로직 추가
│   ├── template_ppt_comparator_tool.py  # 🆕 신규: 품질 검증 도구
│   ├── ppt_quality_validator_tool.py    # 기존
│   ├── template_analyzer_tool.py
│   ├── slide_type_matcher_tool.py
│   ├── content_mapping_tool.py
│   ├── templated_pptx_builder_tool.py
│   └── visualization_tool.py
│
├── agents/presentation/
│   ├── unified_presentation_agent.py    # 개선: 자동 품질 검증 추가
│   └── base_agent.py
│
├── prompts/presentation/
│   ├── templated_react_agent_system.prompt  # 개선: topic 정제 지침
│   └── react_agent_system.prompt            # 개선: topic 정제 지침
│
├── api/v1/
│   └── presentation.py                  # 기존 (API 추가)
│
└── schemas/
    └── presentation.py                  # 기존 (스키마 추가)
```

### 10.2 프론트엔드

```
frontend/src/pages/user/chat/components/presentation/
├── PresentationOutlineModal.tsx         # 개선: 품질 리포트 표시
├── OutlineTab.tsx                       # 기존
├── SlidePreviewTab/                     # 신규
│   ├── index.tsx
│   ├── SlideNavigator.tsx
│   ├── EditView.tsx
│   └── DesignView.tsx
├── DetailEditTab/                       # 신규
│   ├── index.tsx
│   └── ElementEditor.tsx
├── PPTReasoningPanel.tsx                # 신규 (AI 사고 과정)
└── hooks/
    └── useTemplatePPT.ts                # 신규: 상태 관리 훅
```

### 10.3 테스트 및 문서

```
/home/admin/Dev/abekm/
├── test_ppt_quality_validation.py       # 🆕 품질 검증 도구 테스트
├── test_topic_refinement.py             # 🆕 제목 정제 기능 테스트
├── test_template_comparator.py          # 🆕 템플릿 비교 테스트
├── PPT_QUALITY_VALIDATION_SYSTEM.md     # 🆕 품질 검증 시스템 가이드
├── PPT_TITLE_REFINEMENT_SYSTEM.md       # 🆕 제목 정제 시스템 가이드
└── 01.docs/
    └── 13.1.Template_PPT_generation_design.md  # 본 문서
```

---

## 11. 참고 문서

- [13.agent_design_for_presentation.md](./13.agent_design_for_presentation.md) - PPT Agent 전체 설계
- [python-pptx Documentation](https://python-pptx.readthedocs.io/)
- [LibreOffice CLI](https://help.libreoffice.org/latest/en-US/text/shared/guide/start_parameters.html)

---

## 12. 구현 현황

### 12.1 완료된 구현 (2025-12-03)

#### ✅ Priority 1: 템플릿 파싱 개선
- **파일**: `backend/app/services/presentation/ppt_template_extractor.py`
- **구현 내용**:
  - `classify_slide_role()` - 슬라이드 역할 자동 분류 (title, toc, content, thanks)
  - `classify_element()` - 요소별 편집 가능 여부 분류 (editable/fixed)
  - `extract_presentation()` 함수 개선 - role, editable_elements, fixed_elements 포함

#### ✅ Priority 2: PDF 미리보기 생성
- **파일**: `backend/app/services/presentation/pdf_preview_generator.py` (신규, 253줄)
- **구현 내용**:
  - `PDFPreviewGenerator` 클래스
  - `generate_slide_previews()` - LibreOffice 기반 PDF/PNG 변환
  - `generate_all_template_previews()` - 전체 템플릿 미리보기 일괄 생성

#### ✅ Priority 3: 자동 매핑 로직
- **파일**: `backend/app/services/presentation/template_auto_mapping_service.py` (신규, 352줄)
- **구현 내용**:
  - `TemplateAutoMappingService` 클래스
  - `auto_map_outline_to_template()` - AI 아웃라인 → 템플릿 슬라이드 자동 매핑
  - `_match_outline_to_template_slide()` - 역할 기반 슬라이드 매칭
  - `_generate_ai_content_suggestion()` - AI 콘텐츠 제안 생성

#### ✅ Priority 4: 결과 확인 UI (Viewer)
- **파일**: `frontend/src/pages/user/chat/components/presentation/PresentationOutlineModal.tsx`
- **구현 내용**:
  - 단계별 진행 상태 표시 (Generating Step)
  - 최종 결과물 미리보기 (Office Online / Google Docs Viewer)
  - 다운로드 기능

#### ✅ API 엔드포인트 추가
- **파일**: `backend/app/api/v1/presentation.py`
- **새 엔드포인트**:
  - `POST /templates/{template_id}/auto-mapping` - 자동 매핑 실행
  - `GET /templates/{template_id}/preview/{slide_index}` - 슬라이드 미리보기 조회
  - `POST /templates/{template_id}/generate-previews` - 미리보기 생성 트리거

### 12.2 최신 구현 (2025-12-05)

#### ✅ Priority 5: PPT 품질 검증 시스템
- **파일**: `backend/app/tools/presentation/template_ppt_comparator_tool.py`
- **구현 내용**:
  - **AI Agent 자율 품질 검증**: PPT 생성 후 자동으로 템플릿과 비교
  - **검증 항목**:
    - 슬라이드 수/레이아웃/Shape 구조 일치 확인
    - **테이블 데이터 교체 검증** (핵심 - 템플릿 데이터 잔존 방지)
    - 텍스트 교체 여부 확인
    - 품질 점수 산출 (0-100)
  - **상세 리포트 생성**:
    - 치명적 문제 (critical_issues)
    - 경고 (warnings)
    - 슬라이드별 상세 분석
    - 테이블/텍스트 이슈 목록
    - 권장 조치 사항 (recommendations)
  - **Agent 통합**:
    - `UnifiedPresentationAgent`에 자동 검증 로직 추가
    - `_validate_template_ppt_quality()` 메서드로 빌드 후 즉시 검증
    - 검증 결과를 응답에 포함 (`quality_report`)

**검증 결과 예시**:
```python
{
    "passed": False,
    "quality_score": 80.0,
    "critical_issues": ["테이블 데이터 미교체: 1개"],
    "warnings": ["텍스트 미교체: 1개"],
    "recommendations": [
        "프론트엔드에서 tableData를 metadata에 포함",
        "백엔드 enhanced_object_processor.py 확인"
    ]
}
```

#### ✅ Priority 6: 제목 자동 정제 시스템
- **파일**: `backend/app/tools/presentation/outline_generation_tool.py`
- **프롬프트**: `backend/prompts/presentation/*.prompt` (2개)
- **구현 내용**:
  - **문제 해결**: "자동차산업 특허분석 방법론 PPT작성 해줘요" → "자동차산업 특허분석 방법론"
  - **3단계 정제 프로세스**:
    1. **정규식 패턴 매칭** (빠름): "PPT 작성", "해줘요" 등 제거
    2. **LLM 폴백** (복잡한 경우): AI로 명사형 제목 추출
    3. **원본 유지** (이미 명사형인 경우)
  - **Agent 프롬프트 개선**:
    - topic 파라미터 사용 시 명사형 정제 지침 추가
    - 예시 제공: "자동차산업 특허분석 방법론 PPT작성 해줘요" → "자동차산업 특허분석 방법론"
  - **자동 처리**:
    - `_refine_topic()` 메서드로 입력 즉시 정제
    - 로그에 정제 전/후 기록

**정제 예시**:
```python
# Before
topic = "자동차산업 특허분석 방법론 PPT작성 해줘요"
파일명 = "자동차산업 특허분석 방법론 PPT작성 해줘요.pptx" ❌

# After (자동 정제)
topic = "자동차산업 특허분석 방법론"
파일명 = "자동차산업 특허분석 방법론.pptx" ✅
```

#### 📊 검증 및 테스트
- **품질 검증**: `/home/admin/Dev/abekm/test_ppt_quality_validation.py`
- **제목 정제**: `/home/admin/Dev/abekm/test_topic_refinement.py`
- **문서**:
  - `PPT_QUALITY_VALIDATION_SYSTEM.md` - 품질 검증 시스템 가이드
  - `PPT_TITLE_REFINEMENT_SYSTEM.md` - 제목 정제 시스템 가이드

### 12.3 구현 완료 (v1.3.0)

| 기능 | 설명 | 상태 |
|------|------|------|
| **Human in the Loop (상세 편집)** | 생성 전 매핑 결과 검토 및 텍스트 수정 기능 | ✅ 완료 |
| **테이블 데이터 교체** | SimplePPTBuilder에서 테이블 데이터 자동 교체 | ✅ 완료 |
| **AI-First 파이프라인** | 단일 AI 호출로 모든 매핑 생성 | ✅ 완료 |
| **프론트엔드 UI 통합** | PresentationOutlineModal.tsx 4단계 위자드 | ✅ 완료 |

### 12.4 Future Plan (To-Be)

| 우선순위 | 기능 | 설명 |
|----------|------|------|
| 1 | 템플릿 관리 UI (Admin CRUD) | 템플릿 업로드, 삭제, 메타데이터 수정 관리자 페이지 |
| 2 | AI Reasoning Panel 개선 | AI의 사고 과정을 더 상세하게 시각화 |
| 3 | 자동 수정 기능 | 품질 검증 실패 시 Agent가 자동으로 재생성 |
| 4 | 이미지/차트 자동 생성 | AI가 콘텐츠에 맞는 이미지/차트 생성 |
| 5 | 통합 테스트 | 전체 파이프라인에 대한 E2E 테스트 |

---

## 13. Agent Tool 아키텍처

### 13.1 Tool 기반 품질 관리

템플릿 PPT 생성 품질 향상을 위해 **AI Agent가 자율적으로 사용할 수 있는 Tool**을 `/backend/app/tools/presentation/` 디렉토리에 구현했습니다.

```
backend/app/tools/presentation/
├── ai_direct_mapping_tool.py          # 🆕 AI-First 매핑 도구 (545 lines)
├── template_ppt_comparator_tool.py    # 품질 검증 도구
├── outline_generation_tool.py         # 개선 (제목 정제 추가)
├── ppt_quality_validator_tool.py      # 기존 (일반 품질 검증)
├── template_analyzer_tool.py
├── slide_type_matcher_tool.py
├── content_mapping_tool.py
├── templated_pptx_builder_tool.py
└── visualization_tool.py
```

### 13.2 품질 검증 워크플로우

```
┌─────────────────────────────────────────────────────────────────┐
│           Template PPT 생성 + 자동 품질 검증                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  1️⃣ outline_generation_tool                                     │
│     ├─ topic 자동 정제 (요청 표현 제거)                         │
│     └─ 아웃라인 생성                                             │
│                                                                  │
│  2️⃣ template_analyzer_tool                                      │
│     └─ 템플릿 구조 분석                                          │
│                                                                  │
│  3️⃣ content_mapping_tool                                        │
│     └─ 콘텐츠 자동 매핑                                          │
│                                                                  │
│  4️⃣ templated_pptx_builder_tool                                 │
│     └─ PPTX 파일 생성                                            │
│                                                                  │
│  5️⃣ template_ppt_comparator_tool (🆕 자동 실행)                 │
│     ├─ 생성 파일 vs 템플릿 파일 비교                            │
│     ├─ 테이블 데이터 교체 확인                                  │
│     ├─ 텍스트 교체 확인                                         │
│     ├─ 품질 점수 계산 (0-100)                                   │
│     └─ 상세 리포트 생성                                         │
│                                                                  │
│  6️⃣ Final Answer                                                │
│     ├─ file_path: 생성된 파일 경로                              │
│     └─ quality_report: 검증 결과 (🆕)                           │
│         ├─ passed: true/false                                   │
│         ├─ quality_score: 80.0                                  │
│         ├─ critical_issues: [...]                               │
│         └─ recommendations: [...]                               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 13.3 Tool 간 협업

1. **outline_generation_tool** → 제목 정제 → 깔끔한 파일명
2. **templated_pptx_builder_tool** → PPT 생성
3. **template_ppt_comparator_tool** → 품질 검증 → 문제 발견 시 로그 경고

### 13.4 검증 점수 산출 방식

```python
초기 점수 = 100.0

# 구조 문제
- 레이아웃 불일치: -5점
- Shape 개수 불일치: -5점
- 일반 문제: -3점/개

# 콘텐츠 문제
- 테이블 데이터 미교체: -15점/개 (치명적!)
- 텍스트 미교체: -2점/개

합격 기준: 치명적 문제 0개 AND 점수 >= 70점
```

---

## 14. 변경 이력

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| 1.0.0 | 2025-12-03 | 초기 설계 문서 작성 |
| 1.1.0 | 2025-12-03 | Priority 1~4 구현 완료, 구현 현황 섹션 추가 |
| 1.2.0 | 2025-12-05 | **Priority 5~6 구현 완료**: 품질 검증 시스템, 제목 정제 시스템 추가<br/>- `TemplatePPTComparatorTool` (585 lines)<br/>- `_refine_topic()` 메서드 (58 lines)<br/>- Agent 자동 검증 통합<br/>- 테스트 스크립트 3개 추가<br/>- Agent Tool 아키텍처 문서화<br/>- 백엔드 파일 구조 업데이트 |
| **1.3.0** | **2025-12-10** | **AI-First 파이프라인 + 사용자 UI 통합**<br/>- 절충형 AI-First Template PPT 아키텍처 추가<br/>- 4단계 위자드 워크플로우 문서화<br/>- 프론트엔드 ↔ 백엔드 API 흐름 추가<br/>- 핵심 컴포넌트 요약표 추가 |

### v1.3.0 상세 변경 내역

#### 🆕 AI-First 파이프라인 구현
- **새 파일**: `backend/app/services/presentation/ai_ppt_builder.py` (184 lines)
  - `AIPPTBuilder` 클래스: AI 매핑 형식 → SimplePPTBuilder 형식 변환
  - `_convert_mappings()`: isEnabled 필터링, originalName 없어도 elementId로 매칭
  - `build_ppt_from_ai_mappings()`: 편의 함수

- **새 파일**: `backend/prompts/presentation/ai_direct_mapping_system.prompt`
  - AI 콘텐츠 매핑 생성용 시스템 프롬프트
  - 역할별 콘텐츠 가이드, 슬라이드 스타일별 최적화 지침

#### 🔧 프론트엔드 UI 통합
- **파일**: `frontend/src/pages/user/chat/components/presentation/PresentationOutlineModal.tsx`
- **연동 기능**:
  - Step 1 (setup): 템플릿 선택
  - Step 2 (generating_content): AI 콘텐츠 생성 (추론 단계 표시)
  - Step 3 (editor): 슬라이드별 텍스트 편집
  - Step 4 (preview): 결과 미리보기 + 다운로드

#### 🔧 백엔드 개선
- **파일**: `backend/app/agents/presentation/unified_presentation_agent.py`
  - `use_ai_builder=True` 기본값으로 변경
  - `_enrich_mappings_with_original_names()` 메서드 추가
    - 메타데이터에서 elementId → originalName 조회
    - UI 편집 데이터에 originalName 자동 추가
  - `_normalize_mappings_format()` 개선

- **파일**: `backend/app/services/presentation/simple_ppt_builder.py`
  - `_apply_table_mapping()`: 테이블 데이터 적용 (스타일 보존)
  - `_replace_table_cell_text()`: 셀별 텍스트 교체
  - `isEnabled` 체크: 비활성화된 매핑 스킵

#### 📊 해결된 문제
| 문제 | 원인 | 해결 방법 |
|------|------|-----------|
| 빈 객체에 텍스트 매핑 | 메타데이터 필터링 미흡 | `_is_decoration_element()` 필터링 |
| 테이블 내용 미적용 | SimplePPTBuilder 테이블 미지원 | `_apply_table_mapping()` 추가 |
| 아이콘+텍스트 슬라이드 | is_fixed 요소 수정됨 | `isEnabled=False` 체크 |
| 템플릿 텍스트 잔존 | AI 매핑 누락 | 프롬프트 개선 |
| 폰트 크기 변경 | 스타일 미보존 | 단락별 스타일 보존 로직 |
