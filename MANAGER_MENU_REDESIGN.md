# 🎯 WIKL 지식관리자 화면 메뉴 재정의 및 구조 개선안

## 📋 목차
1. [현재 상황 분석](#현재-상황-분석)
2. [문제점 및 개선 필요사항](#문제점-및-개선-필요사항)
3. [재정의된 메뉴 구조](#재정의된-메뉴-구조)
4. [각 메뉴의 역할과 기능](#각-메뉴의-역할과-기능)
5. [컴포넌트 구조 개선안](#컴포넌트-구조-개선안)
6. [구현 로드맵](#구현-로드맵)

---

## 1. 현재 상황 분석

### 1.1 현재 메뉴 구조 (ManagerLayout.tsx)
```typescript
const managerMenuItems = [
  { name: '관리 대시보드', path: '/manager', icon: BarChart3, id: 'dashboard' },
  { name: '컨테이너 관리', path: '/manager/containers', icon: FolderOpen, id: 'containers' },
  { name: '권한 승인', path: '/manager/permissions', icon: Lock, id: 'permissions' },
  { name: '문서 관리', path: '/manager/documents', icon: FileText, id: 'documents' }
];
```

### 1.2 현재 파일 구조
```
/pages/manager/
├── ManagerDashboard.tsx              # 대시보드 (통계, 빠른 링크)
├── ContainerManagement.tsx           # 컨테이너 관리
├── ContainerManagement.tsx.backup
├── Dashboard.tsx                     # 중복? (사용 여부 불명확)
├── DocumentManagement.tsx            # 문서 관리
├── PermissionApprovalManagement.tsx  # 사용 안 함?
├── UserPermissionManagement.tsx      # 권한 관리
├── UserPermissionManagement.tsx.backup
├── components/
│   └── DocumentAccessControlModal.tsx
├── container-management/
│   ├── index.tsx                     # 실제 컨테이너 관리 (새 구조)
│   └── components/
│       ├── ContainerForm.tsx
│       ├── ContainerHierarchy.tsx
│       ├── ContainerList.tsx
│       ├── ContainerStats.tsx
│       └── index.ts
└── permission-management/
    ├── index.tsx                     # 실제 권한 관리 (새 구조)
    ├── README.md
    └── components/
        ├── AddPermissionModal.tsx
        ├── PendingApprovals.tsx
        ├── PermissionFilters.tsx
        ├── PermissionInfoPanel.tsx
        ├── PermissionStats.tsx
        ├── PermissionTable.tsx
        └── index.ts
```

### 1.3 현재 기능 매핑

| 메뉴 | 현재 파일 | 실제 사용 | 탭 구조 | 주요 기능 |
|------|----------|----------|---------|----------|
| **관리 대시보드** | `ManagerDashboard.tsx` | ✅ | 없음 | 통계, 승인 대기, 품질 지표, 빠른 링크 |
| **컨테이너 관리** | `container-management/index.tsx` | ✅ | 목록/계층/통계 | 컨테이너 CRUD, 계층 구조 |
| **권한 승인** | `permission-management/index.tsx` | ✅ | 승인대기/권한현황/내요청 | 권한 요청 승인, 권한 부여/삭제 |
| **문서 관리** | `DocumentManagement.tsx` | ✅ | 없음 | 문서 승인/반려, 품질 관리 |

---

## 2. 문제점 및 개선 필요사항

### 2.1 구조적 문제점

#### ❌ 일관성 부족
- `container-management/`, `permission-management/`는 디렉토리 구조
- `DocumentManagement.tsx`는 단일 파일
- 백업 파일들 혼재 (`.backup`)
- 사용하지 않는 파일들 존재

#### ❌ 메뉴 역할 중복 및 불명확
- "권한 승인" 탭 내에 "승인 대기" + "권한 현황" 혼재
- "문서 관리"에 승인 기능 있음 (하지만 실제로는 즉시 등록)
- "내 요청" 탭이 지식관리자에게 필요한지 불명확

#### ❌ 문서 접근 권한 관리 부재
- 문서별 세부 권한 설정 불가
- 컨테이너 단위 권한만 존재
- 협업 문서 공유 메커니즘 없음

### 2.2 사용자 경험 문제

#### 지식관리자의 실제 업무 흐름
```
1. 대시보드에서 전체 현황 파악
   ↓
2. 권한 요청 승인/거부 처리
   ↓
3. 컨테이너 구조 관리
   ↓
4. 사용자 권한 직접 부여
   ↓
5. 문서 접근 권한 설정
   ↓
6. 품질 모니터링
```

현재 구조는 이 흐름을 잘 지원하지 못함.

---

## 3. 재정의된 메뉴 구조

### 3.1 새로운 4+1 메뉴 체계

```
┌─────────────────────────────────────────────────────────────┐
│  📊 관리 대시보드 (Overview)                                  │
│  - 목적: 전체 현황 파악 및 빠른 액션                          │
│  - 기능: 통계, 알림, 최근 활동, 빠른 링크                     │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  🗂️ 컨테이너 관리 (Container Management)                     │
│  - 목적: 지식 체계 구조화                                     │
│  - 기능: 컨테이너 CRUD, 계층 구조, 메타데이터                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  👥 사용자 권한 관리 (User Access Management)                │
│  - 목적: 사용자의 컨테이너 접근 권한 관리                     │
│  - 탭 1: 승인 대기 - 권한 요청 검토 및 승인/거부             │
│  - 탭 2: 권한 현황 - 부여된 권한 조회 및 직접 부여/삭제      │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  📄 문서 접근 관리 (Document Access Management)              │
│  - 목적: 문서별 세부 접근 권한 설정                           │
│  - 기능: 문서 목록, 접근 권한 설정, 공유 관리                │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  📈 품질 모니터링 (Quality Monitoring) - 선택적               │
│  - 목적: 문서 품질 및 사용 현황 분석                          │
│  - 기능: 품질 지표, 사용 통계, 개선 제안                      │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 메뉴 이름 변경 제안

| 기존 | 새 이름 | 영문 | 아이콘 | 이유 |
|------|---------|------|--------|------|
| 관리 대시보드 | **관리 대시보드** | Dashboard | 📊 BarChart3 | 유지 |
| 컨테이너 관리 | **컨테이너 관리** | Containers | 🗂️ FolderTree | 유지 |
| ~~권한 승인~~ | **사용자 권한 관리** | User Access | 👥 Users | 명확성 |
| ~~문서 관리~~ | **문서 접근 관리** | Document Access | 🔒 ShieldCheck | 역할 명확화 |
| (신규) | **품질 모니터링** | Quality | 📈 LineChart | 선택적 추가 |

---

## 4. 각 메뉴의 역할과 기능

### 4.1 📊 관리 대시보드

#### 역할
- 지식관리자의 일일 업무 시작점
- 전체 관리 현황 한눈에 파악
- 긴급 처리 필요 항목 알림

#### 주요 기능

**📈 핵심 지표 (4개 카드)**
```typescript
[팀 사용자 수: 45명] [관리 컨테이너: 12개] 
[대기 중 요청: 3건] [이번 달 문서: 156개]
```

**⚡ 빠른 액션**
```typescript
- 승인 대기 권한 요청 (3건) → 클릭 시 "사용자 권한 관리 > 승인 대기" 탭으로
- 새 문서 (15개) → 클릭 시 "문서 접근 관리"로
- 품질 검토 필요 (2건) → 클릭 시 "품질 모니터링"으로
```

**📊 차트 및 트렌드**
```typescript
- 주간 문서 업로드 추이 (라인 차트)
- 컨테이너별 문서 분포 (도넛 차트)
- 사용자 활동 히트맵 (주간/시간대)
```

**🔗 바로가기**
```typescript
- 컨테이너 관리로 이동
- 사용자 권한 관리로 이동
- 문서 접근 관리로 이동
```

#### 컴포넌트 구조
```
manager-dashboard/
├── index.tsx
└── components/
    ├── StatsCards.tsx          # 핵심 지표 카드
    ├── QuickActions.tsx        # 빠른 액션 리스트
    ├── TrendsChart.tsx         # 트렌드 차트
    ├── ActivityHeatmap.tsx     # 활동 히트맵
    ├── RecentActivities.tsx    # 최근 활동
    └── index.ts
```

---

### 4.2 🗂️ 컨테이너 관리

#### 역할
- 지식 체계의 구조화 및 분류
- 컨테이너 생성/수정/삭제/계층 관리
- 컨테이너 메타데이터 설정

#### 주요 기능

**탭 구조 (기존 유지)**
```
├── 컨테이너 목록 (List View)
│   - 카드형 목록
│   - 검색 및 필터
│   - 생성/수정/삭제
│
├── 계층 구조 (Tree View)
│   - 트리 뷰
│   - 드래그 앤 드롭
│   - 경로 표시
│
└── 통계 (Statistics)
    - 컨테이너별 문서 수
    - 컨테이너별 사용자 수
    - 용량 사용 현황
```

#### 개선 사항
- ✅ 현재 구조 유지 (잘 되어 있음)
- 컨테이너별 접근 로그 추가
- 컨테이너 템플릿 기능 (선택적)

#### 컴포넌트 구조 (현재 유지)
```
container-management/
├── index.tsx
└── components/
    ├── ContainerForm.tsx
    ├── ContainerHierarchy.tsx
    ├── ContainerList.tsx
    ├── ContainerStats.tsx
    └── index.ts
```

---

### 4.3 👥 사용자 권한 관리

#### 역할
- 사용자의 **컨테이너 단위 접근 권한** 관리
- 권한 요청 승인/거부
- 직접 권한 부여/삭제

#### 주요 기능

**탭 1: 승인 대기 (Pending Approvals)**
```typescript
역할: 사용자들이 요청한 컨테이너 권한을 검토하고 승인/거부

표시 정보:
- 요청일시
- 요청자 (이름, 부서, 사번)
- 요청 컨테이너
- 요청 권한 (읽기/쓰기/관리자)
- 요청 사유
- 작업 (승인/거부 버튼)

기능:
- 개별 승인/거부
- 일괄 승인/거부
- 거부 사유 입력
- 자동 승인 규칙 설정 (선택적)
```

**탭 2: 권한 현황 (Current Permissions)**
```typescript
역할: 현재 부여된 권한을 조회하고 직접 관리

표시 정보:
- 지식컨테이너
- 부서
- 사용자 (이름, 사번)
- 권한 (읽기/쓰기/관리자)
- 부여일
- 작업 (수정/삭제)

기능:
- 검색 및 필터 (컨테이너, 부서, 사용자)
- 권한 직접 부여 (사용자 검색 → 컨테이너 선택 → 권한 부여)
- 권한 수정 (권한 레벨 변경)
- 권한 삭제
- 통계 (총 사용자, 컨테이너, 권한 수)
```

**~~탭 3: 내 요청~~ (제거)**
```
이유: 지식관리자가 다른 컨테이너에 권한을 요청하는 경우가 드뭄
대안: 필요시 사용자 화면에서 요청 가능
```

#### 개선 사항
- ✅ 현재 구조 대부분 유지
- "내 요청" 탭 제거
- 승인 대기 탭을 첫 번째로 이동 (중요도)
- 통계 및 필터 개선

#### 컴포넌트 구조 (현재 거의 유지)
```
user-access-management/  (이름 변경)
├── index.tsx
└── components/
    ├── PendingApprovals.tsx      # 승인 대기 (기존)
    ├── PermissionTable.tsx       # 권한 현황 테이블 (기존)
    ├── AddPermissionModal.tsx    # 권한 추가 모달 (기존)
    ├── PermissionFilters.tsx     # 필터 (기존)
    ├── PermissionStats.tsx       # 통계 (기존)
    ├── PermissionInfoPanel.tsx   # 정보 패널 (기존)
    └── index.ts
```

---

### 4.4 📄 문서 접근 관리 (NEW 개선)

#### 역할
- **문서별 세부 접근 권한** 설정
- 문서 공유 및 협업 관리
- 민감 문서 보호

#### 주요 기능

**문서 목록**
```typescript
표시 정보:
- 문서 제목
- 작성자
- 작성일
- 컨테이너
- 접근 권한 상태:
  🌐 전체 공개 (컨테이너 권한 있는 모두)
  🔒 제한 공개 (N명)
  🚫 비공개 (관리자만)
- 조회수
- 작업 (권한 설정 버튼)

필터:
- 상태별 (전체공개/제한공개/비공개)
- 컨테이너별
- 작성자별
- 날짜별
```

**문서 접근 권한 설정 모달**
```typescript
1. 공개 범위 선택 (3단계)
   - 🟢 컨테이너 전체 공개 (기본)
     → 컨테이너 권한 있는 모든 사용자
   
   - 🟠 제한된 공개 (맞춤 설정)
     → 특정 사용자/부서/컨테이너만
   
   - 🔴 비공개 (관리자만)
     → 관리자 + 작성자만

2. 사용자/그룹 추가 (제한 공개 시)
   - 사용자 검색
   - 부서 선택
   - 다른 컨테이너 공유

3. 권한 레벨 설정
   - 읽기 전용 (뷰어만)
   - 다운로드 가능 (읽기 + 다운로드)
   - 편집 가능 (읽기 + 다운로드 + 편집)

4. 추가 옵션
   - 만료일 설정
   - 알림 발송
```

**통계**
```typescript
[전체 문서: 156] [전체 공개: 120] [제한 공개: 25] [비공개: 11]
[공유된 문서: 15] [이번 달 신규: 12]
```

**사용 시나리오**
```typescript
시나리오 1: 프로젝트 협업 문서
- 문서: "2025 시스템 개선 제안서"
- 설정: 제한 공개
- 접근 허용:
  - 홍길동 (기획팀) - 편집 가능
  - 김인사 (HR팀) - 다운로드 가능
  - 인프라팀 컨테이너 - 읽기 전용

시나리오 2: 민감한 초안 문서
- 문서: "2025년 예산 계획안 (초안)"
- 설정: 비공개
- 접근: 관리자 + 작성자만
- 검토 완료 후 → 제한 공개로 변경

시나리오 3: 일반 가이드 문서
- 문서: "AWS 사용 가이드"
- 설정: 전체 공개 (기본)
- 접근: MS서비스팀 전체
```

#### 개선 사항
- ✅ `DocumentAccessControlModal.tsx` 이미 생성됨
- `DocumentManagement.tsx` 재작성
- 단일 파일 → 디렉토리 구조로 변경
- 승인 기능 제거 (즉시 등록 유지)
- 접근 권한 관리 기능 추가

#### 컴포넌트 구조 (신규)
```
document-access-management/
├── index.tsx
└── components/
    ├── DocumentList.tsx              # 문서 목록
    ├── DocumentFilters.tsx           # 필터
    ├── DocumentStats.tsx             # 통계
    ├── DocumentAccessControlModal.tsx # 권한 설정 모달 (이미 생성)
    ├── AccessStatusBadge.tsx         # 권한 상태 배지
    └── index.ts
```

---

### 4.5 📈 품질 모니터링 (선택적)

#### 역할
- 문서 품질 및 사용 현황 분석
- 개선이 필요한 문서 식별
- 사용 패턴 분석

#### 주요 기능

**품질 지표**
```typescript
- 평균 평점
- 오래된 문서 (6개월 이상 미수정)
- 낮은 평점 문서 (3점 미만)
- 접근 오류 문서
- 누락된 메타데이터
```

**사용 통계**
```typescript
- 인기 문서 Top 10
- 사용 빈도가 낮은 문서
- 검색되지 않는 문서
- 다운로드 통계
```

**개선 제안**
```typescript
- 업데이트 필요 문서 목록
- 통합 가능한 중복 문서
- 태그 추가 필요 문서
```

#### 컴포넌트 구조 (신규 - 선택적)
```
quality-monitoring/
├── index.tsx
└── components/
    ├── QualityMetrics.tsx        # 품질 지표
    ├── UsageStats.tsx            # 사용 통계
    ├── ImprovementSuggestions.tsx # 개선 제안
    └── index.ts
```

---

## 5. 컴포넌트 구조 개선안

### 5.1 최종 디렉토리 구조

```
/frontend/src/pages/manager/
├── dashboard/                        # 📊 관리 대시보드 (신규 디렉토리)
│   ├── index.tsx
│   └── components/
│       ├── StatsCards.tsx
│       ├── QuickActions.tsx
│       ├── TrendsChart.tsx
│       ├── ActivityHeatmap.tsx
│       ├── RecentActivities.tsx
│       └── index.ts
│
├── container-management/             # 🗂️ 컨테이너 관리 (기존 유지)
│   ├── index.tsx
│   └── components/
│       ├── ContainerForm.tsx
│       ├── ContainerHierarchy.tsx
│       ├── ContainerList.tsx
│       ├── ContainerStats.tsx
│       └── index.ts
│
├── user-access-management/           # 👥 사용자 권한 관리 (이름 변경)
│   ├── index.tsx
│   └── components/
│       ├── PendingApprovals.tsx
│       ├── PermissionTable.tsx
│       ├── AddPermissionModal.tsx
│       ├── PermissionFilters.tsx
│       ├── PermissionStats.tsx
│       ├── PermissionInfoPanel.tsx
│       └── index.ts
│
├── document-access-management/       # 📄 문서 접근 관리 (신규)
│   ├── index.tsx
│   └── components/
│       ├── DocumentList.tsx
│       ├── DocumentFilters.tsx
│       ├── DocumentStats.tsx
│       ├── DocumentAccessControlModal.tsx
│       ├── AccessStatusBadge.tsx
│       └── index.ts
│
└── quality-monitoring/               # 📈 품질 모니터링 (선택적)
    ├── index.tsx
    └── components/
        ├── QualityMetrics.tsx
        ├── UsageStats.tsx
        ├── ImprovementSuggestions.tsx
        └── index.ts
```

### 5.2 삭제할 파일

```
❌ /pages/manager/ManagerDashboard.tsx      → dashboard/index.tsx로 이동
❌ /pages/manager/Dashboard.tsx             → 중복, 삭제
❌ /pages/manager/DocumentManagement.tsx    → document-access-management/로 재작성
❌ /pages/manager/PermissionApprovalManagement.tsx → 사용 안 함
❌ /pages/manager/UserPermissionManagement.tsx → user-access-management/index.tsx (이미 있음)
❌ /pages/manager/*.backup                  → 백업 파일 삭제
❌ /pages/manager/components/               → 각 모듈의 components/로 이동
```

### 5.3 이름 변경

```
permission-management/ → user-access-management/
(DocumentManagement.tsx) → document-access-management/
```

---

## 6. 구현 로드맵

### Phase 1: 정리 및 재구조화 (1일)

✅ **Step 1.1: 백업 및 사용하지 않는 파일 삭제**
```bash
# 백업 파일 삭제
rm /pages/manager/*.backup
rm /pages/manager/PermissionApprovalManagement.tsx
rm /pages/manager/Dashboard.tsx
```

✅ **Step 1.2: 디렉토리 이름 변경**
```bash
# permission-management → user-access-management
mv permission-management user-access-management
```

✅ **Step 1.3: ManagerLayout.tsx 메뉴 업데이트**
```typescript
const managerMenuItems = [
  { name: '관리 대시보드', path: '/manager', icon: BarChart3, id: 'dashboard' },
  { name: '컨테이너 관리', path: '/manager/containers', icon: FolderTree, id: 'containers' },
  { name: '사용자 권한 관리', path: '/manager/user-access', icon: Users, id: 'user-access' },
  { name: '문서 접근 관리', path: '/manager/document-access', icon: ShieldCheck, id: 'document-access' },
  // { name: '품질 모니터링', path: '/manager/quality', icon: LineChart, id: 'quality' }, // 선택적
];
```

### Phase 2: 대시보드 모듈화 (1일)

✅ **Step 2.1: dashboard/ 디렉토리 생성**
```bash
mkdir dashboard
mkdir dashboard/components
```

✅ **Step 2.2: ManagerDashboard.tsx 분할**
- `StatsCards.tsx`: 핵심 지표 카드
- `QuickActions.tsx`: 빠른 액션 리스트
- `TrendsChart.tsx`: 트렌드 차트
- `RecentActivities.tsx`: 최근 활동
- `index.tsx`: 메인 컴포넌트

### Phase 3: 사용자 권한 관리 개선 (0.5일)

✅ **Step 3.1: "내 요청" 탭 제거**
```typescript
// user-access-management/index.tsx
const [activeTab, setActiveTab] = useState<'pending' | 'permissions'>('pending');
```

✅ **Step 3.2: 탭 순서 조정**
- 승인 대기 (첫 번째)
- 권한 현황 (두 번째)

### Phase 4: 문서 접근 관리 구현 (2일)

✅ **Step 4.1: document-access-management/ 디렉토리 생성**

✅ **Step 4.2: 컴포넌트 생성**
- `DocumentList.tsx`
- `DocumentFilters.tsx`
- `DocumentStats.tsx`
- `AccessStatusBadge.tsx`
- `index.tsx`

✅ **Step 4.3: DocumentAccessControlModal 통합**
```typescript
// 이미 생성된 모달을 components/로 이동
mv /pages/manager/components/DocumentAccessControlModal.tsx \
   document-access-management/components/
```

✅ **Step 4.4: 백엔드 API 추가 필요**
```typescript
// 필요한 API 엔드포인트
POST /api/v1/documents/{id}/access-rules      # 접근 규칙 설정
GET  /api/v1/documents/{id}/access-rules      # 접근 규칙 조회
GET  /api/v1/documents?access_level=public    # 문서 목록 (접근 레벨 필터)
```

### Phase 5: 라우팅 업데이트 (0.5일)

✅ **Step 5.1: App.tsx 라우트 업데이트**
```typescript
<Route path="/manager/*" element={...}>
  <Route index element={<Dashboard />} />
  <Route path="containers" element={<ContainerManagement />} />
  <Route path="user-access" element={<UserAccessManagement />} />
  <Route path="document-access" element={<DocumentAccessManagement />} />
  {/* <Route path="quality" element={<QualityMonitoring />} /> */}
</Route>
```

✅ **Step 5.2: ManagerLayout.tsx 컴포넌트 임포트 업데이트**
```typescript
import Dashboard from '../pages/manager/dashboard';
import ContainerManagement from '../pages/manager/container-management';
import UserAccessManagement from '../pages/manager/user-access-management';
import DocumentAccessManagement from '../pages/manager/document-access-management';
```

### Phase 6: 테스트 및 문서화 (1일)

✅ **Step 6.1: 기능 테스트**
- 각 메뉴 네비게이션
- 컴포넌트 렌더링
- API 연동

✅ **Step 6.2: 문서 작성**
- 각 모듈별 README.md
- 컴포넌트 사용 가이드

---

## 7. 핵심 개선 요약

### 7.1 구조적 개선

| 개선 항목 | 변경 전 | 변경 후 |
|----------|---------|---------|
| **일관성** | 단일 파일 + 디렉토리 혼재 | 모두 모듈 디렉토리 구조 |
| **메뉴 명칭** | 권한 승인, 문서 관리 | 사용자 권한 관리, 문서 접근 관리 |
| **탭 구조** | 승인대기/권한현황/내요청 | 승인대기/권한현황 (내요청 제거) |
| **문서 권한** | 없음 | 문서별 접근 권한 설정 추가 |

### 7.2 기능적 개선

**사용자 권한 관리 (컨테이너 단위)**
- 누가 어떤 컨테이너에 접근할 수 있는가?
- 승인 대기 → 권한 현황 순서로 중요도 반영

**문서 접근 관리 (문서 단위)**
- 누가 어떤 문서를 볼/다운로드/편집할 수 있는가?
- 3단계 공개 범위 (전체/제한/비공개)
- 협업 문서 특정인 공유
- 민감 문서 보호

### 7.3 사용자 경험 개선

**명확한 역할 분리**
```
컨테이너 관리: 지식 체계 구조화
    ↓
사용자 권한 관리: 컨테이너 접근 권한
    ↓
문서 접근 관리: 문서별 세부 권한
    ↓
품질 모니터링: 품질 및 사용 현황
```

**직관적인 워크플로우**
```
1. 대시보드에서 "승인 대기 3건" 확인
2. "사용자 권한 관리 > 승인 대기" 탭에서 처리
3. 필요시 "권한 현황" 탭에서 직접 권한 부여
4. "문서 접근 관리"에서 민감 문서 접근 제한
5. "품질 모니터링"에서 전체 품질 확인
```

---

## 8. 다음 단계 액션 아이템

### 우선순위 HIGH
1. ✅ 메뉴 이름 변경 (ManagerLayout.tsx)
2. ✅ permission-management → user-access-management 이름 변경
3. ✅ "내 요청" 탭 제거
4. ✅ DocumentManagement를 document-access-management로 재작성
5. ✅ 백엔드 API 엔드포인트 추가 (문서 접근 규칙)

### 우선순위 MEDIUM
6. ✅ ManagerDashboard를 dashboard/ 모듈로 분할
7. ✅ 통계 및 차트 개선
8. ✅ 품질 모니터링 메뉴 추가 (선택적)

### 우선순위 LOW
9. ✅ 각 모듈별 README.md 작성
10. ✅ 컴포넌트 Storybook 문서화
11. ✅ E2E 테스트 작성

---

## 9. 결론

### 개선의 핵심
1. **명확한 역할 분리**: 각 메뉴가 독립적이고 명확한 기능 수행
2. **일관된 구조**: 모든 메뉴가 동일한 디렉토리 패턴
3. **사용자 중심**: 지식관리자의 실제 업무 흐름 반영
4. **확장 가능**: 새로운 기능 추가가 용이한 구조

### 기대 효과
- 🎯 **사용성 향상**: 직관적인 메뉴 구조와 명칭
- 🔧 **유지보수성**: 모듈화된 컴포넌트 구조
- 🚀 **확장성**: 새로운 기능 추가 용이
- 📊 **일관성**: 통일된 개발 패턴

