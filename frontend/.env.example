# ===========================================
# Frontend .env 예제
# ===========================================
# 이 파일을 복사하여 frontend/.env.local(권장) 또는 frontend/.env로 사용하세요.
#
# 핵심 원칙
# - CRA(React) 환경변수는 "빌드 시점"에 번들에 고정됩니다.
# - 따라서 기본값은 어디서 빌드해도 안전하게 동작하도록 "빈 값(상대경로)"을 권장합니다.
#
# API URL 규칙 (중요)
# - 권장: REACT_APP_API_URL="" (빈 값) → 브라우저는 /api/... 로 호출
#   - 로컬 개발(npm start): setupProxy.js가 /api 를 백엔드로 프록시
#   - 운영(nginx): nginx가 /api 를 백엔드로 프록시
# - 절대 URL을 쓰고 싶으면: http(s)://host[:port]
#   - ❌ 끝에 /api 를 붙이지 마세요 (예: http://host:8000/api)
#   - ❌ 값 자체를 /api 로 두지 마세요 (이전 /api/api 이슈 원인)
REACT_APP_API_URL=

# 개발 서버 프록시 타깃 (Node에서만 사용, 브라우저 번들에 노출되지 않음)
# - npm start (로컬): 기본값 http://localhost:8000
# - docker-compose에서 CRA dev server를 쓰는 경우(권장): http://backend:8000
API_PROXY_TARGET=

# WebSocket URL (선택)
# - 권장: 빈 값 → 현재 origin 기반으로 자동 구성(ws/wss 자동 전환)
# - 필요 시 절대 URL: ws(s)://host[:port]
#   - 끝에 /ws 또는 /api 를 붙이지 마세요 (코드가 경로를 붙입니다)
REACT_APP_WS_URL=

# 환경 식별자 (로그/표시용)
REACT_APP_ENV=development

# 디버그 설정
REACT_APP_DEBUG=false
REACT_APP_LOG_API_REQUESTS=false

# 채팅 세션 TTL (분 단위)
REACT_APP_CHAT_SESSION_TTL_MINUTES=30

# 앱 정보
REACT_APP_TITLE=WKMS (Development)
REACT_APP_VERSION=1.0.0-dev
