# ===========================================
# 프론트엔드 환경 설정 예제
# ===========================================
# 이 파일을 복사하여 .env 또는 .env.local로 만들고 값을 설정하세요

# 백엔드 API 서버
# 로컬 개발: 주석 처리 (setupProxy.js가 /api 경로를 프록시)
# Docker/프로덕션: 활성화 (직접 백엔드 호출 또는 Nginx 경유)
# REACT_APP_API_URL=http://localhost:8000
REACT_APP_API_URL=http://<YOUR_SERVER_IP>:8000

# WebSocket 엔드포인트 (STT, 실시간 채팅용)
# 로컬 개발: ws://localhost:8000
# Docker/프로덕션: ws://<YOUR_SERVER_IP>:8000 또는 Nginx 프록시 경유
REACT_APP_WS_URL=ws://<YOUR_SERVER_IP>:8000

# 환경 식별자
REACT_APP_ENV=development

# 디버그 설정
REACT_APP_DEBUG=false
REACT_APP_LOG_API_REQUESTS=false

# 채팅 세션 TTL (분 단위, 기본값: 30분)
# REACT_APP_CHAT_SESSION_TTL_MINUTES=30

# 앱 정보
REACT_APP_TITLE=WKMS (Development)
REACT_APP_VERSION=1.0.0-dev
