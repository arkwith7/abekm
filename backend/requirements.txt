# 웹 프레임워크
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# 데이터 검증 및 설정
pydantic==2.11.7
pydantic-settings==2.10.1
python-dotenv==1.0.0

# 데이터베이스 관련
sqlalchemy==2.0.23
psycopg2-binary==2.9.9
asyncpg==0.29.0
pgvector==0.4.1
alembic==1.16.5

# HTTP 클라이언트
requests==2.32.5
httpx==0.28.1

# 인증 및 보안
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
email-validator==2.3.0

# AWS SDK
# LangChain AWS 1.x가 boto3/botocore 최신 범위를 요구합니다.
boto3==1.42.12
botocore==1.42.12
amazon-transcribe==0.6.2  # AWS Transcribe Streaming (실시간 STT)

# Azure SDK
# azure-ai-formrecognizer==3.3.0  # Deprecated - FIGURES 미지원
azure-ai-documentintelligence==1.0.0  # GA (2024-11-30) - FIGURES 지원
azure-identity==1.15.0
azure-storage-blob==12.23.1

# Upstage Document Parse API (Azure DI 대안)
upstage==0.1  # Document Parse, Layout Analysis - REST API 직접 호출 방식

# LLM 및 AI 프레임워크 (최신 호환 버전)
# LangChain OpenAI 1.x는 openai>=1.109.1 을 요구합니다.
openai==1.109.1
# LangChain 1.x + LangGraph 1.x (Agentic/Graph 프로덕션 전환 기반)
langchain==1.2.0
langchain-core==1.2.2
langchain-openai==1.1.5
langchain-aws==1.1.0
langchain-community==0.4.1
langchain-text-splitters==1.1.0
langgraph==1.0.5
langgraph-checkpoint>=2.1.0,<4.0.0
langgraph-checkpoint-postgres>=3.0.0,<4.0.0
langgraph-sdk>=0.3.0,<1.0.0
langsmith==0.5.0
duckduckgo-search==5.3.0  # 인터넷 검색 도구 (폴백)
tavily-python>=0.5.0  # Tavily AI 검색 도구 (권장)

# 비동기 및 파일 처리
aiofiles==24.1.0
aiosqlite==0.20.0
psutil==5.9.8
nest_asyncio==1.6.0  # Celery에서 비동기 함수 실행 지원

# Redis (실시간 채팅 및 캐싱)
redis[hiredis]==5.0.1
aioredis==2.0.1

# Celery (비동기 백그라운드 작업)
celery==5.3.4
flower==2.0.1  # Celery 모니터링 (선택)

# 로깅
loguru==0.7.3

# 데이터 분석 (Jupyter 노트북 테스트용)
pandas==2.2.0
numpy==1.26.3

# 한국어 NLP
konlpy==0.6.0
JPype1==1.4.1
kss==6.0.3  # Korean Sentence Splitter
kiwipiepy==0.18.0  # 한국어 형태소 분석 (Mecab 대안)

# 영어 NLP (다국어 검색 지원)
nltk==3.8.1  # 경량 영어 NLP 라이브러리
pyspellchecker==0.7.2  # 간단한 영어 오탈자 교정

# 문서 처리 라이브러리 (MS Office + HWP)
python-docx>=0.8.11
openpyxl>=3.1.0
python-pptx>=0.6.21
olefile>=0.46
PyPDF2>=3.0.0
pdfplumber>=0.11,<0.12
pdfminer.six==20231228
unoconv>=0.9.0  # LibreOffice headless 변환
pytesseract>=0.3.10  # OCR 엔진
Pillow>=10.0.0  # 이미지 처리 (OCR용)
pdf2image>=1.17.0  # PDF/PPT 슬라이드 이미지 변환
ImageHash>=4.3.1  # 이미지 퍼셉추얼 해시 (유사 이미지 검색용)

# HWP 파일 처리 (대안)
lxml>=4.9.0  # HWPX XML 파싱
zipfile-deflate64>=0.2.0  # 대용량 ZIP 파일 지원

# 테스트
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
